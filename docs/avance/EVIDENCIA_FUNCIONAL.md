# Evidencia funcional — matriz inicial

Estados: `YA_EXISTE_POR_VERIFICAR`, `PENDIENTE`, `EN_CURSO`, `IMPLEMENTADO`, `VERIFICADO`, `BLOQUEADO_EXTERNO`. «Existe» describe código o antecedente; no implica aceptación de todo el flujo.

| Flujo | Reutilización acreditable | Brecha principal | Evidencia a producir | Estado |
|---|---|---|---|---|
| FLU-01 · proyecto durable | `gestor-documentos`, repositorio IndexedDB, snapshots, QA multiproyecto V9 | estado visible de revisión/error y búsqueda cotidiana | dos documentos independientes, copia, error de almacenamiento y reapertura desde UI | EN_CURSO |
| FLU-02 · componente propio | definiciones con perfiles, assets SHA-256, snapshot de instancia y QA contactor | asistente reversible, montaje/puertos completos y vínculo técnico versionado | dos proyectos, edición de definición, imagen independiente, importación limpia | EN_CURSO |
| FLU-03 · esquema/tablero | `esquema-svg`, `esquema-pdf`, topología del proyecto | edición diaria multihoja sin segunda conectividad | edición desde esquema, cruce no conectado, undo, PDF | PENDIENTE |
| FLU-04 · simular/analizar | motores V2–V9 y QA industrial/física/Ingeniería | cobertura de casos objetivo y oráculos independientes | SIM-01…10, calidad de datos y límites | YA_EXISTE_POR_VERIFICAR |
| FLU-05 · propuesta V9 | `src/diseno-asistido`, preview/aplicación transaccional | integración y vigencia en nuevos flujos | BASE conservada, invalidación y decisión aplicada | YA_EXISTE_POR_VERIFICAR |
| FLU-06 · montaje/cables | escena Three.js, edición/canaletas/mazo existentes | anclaje, XYZ manual fiel, ruta única/migración y 3D profesional | fixtures V9, drag real, colisiones informativas, densidad | PENDIENTE |
| FLU-07 · revisión/documentos | exportadores, dossier, Ingeniería | paquete coherente y revisión obsoleta visible | identidad/cantidades, PDF renderizado, CSV/HTML hostil | PENDIENTE |
| FLU-08 · CAD/instalación | `app/mundo*`, extractor y derivado existente | pipeline trazable, unidad/Z, vínculos y derechos | manifiesto y QA sintético; original privado local | PENDIENTE |

## Activos y entorno de partida

- DWG privado: 22.657.062 bytes, cabecera `AC1032`, SHA-256 `7d2e1adefee31d4917d0cc6aba7f033b2ff7d89e7d378ebe04295ec04a47d1de`; inspección de contenido geométrico y licencia pendientes. No copiar al árbol público.
- `datos/cubierta.json` existente dice derivar de `Cubierta.dxf`, informa alturas supuestas, pero no identifica hash/revisión del original ni derechos. La relación con el DWG actual es `NO VERIFICADA`.
- Referencia de pruebas V9: 1412 unitarias, gate histórico 13/267 y entrega offline reportados para `4c2924c`; no son resultados del candidato funcional nuevo.
- Candidato M1 antes de checkpoint: typecheck y compilación TypeScript verdes; 1419/1419 tests unitarios en 60,9 s, 0 fallos y 0 omitidos. Build QA de Vite: 399 módulos, 5,85 s. `qa/flujo-m1.mjs`: 6/6 comprobaciones, 0 errores JS, ~18,4 s; `qa/componentes-personalizados.mjs`: recorrido completo de borrador, búsqueda/filtro, contactor, portabilidad y equivalencia eléctrica verde, 0 errores JS. Se usó Chromium instalado explícitamente, sin instalar dependencias. Esta evidencia deberá repetirse si cambia la frontera probada.
- Hallazgo y corrección M1: al copiar un ejemplo, la revisión inicial almacenada no contenía los números de conductor que ya se mostraban en el editor después del montaje. La creación ahora numera el candidato antes de persistirlo; el test de gestor exige equivalencia entre primera revisión y vista. El QA de copia conserva todos los campos declarados del ejemplo y comprueba identidad/contenido durable sin rechazar defaults legítimos añadidos por el cargador.
- Riesgo CMP-06 que motivó el incremento: una instancia conserva `{definicionId, revision}` y una copia profunda de su función, mientras la biblioteca anterior solo conservaba latest por ID. El exportador empaquetaba esa versión aunque el proyecto referenciara otra. El archivo append-only y el cierre exacto eliminan esa sustitución silenciosa para nuevas revisiones; **no** resuelven historial perdido antes de V3 ni adopción explícita de cambios por instancias conectadas.
- Incremento CMP-06: archivo append-only de revisiones y migración IndexedDB V2→V3 probada en Chromium real (2/2, 0 errores JS). Los tests de repositorio/codec comprueban actualización r1→r2, exportación exacta, despublicación sin pérdida del histórico, importación r1 con latest local r2, rollback y rechazo de paquetes falsos/incompletos. El paquete V1 aún no representa dos revisiones de un mismo ID; ese caso falla explícitamente. Las revisiones anteriores sobrescritas antes de V3 no se recuperan por inferencia.
- Incremento integrado M1: TypeScript y build QA (401 módulos, 5,87 s) verdes; 1442/1442 unitarias en 82,4 s, 0 fallos/omitidos. `qa/flujo-m1.mjs` 9/9 (A/B, búsqueda, revisión visible, copia de ejemplo y contenido durable), `qa/estado-documento-m1.mjs` 10/10 (nombre/revisión, fallo IndexedDB, reintento, solo lectura y barra a 1024/1366/1920), `qa/componentes-teclado.mjs` 12/12 (foco, inercia, Escape, borrador, lectura lenta/fallida de imagen), `qa/revisiones-componentes-idb.mjs` 2/2; 0 errores JS. La importación individual ahora confirma asset+definición+histórico atómicamente; tests de repositorio 35/35. El archivo `.tscomp` V1 de r2+ se recrea como r1 local y la UI lo informa; NO conserva el historial original.
- `qa/componentes-personalizados.mjs` completó creación PNG, 11 terminales/cables, simulación de polo/NA/NC, recarga, export/import de proyecto y comparación nativa: `TODO OK`, 0 errores JS, **antes** de la nueva importación individual atómica; repetir su frontera antes de aceptar M1. La identidad del `.tscomp` reimportado se confirmó como copia nueva porque la original archivada no puede reciclarse. `qa/multiproyecto.mjs` completó A/B/ejemplo/snapshots/restauración/duplicado/eliminación/reapertura: `TODO OK`, 0 errores JS; ese resultado no se invalida por el cambio del importador individual. Son comprobaciones focales, no campaña M9 ni aceptación humana.
- `qa/correcciones.mjs` auxiliar encontró un falso negativo del harness: solo preguntaba por el primer punto agarrable de cada cable, luego lo descartaba si caía fuera de zona, y omitía gestos bajo `if`. Ahora espera copia/rutas reales, busca puntos válidos con margen y muestra creciente, exige los gestos sin saltarlos y cierra Chromium/servidor. Ejecución focal posterior: 29/29, 0 errores JS, ~107 s. No se atribuye ese fallo inicial al producto.
- Perfil de rendimiento M0 (medición asistida, una ejecución): Windows 10.0.26200, i5-14400/16 hilos, 15,7 GB RAM; Chromium 153.0.8010.12, 1440×900, DPR 1, render SwiftShader (GPU física no verificada). En «Arranque estrella-triángulo» (17 aparatos, 61 conductores): editor interactivo 1,364 s, abrir ejemplo con confirmación visible 9,336 s, heap JS 20,5→112,3 MB, 30 cuadros mediana 8,1 ms y peor 9,4 ms, validación visible 6,284 s, 0 errores JS. No es R1 (30/100), ni p95 estadístico de apertura. El Chromium instalado no coincide con la revisión pedida por Playwright 1.61.1 y se indicó ejecutable explícito; no se instaló otra versión.
- Medición M0 complementaria en 1366×768, mismo ejemplo y Chromium SwiftShader (tiempos end-to-end Playwright+render, no solo handler): selección frontal 20 muestras p50 703 ms/p95 777 ms; drag frontal del piloto R, seis pasos, 8 muestras p50 6,98 s/p95 7,17 s; confirmación de autoguardado tras soltar 8 muestras p50 736 ms/p95 767 ms; Guardar/exportar JSON por botón 10 muestras p50 364 ms/p95 373 ms. 0 errores JS, navegador y servidor cerrados. El drag es una señal de riesgo para M5/M6 y requiere perfilado propio antes de atribuir causa. Captura del modal «Mis Tableros» a 1366×768 inspeccionada: búsqueda/acciones visibles sin recortes; aceptación visual humana pendiente.
- Escena sintética R1 en `qa/lib/densidad-r1.mjs`: 30 aparatos/100 conductores, extremos y bornes coherentes, 100/100 geometrías renderizadas, fixture SHA-256 `f860d627ebd6ed926b5bcddcc715ebd862efd71469e0ea67f2523311fa77523f`; **no es un tablero certificado**. `qa/benchmark-r1.mjs` está fuera del gate. En Windows/i5-14400/SwiftShader (NO GPU física), segunda ejecución separada de 707,1 s: importar 15,0 s; reapertura n=2 p50 34,0/p95 34,6 s; selección n=5 p50 3,70/p95 3,83 s; drag real de seis pasos n=5 p50 38,25/p95 38,63 s; flush n=5 p50 2,57/p95 8,73 s; Guardar JSON n=5 p50 26,76/p95 27,09 s; validar n=3 p50 15,97/p95 16,22 s; render directo n=30 p95 33,8 ms. Hubo 134 long tasks durante drag/persistencia (máximo 12,34 s), 0 errores JS, Chromium/servidor cerrados. **Rendimiento R1 NO ACEPTADO**; falta optimización causal y prueba en GPU física. El primer intento de benchmark fue rojo por un gesto inválido del harness (aux-b no estaba seleccionado), corregido seleccionándolo antes de arrastrar; sus cinco reaperturas p50 32,52/p95 39,52 s y selecciones p50 3,61/p95 3,80 s se conservan como run distinto, no se mezclan con los cuantiles finales.
- Perfil causal corto R1 (`R1_FOCAL=1`, ejecución separada de n=1, mismo fixture/hash): selección host 3,77 s pero listeners canvas 7,1 ms en down y 55,9 ms en up; drag host 27,60 s, seis movimientos 14,17 s y suelta 11,83 s. El listener de `pointerup` ocupó 10,97 s; contadores del editor registraron un reparto, una firma y **98/100 tubos reconstruidos** al soltar. CDP midió 11,48 s de script y 1,7 ms de layout durante el drag; las seis llamadas pointermove consumieron en el canvas p95 0,8 ms, de modo que no se debe culpar a ese handler sin más análisis de render/sincronización. Confirmar persistencia costó 708,5 ms al host frente a 0,5 ms dentro del navegador, compatible con coste de protocolo/serialización, no prueba de autosave lento. 30 long tasks (peor 10,97 s), 0 errores JS, proceso Chromium/servidor cerrados. Exploratorio: Playwright 1.61.1 usó Chromium 1243 explícito por falta de la revisión esperada; WebGL fue SwiftShader, **no certifica GPU física ni demuestra p95 estadístico con n=1**. Prioridad causal: aislar el coste de reconstrucción completa al soltar, preservando reparto, longitudes, picking y undo.
- Segundo perfil focal R1, separado del anterior y con igual fixture/hash: el hook de rutas dio 98 trazados antes/después de mover `aux-b`; **solo 10 cambiaron puntos** y 88 conservaron puntos/radio/color declarado, mientras se reconstruyeron 98 tubos. Los 10 fueron `dol-w3`, `dol-w28` y ocho reservas del bornero; radio y color no cambiaron. La comparación usa puntos que el hook redondea a 0,1 mm: demuestra una oportunidad de geometría incremental, pero no equivale a una comparación bit a bit de floats internos ni a una prueba de identidad visual. Drag host 26,32 s, `pointerup` canvas 10,565 s, CDP script 11,00 s, 0 errores JS y Chromium/servidor cerrados. Mantener reparto global determinista y medir una reconciliación de mallas por firma exacta antes de aceptarla.
- Reconciliación R1 (`6f4b222`, perfil focal nuevo n=1, mismo fixture/hash, SwiftShader): el cálculo global conserva 10 rutas cambiadas y 88 estables; se reconstruyeron **10 tubos para esas 10 rutas**, frente a 98 anteriormente. El desglose del `pointerup` fue 11.846,5 ms en canvas, de los cuales `rutasDeCables` empleó 11.788,4 ms y reconciliar geometría solo 14,6 ms. Drag host 31.273,9 ms: **no hay reducción de latencia demostrada** frente a las ejecuciones anteriores ni p95 estadístico. Picking humano de `dol-w3`, hover, undo/redo de rutas idénticas, 100 geometrías tras redo, 0 errores JS y cierre de Chromium/servidor fueron correctos. `qa/cables.mjs` mantuvo 63/63 cables y clones de resaltado estables 5→5→5→5 durante cuatro movimientos de preview (0 JS). Los 5 tests unitarios de reconciliación pasaron. El cuello medido es el repartidor global, no la construcción de mallas; R1 **sigue NO ACEPTADO** y requiere intervención focal posterior sin sacrificar determinismo/ocupación, además de GPU física.
- M1 focal: asistente de seis pasos `qa/asistente-componentes-m1.mjs` 19/19, biblioteca con teclado 12/12, adopción A/B 24/24 y `qa/componentes-personalizados.mjs` largo exit 0, todos sin errores JS. Capturas de Bornes y Revisión inspeccionadas a 1024/1366 px: al entrar los pasos quedan visibles, filas de bornes accesibles por scroll y Validar/Guardar en el pie final. La inspección visual humana del usuario sigue pendiente.
- M1 mecánica: `qa/montaje-componente-m1.mjs` ampliado 31/31 y build QA 406 módulos verdes, 0 errores JS: rechazo visible sobre canaleta, colocación atornillada sin DIN, anclajes, picking de la malla en la posición del modelo, drag a canaleta y riel con reversión exacta, drag a hueco válido sin `rielId`, inspector y persistencia/picking tras recarga; legacy marcado NO_EVALUABLE. La ampliación descubrió un **bug real**, no un timeout: `reconstruirDispositivoUno` omitía actualizar el registro `escenario.aparatos`, por lo que picking, arrastre y malla podían quedar desacoplados del modelo. Se corrigió el registro y las etiquetas junto con la regla de arrastre de placa. El primer intento de esta ampliación quedó rojo tras 10 checks por no encontrar punto real de agarre; se conservó como diagnóstico y no se atribuyó erróneamente al harness.
- M1 ficha técnica y portabilidad, frontera previa a UI de bloques: 169/169 pruebas focales del núcleo con V8 PRODUCTO+curva exactos, hashes/familia/cierre de dependencias, V1 legado, `.tscomp` V2, paquete proyecto V3 y rollback de importación. QA de ficha visible 11/11, 0 errores JS: selección de ficha, origen/procedencia y exportación/importación individual en almacenamiento limpio. Esa ejecución aún no acreditaba puertos multibloque ni aceptación visual humana; el incremento posterior se informa debajo.
- M1 puertos/ficha tras integración UI: typecheck app/core, build QA de 406 módulos y 33/33 tests focales verdes. `qa/asistente-componentes-m1.mjs` 19/19 y `qa/ficha-componente-m1.mjs` 18/18, 0 fallos, timeouts, skipped o errores JS; el segundo comprobó ID eléctrico distinto de rótulo, ancla u/v sobre imagen, bloque con lado/orden/tramo, avisos NO_EVALUABLE, ficha PRODUCTO V8 exacta y `.tscomp` V2 reimportado con activo en almacenamiento limpio. Capturas a 1024 px de bloques y ficha inspeccionadas: controles y pie alcanzables por scroll; no equivalen a aceptación humana del usuario. Se mantuvo la regla de que la ficha vincula una revisión exacta en la definición, sin afirmar autenticidad ni sustituir parámetros eléctricos automáticamente.
- M2 primer corte (evidencia histórica previa a las vistas opt-in): `test/esquema.test` + `test/esquema-svg-interaccion.test` 51/51 verdes, typechecks y build QA verdes. `qa/esquema-desconexion-m2.mjs` exit 0: inspección por ID/bornes en esquema incluso sobre ejemplo de solo lectura, veto antes de confirmar, cancelación sin mutación, desconexión mediante la acción central, Ctrl+Z/Y, guardado/reapertura y 0 errores JS. SVG exportado sigue vectorial sin hit-target interactivo. Este corte no acreditaba ESQ-01/02/03 completos; la frontera actual se detalla en el checkpoint posterior.
- M1 carcasa/recorte, checkpoint posterior: `a955637` añade carcasa opcional separada de función y bornes, snapshot de instancia, render dimensional, `.tscomp` V3 y paquete de proyecto V4 sin degradación silenciosa; 49/49 pruebas focales de persistencia/portabilidad. `5a98001` añade escala 100–400 % y encuadre desde controles visibles con PNG derivado acotado; bornes u/v quedan fijos, guardar exige aplicar el recorte, y el usuario es informado de que una futura edición parte del derivado. QA `qa/recorte-componentes-m1.mjs` con teclado real: 8/8, 128,8 s, 0 errores JS y Chromium/servidor cerrados. Capturas revisadas: ancla X1 visible y pie alcanzable, pero no sustituyen aceptación humana. UI de selección de carcasa sigue pendiente.
- M2 vistas opt-in, checkpoint posterior: `6f04a10` guarda representaciones por ID y hoja estable, resuelve bobina/polos/contactos sobre un solo aparato, rechaza vistas ambiguas/hostiles y conserva legacy. Pruebas core focales 149/149. `2ce0141` añade selección y arrastre por ID gráfico, undo/redo, borrado solo de vista, referencias cruzadas y pendientes visibles; `qa/esquema-representaciones-m2.mjs` verificó 6/6 hitos, 0 errores JS, con guardado/reapertura y cierre de Chromium/servidor. Typechecks app/core y build QA verdes; combinación rápida de recorte/carcasa/SVG/vistas: 25/25, 0 omitidas. No hay todavía creación/conexión desde la UI del esquema ni campaña completa sobre este HEAD.

## Evidencia del checkpoint funcional de 2026-09-23 noche

- M1 CMP-04: `9289fb9` expone en el asistente la carcasa legacy/riel/caja, acabado y retirada explícita, sin tocar perfil/bornes. QA `qa/carcasa-componentes-m1.mjs` 15/15 en 88,4 s, 0 JS, con selección y aspecto revisado en capturas. El montaje de carcasa no declara aptitud física cuando faltan datos: NO_EVALUABLE.
- M1 §9.4: `test/aceptacion-integral-m1.test.ts` prueba contactor propio en dos documentos, revisión r2 que cambia solo PNG, instancia B fijada a r1, adopción solo A, 28 cables idénticos, simulación antes/después, ficha exacta y asset en paquete importado en repositorio limpio; 1/1 focal y 61/61 combinado con adopción/core/repositorio. Tras pasar el preview de `<img>` a `<canvas>`, se corrigieron dos sondas obsoletas sin quitar comprobaciones: asistente 19/19 y adopción 24/24, 0 JS. `qa/componentes-personalizados.mjs` largo se repitió sobre el candidato integrado: `TODO OK`, 0 JS, con 11 conductores creados por clic, polo/NA/NC en reposo y marcha, recarga, paquete en navegador limpio, activo PNG y comparación con contactor nativo. El nativo solo declara NA; el NC propio se acredita por su perfil explícito, no inventando un borne nativo. Aceptación visual humana sigue pendiente.
- M2 ESQ-01/02: activación legacy todo-o-nada y desdoblamiento funcional desde UI (`b552b95`, `4072c0d`). La activación conserva metadatos de hojas existentes y muestra en preview diferencias título/columnas; rechaza casillas que no caben. `resolverComportamiento` adapta KM legacy sin persistir un perfil falso. 23/23 tests focales; QA de navegador 5/5, 0 JS, con dos hojas, cancelación, undo/redo, navegación sin perder formulario y reapertura; dispositivo y conductores únicos. No acredita todavía conexión nueva desde esquema, cruce no unido, renumeración ni M2 completo.
- M4 SIM-03: fallo runtime inyectado `salida-sensor-abierta` solo para sensor con contacto/salida digital; abre la rama de mando por el perfil y no persiste en `Proyecto`. Bomba de nivel: KM cae y motor se detiene al fallar la boya; test de detector PNP nativo/importado y testigo 3D desde `ResultadoSimulacion`. Focal simulación 86/86 y test visual; `qa/bomba-fallo-m4.mjs` 9/9 en 29,64 s, 0 JS, browser/servidor cerrados. Si se retira el fallo manteniendo la boya accionada, el control automático puede volver a pedir marcha: el QA retira primero la demanda. No se afirma que toda avería real de sensor falle abierta ni que exista interlock de reinicio en ese circuito.
- M0 R1: `12a79f3` retira físicamente barras obsoletas y poda por AABB conservando tangencias; `d1f2041` reemplaza Set por sello por segmento con prueba de multicíelda/overflow. 5/5 unitarios y 5/5 SHA-256 exactos del reparto (28, 23, 59, 98 y 98 rutas), sin cambiar puntos/radio/profundidad/orden. Comparación pareada aislada del último corte Node R1+drag: 6865→4518 ms medianas (−34,2 %, tres pares, carga variable). Medición independiente final: 4978 ms para R1+drag; no es el p95 de navegador. R1 sigue NO ACEPTADO hasta repetir gesto real y medir GPU física.
- Campaña integrada después de estos cortes: typecheck app/core, 1529/1529 pruebas (`node --test` TAP, 25,57 s, 0 fallos/omitidas), build QA 409 módulos/6,46 s. La primera campaña fue roja por el test estático `marcas-industriales` obsoleto: exigía `marca(p.id)` aunque el producto había separado `rotuloVisibleBorne` de `borne.id` en M1. La prueba ahora verifica rótulo visible vinculado al ID y fallback al ID; 7/7 focal y repetición integral verde. No se escondió el rojo ni se cambió el producto para satisfacer el regex.
- M4 SIM-10, diagnóstico sin implementación: la física usa longitud manual o distancia recta XY estimada; la ruta 3D del dibujo no alimenta `simularFisicaProyecto`. La sección sí altera R/X al recalcular. El editor tampoco invalida siempre el snapshot de simulación al confirmar edición local de sección/ruta. Tomar directamente la caché de geometría como longitud eléctrica sería costoso y semánticamente falso antes del contrato M6. Pendiente definir longitud declarada/derivada con origen y revisión, y compartirla con física/Ingeniería/documentos; no presentar SIM-10 como verificado.

## Evidencia focal posterior — 2026-09-23

- M0 R1: `qa/benchmark-r1.mjs` pasó un focal n=1 sobre el mismo fixture SHA `f860d627…` tras sustituir la espera implícita de navegación de Playwright por un clic humano y espera explícita de modo Trabajo. Importación 8,33 s, selección 4,70 s, drag host 24,43 s (seis movimientos 16,76 s, `pointerup` host 5,76 s/canvas 4,83 s), reparto 4,76 s, reconciliación 21 ms; 10/98 rutas y 10 mallas cambiadas. Hover/picking y undo/redo exactos verdes; 0 JS, navegador/servidor cerrados. Comparar 11,79→4,76 s de reparto con otro SHA permite ubicar la oportunidad, no afirmar una mejora p95 ni aceptación R1: SwiftShader, n=1 y sin GPU física. El primer intento rojo se agotó esperando navegación tras un clic que sí cambió de modo; no fue una aserción del producto.
- M4 SIM-10: `qa/seccion-en-vivo-m4.mjs` usa biblioteca, copia y controles visibles. Con energía activa, sección 2,5→6 mm² actualizó R de `0,137928` a `0,05747 Ω` sin apagar; Undo y Redo restauraron y reaplicaron tanto documento como física. 8/8, 0 JS, 35,5 s, navegador/servidor cerrados. La primera ejecución real encontró el bug de Undo: diseño 2,5 mm² con simulación todavía en 6 mm²; la corrección añade recálculo del panel al sustituir Proyecto. Esto **no** cierra longitud de ruta→física.
- M3 DOC-02 (identidad, no paquete): `test/documentacion-multivista-m3.test.ts` 1/1. Tres símbolos del mismo KM ocupan dos hojas y una sola fila de índice/BOM; tres conexiones reales conservan extremos y terminales, mientras un cable interhoja produce dos referencias gráficas sin duplicar material. HTML e Ingeniería concuerdan en conteo. DOC-01 sigue abierto por falta de ID/revisión real en HTML/PDF; DOC-02 completo requiere reunir artefactos de una misma revisión.
- Integración en `49c9db4`: typecheck app/core, 1530/1530 pruebas Node en 31,15 s, 0 fail/skip, build QA 409 módulos en 7,81 s. Los QA largos M9 y aceptación humana no se repitieron; la evidencia anterior queda en su SHA.

## Evidencia focal nueva — 2026-09-23, ESQ-02/DOC-01

- `2c9c433`: `estadoRutaFisica: 'pendiente'` pertenece a la conexión eléctrica V2, no a una ruta renderizada. El cargador V1 conserva el legacy, el V2 mantiene el discriminante tras guardar/cargar y los estados desconocidos/contradictorios se diagnostican. Tests de ruta, física, DRC, puerta, ficha e Ingeniería focales 24/24 junto con procedencia; el filtro de Ingeniería impide que un snapshot físico ajeno aporte 999 m a una conexión pendiente.
- `02dfd2c`: importación visible del fixture V2, un cable físico y una conexión pendiente. Inspector/lista/resumen no muestran longitud u ocupación inventadas; cambiar sección prevista no materializa cable. Reapertura conserva estado y sección. `qa/ruta-pendiente-m2.mjs`: 9/9, 0 fallos, 0 errores JS, 9,9 s; servidor y navegador cerrados.
- `b57fe47`: HTML, dossier PDF y esquema PDF incluyen procedencia confirmada de documento y Build ID, distinguen revisión editorial y muestran ejemplo efímero sin identidad falsa. El PDF deriva numeración sobre copia; el primer preview no cambia bytes del proyecto ni revisión. Descarga y preview comparten SHA exacto; edición exige nueva vista y segundo clic. `qa/procedencia-documental-m3.mjs`: 10/10, 0 fallos, 0 errores JS, 83 s; servidor/Chromium cerrados. Tests focales de procedencia y documento pasaron.
- Tras los cortes combinados: TypeScript app/core verdes; `node --test --test-reporter=dot dist/test/*.test.js` terminó exit 0; build QA 410 módulos verde. Este resultado no reemplaza campaña histórica de navegador M9, ni inspección humana/GPU física.
- Límites explícitos: todavía no se puede crear la conexión pendiente desde el esquema; DOC-01 no se extiende aún a SVG/DXF/CSV ni compone un paquete único de revisión. El master spec sigue EN_CURSO y `FUNCTIONAL_COMPLETE` no se declara.

## Evidencia focal posterior — 2026-09-24, ESQ-02 visible y R1

- La frase anterior «todavía no se puede crear» corresponde al checkpoint `b57fe47`; el corte `8ebbcc5` añade ahora el gesto visible. En el esquema opt-in, un pin accesible inicia la conexión, el origen puede cruzar hojas y la confirmación crea exactamente un `Conductor` pendiente en el Proyecto común. Cancelar, duplicado y solo lectura no mutan; desconectar, Undo/Redo y recargar preservan la identidad. `qa/esquema-conexion-pendiente-m2.mjs` recorrió estas fronteras en navegador: **19/19**, 0 errores JS, 90,6 s. El test focal de propuesta rechaza anclas ambiguas sin elegirlas por orden de array. No se acreditan aún ESQ-03, renumeración ni M2 completo.
- `4bb190c` prefiltra sólidos por la envolvente XY ampliada de la ruta antes de la prueba exacta de suelo/colisión; no cambia orden ni comparación final. `qa/rejilla-equivalencia-r1.mjs` mantuvo **5/5** hashes exactos. Las parejas Node del mismo proceso, 6153→5010 ms y 6241→4805 ms; R1-drag 5280→4410 ms, son exploratorias y no reemplazan el p95 de navegador ni la medición en GPU física. **R1 NO ACEPTADO.**
- Batería integrada sobre estos cortes: app/core TypeScript verde, **1551/1551** tests Node en **28,8 s**, 0 fallos/omitidos, build QA **410 módulos** en aproximadamente **6,8 s**. Estas pruebas no equivalen al gate histórico M9. DOC-01 sigue parcial fuera de HTML/dossier/esquema PDF; M4 no recibió política de longitud de ruta en este corte.
- Regresiones de navegador posteriores al corte M0/M2: `qa/cables-fusion.mjs` **verde** en 189,5 s, 0 fusiones 3D, 59/59 puntos frontales, 186/186 clics correctos; `qa/activar-representaciones-m2.mjs` **verde** en 41,3 s después de acotar sus selectores a `.simbolo`, sin relajar condiciones; `qa/esquema-desconexion-m2.mjs` **verde** en 111,1 s; `qa/fixture-puerta.mjs` **verde** en 19,6 s con cierre/apertura y 0 fantasmas. Las cuatro reportaron 0 errores JS. `git ls-files --others --exclude-standard` no mostró artefactos. La inspección de líneas de comando de procesos Windows se denegó por permisos; los procesos QA propios terminaron con exit 0 y el harness cerró servidor/navegador.

## Evidencia focal posterior — 2026-09-24, procedencia extendida y cruce no unido

- El estado anterior de DOC-01 limitado a HTML/PDF pertenece al checkpoint previo. El código validado quedó en `e0cbb3a`, `557150d`, `a717a7a` y `d65a1e3`: la procedencia confirmada/efímera se propaga a esquema SVG/DXF, placa DXF, rótulos PDF, Ingeniería V7 JSON/HTML/cuatro CSV y Diseño V9 exportado, sin reemplazar sus snapshots o límites técnicos. La UI confirma la revisión antes de exportar; un ejemplo no recibe identidad durable falsa. `qa/procedencia-documental-m3.mjs` pasó **16/16** en **89,207 s** en la repetición final, 0 errores JS; `qa/esquema-dossier.mjs` recorrió la edición real de datos del proyecto, preview obsoleto, guardado y PDF con **28/28** en **134,8 s** antes del ajuste gráfico en T, 0 errores JS. `qa/datos-proyecto.mjs` quedó verde incluyendo el campo IP vacío y el rechazo visible de un IP mal formado. Estos resultados no acreditan por sí solos inspección humana de impresión ni el paquete DOC-02.
- Cruces ESQ-03: `crucesSinUnion` deriva intersecciones puntuales interiores y en T; `tramosVisiblesDeHilo` recorta el conductor que debe separarse y el nudo solo aparece cuando varios hilos comparten un borne real. SVG, PDF y DXF dibujan el hueco coherente; el área seleccionable SVG coincide con la tinta visible. La regresión incluye conductor independiente sobre la coordenada de nudo real ajeno, incluso extremo colineal: se recorta para no sugerir unión. Pruebas focales de cruces **20/20**; `qa/esquema-desconexion-m2.mjs` pasó con exit 0 después del ajuste en T, con guardado/reapertura y sin errores JavaScript. La inspección visual automatizada de SVG/PDF verificó geometría y legibilidad antes de la T; no sustituye aceptación humana. Solapes colineales de longitud positiva, nudo DXF relleno, renumeración y flujos multihoja completos siguen pendientes.
- App/core TypeScript verdes; batería Node integrada posterior a T **1584/1584** en **30,949 s**, 0 fallos/omitidos; build QA **411 módulos** en **6,45 s**. `qa/ingenieria-documentacion.mjs` **12/12**, 0 errores JavaScript, **35,1 s**; su arnés ignora solo el 404 del favicon y no silencia otros errores. No se atribuye esta evidencia a CI ni al gate histórico M9. M0/R1, M1, M2 y M3 siguen EN_CURSO; `FUNCTIONAL_COMPLETE` NO declarado. DOC-02 aún necesita una política canónica de BOM: `src/motores/documentacion.ts` agrupa por fabricante/referencia/descripcion y `src/ingenieria/documentacion.ts` también distingue tipo/perfil/modelo físico; no asumir equivalencia de cantidades sin reconciliarla.

## Incremento DOC-02 de 2026-09-24 — evidencia y límites

- BOM canónica `c5b5fb7`: 7 regresiones nuevas de identidad comercial/técnica/propia, variantes nominales, orden y ausencia de referencia; los consumidores históricos de dossier e Ingeniería conservaron sus contratos públicos. La batería combinada antes del ensamblador pasó **1603/1603**; posterior al ensamblador pasó **1604/1604**, 0 fallos/omitidas.
- ZIP/manifiesto `8f72137`: 6 tests de rutas seguras, duplicados, CRC, reproducibilidad y SHA-256; lector Python `zipfile` verificó lectura y CRC de un ZIP real. Lista IO `2ed20a3`: 6 pruebas de PLC nativo/propio, bornes conectados/desconectados, orden, perfiles legacy/hostiles y no duplicación de vistas. Ensamblador: 1 prueba focal con texto hostil, conexión interhoja, aparato multivista y DRC no aprobado.
- App/core TypeScript verdes; build QA **417 módulos** en **6,24 s**. Los cortes `ef3683e` (ensamblador/UI) y `89c38bc` (QA) corresponden al código probado. `qa/ingenieria-documentacion.mjs` descargó desde la UI el paquete ZIP de un ejemplo efímero y comprobó firma ZIP, manifiesto y lista IO: **13/13**, **24,8 s**, 0 errores JS, exit 0 y cierre de navegador/servidor. Esta sonda no sustituye una prueba de importación/extracción offline en otra sesión ni la campaña M9/CI.
- Límite DOC-02: los conductores individuales del paquete no se suman como manguera; aún falta un caso explícito de manguera 3×/4× en aceptación. El vínculo entre igual SKU y variantes nominales aparece como filas separadas, sin motivo visible en tablas heredadas. DOC-03…09, aceptación humana y `FUNCTIONAL_COMPLETE` permanecen abiertos.

## Incremento DOC-02 posterior — paquete offline, obsolescencia y explicación de BOM

- `45de890`: los mismos campos nominales que forman la clave de variante alimentan un resumen legible en ambos consumidores BOM. Un SKU 16 A / 20 A conserva dos partidas y los CSV/HTML/PDF explican la separación. Test de orden inverso y JSON roundtrip; página 6/10 del dossier A4 sintético inspeccionada con render Poppler, sin recortes en ambas tablas. El aviso de autoTable de 14 unidades procede de otra tabla preexistente del dossier (`construirDossier`, anterior al BOM), no de la nueva tabla de variantes; tratarlo en DOC-06, no ocultarlo.
- `76190f1`: test del contrato 3×/4× de obra frente a siete conductores de Proyecto. No se agregan metros de manguera al ZIP sin entidad persistente; el alcance de la lista dice explícitamente que son conductores individuales. Importar las tiradas de cubierta globales al ZIP contaminaría identidad A/B. La lista de compra de manguera asociada a un Proyecto está NO MODELADA.
- Build QA 417 módulos/8,29 s; TypeScript app/core; `node --test` 1606/1606, 0 fallos y 0 omitidos, 25,845 s. Navegador del mismo código: Ingeniería/documentación 13/13/34,4 s, offline 17/17/25,9 s, obsolescencia A/B 6/6/60,4 s, esquema/dossier exit 0; 0 errores JS reportados y cada arnés cerró Chromium/servidor. El primer run Ingeniería fue rojo por una regex que exigía la cabecera CSV antigua; el producto ya emitía `Variante declarada`. La aserción se actualizó para exigir esa columna y un valor, no para relajar el contrato.
- DOC-02 tiene ahora evidencia focal de coherencia, portabilidad y stale rejection, no una aprobación humana ni cierre M3. DOC-03 requiere baseline de revisión emitida: las fotografías de recuperación son podables y no equivalen a una entrega.

## Incremento M3 DOC-03…09 — 2026-09-24, evidencia focal (no aceptación del hito)

- DOC-03: `12c602a` archiva fotografías de diseño y hashes de emisiones verificables fuera del ciclo de poda de autosave; `0550b19` compara aparatos, conexiones, secciones, protección, ficha técnica, ruta y criterio sin inferir geometría AUTO; `1650d88` integra descarga, selección local del ZIP, declaración explícita de entrega, consulta tras eliminar el tablero y comparación en UI. La repetición integrada de `qa/revision-documental-m3.mjs` pasó **20/20** en **127,1 s**, sin errores JS, con reapertura/aislamiento A/B y consulta del archivo tras borrar A. No prueba recepción del destinatario ni conserva los bytes del ZIP.
- DOC-04/05: `f9c3a17` prepara marcadores de aparatos y ambos extremos de cada hilo; `acaee28` amplía bornes a terminales de aparatos, con columna `BORNERA`/`APARATO` sin duplicar vistas ni hilos. `7c451b0` y `3ca4591` separan ruta ortogonal 2D, reserva, puntas, redondeo, longitud eléctrica manual y propuesta estimada de corte en la misma revisión. El corte verificado permanece vacío. Focales de marcadores **7/7** y desglose de longitudes **4/4**, con orden de arrays y roundtrip.
- DOC-06/07/09: `43b749d` documenta y prueba escape CSV (focal **27/27**); `1f52d16` corrige paginación del dossier, anchuras de tablas sin avisos autoTable y frases que insinuaban aprobación; `87d7477` divide BOM HTML en dos tablas vinculadas por partida, usa estados legibles y cabeceras repetidas; `1a89791` exige que un borrador con fallo DRC se emita con el error visible. El dossier PDF sintético de 14 páginas se renderizó y se inspeccionaron ficha, continuación, térmico y DRC sin clipping; Poppler informó ausencia local de fuentes `Symbol`/`ArialUnicode` en el visor, no fallo de generación. El HTML se imprimió con Chromium A4 para fixture de puerta (**3 páginas**) y estrella-triángulo (**9 páginas**): 0 caracteres fuera del margen de 14 mm, filas íntegras, cabeceras repetidas y BOM legible. Queda espacio en blanco de paginación en páginas finales; revisión humana del impreso aún pendiente.
- DOC-08 y texto hostil PDF: `c96ddb1` añade CSP defensiva a Ingeniería y pruebas de texto hostil, recursos locales, no ejecución HTML/SVG y rechazo de informe HTML como orden de edición (**2/2**). `88aaca6` comprueba en PDF real acentos, comillas, saltos y marcadores hostiles como operadores de texto, sin acciones ejecutables (**1/1**); la inspección pypdf/Poppler corroboró legibilidad. `6925081` pide permiso visible para logo/imágenes del dossier antes de preparar el ZIP: repetición integrada del QA navegador **8/8** en **23,0 s**, cancelación sin descarga ni archivo y aceptación con PDF que incorpora la imagen; 0 JS. El ZIP no adjunta assets crudos. El PDF WinAnsi no garantiza fidelidad de Unicode arbitrario.
- Validación integrada sobre el código de `d086297`: typechecks app/core y Node **1640/1640** en **40,18 s** totales, 0 fallos, 0 skipped; build QA **421 módulos** en **7,46 s**. Dos aserciones DOC-02 antiguas esperaban la estructura/frase anterior del HTML/CSV; `d086297` las acotó a la tabla de conexiones y a la afirmación explícita de no sumar manguera, conservando el contrato. `pnpm test` del entorno intentó reinstalar dependencias desde npm con red restringida y movió enlaces a `node_modules/.ignored`; se detuvo, se restauraron solo esos paquetes locales y se ejecutó el equivalente exacto del script con `tsc` app/core y Node. El árbol Git versionado no fue afectado.
- Navegador sobre esa build: paquete offline **17/17** en **10,3 s** (0 HTTP/JS), Ingeniería **13/13** en **37,9 s**, ZIP obsoleto A/B **6/6** en **58,1 s**, procedencia **16/16** en **90,4 s**, todos con 0 errores JS. El dossier editable pasó su recorrido completo tras indicar `PYTHON` del runtime local: preview, formato, imágenes 2D/3D, descarga, texto extraído del PDF y reapertura, sin JS. El primer intento de esa suite llegó a la extracción y falló por `spawnSync python EPERM` del alias del entorno, no por una aserción; no se ocultó. Tras los QA no quedaron procesos Chromium/Node del runtime de pruebas. La campaña histórica M9 y revisión humana del impreso no se ejecutaron/aceptaron en este corte. M3 sigue EN_CURSO; `FUNCTIONAL_COMPLETE` no se declara.

## Selección por impacto

- M1: modelo/persistencia/componentes/assets y QA multiproyecto/componentes, más entrega offline al tocar portabilidad.
- M2: esquema, IDs, bornes, conectividad, simulación y QA esquema/dossier.
- M3: documentos, Ingeniería, impresión/PDF, entrega y texto hostil.
- M4: motores de simulación, física, PLC e Ingeniería; QA industrial/automatización/física/equipos.
- M5–M6: geometría, picking, edición, canaletas, puerta, persistencia, longitudes, Ingeniería/V9 y QA visual/fusión.
- M7: mundo/planta, fixtures CAD sintéticos, seguridad de imports y prueba privada local separada.
- M8–M9: recorridos integrados, accesibilidad/teclado, offline, CI, artifacts y revisión visual/humana pendiente.

Si un cambio cruza identidad, conectividad, anclajes, longitudes o el resolver técnico, ampliar la prueba en esa frontera; no limitarse a la suite del archivo editado.

## ESQ-04 — borrado de vista frente a aparato eléctrico

- `test/eliminar-dispositivos-m2.test.ts`: 7/7 focales para plan sin mutación, dependencias multivista/multihoja, datos técnicos y ajenos, ausencia legacy, proyecto cambiado durante confirmación, solo lectura, selección múltiple/orden inverso y reapertura.
- `qa/esquema-eliminacion-m2.mjs`: 12/12 por interacción visible y almacenamiento real, 28,5 s, 0 fallos y 0 errores JS. Comprueba «Borrar vista» sin perder aparato, «Eliminar aparato» con preview de tres conexiones/dos hojas/datos, cancelar sin historial, eliminación de dependencias, Undo/Redo y recarga sin huérfanos.
- TypeScript app/core verde; Node integrado 1647/1647, 0 fallos/omitidos, 94,6 s bajo concurrencia con navegador. Build QA 422 módulos/8,03 s. QA M2 previos afectados: desconexión exit 0 (6 hitos, ~103 s) y activación de vistas exit 0 (5 hitos, ~40 s), ambos 0 errores JS y cierre de Chromium/servidor. No atribuir a este SHA la campaña histórica completa, CI ni aceptación humana de M2.

## ESQ-08 — renumeración explícita y salida documental coherente

- `test/renumeracion-esquema-m2.test.ts` 6/6: preview puro, bobina multivista como ancla, reservas textuales congeladas, orden inverso, colisiones, rechazo de plan alterado/proyecto cambiado/ejemplo, referencias por ID y guardar/cargar. `qa/esquema-renumeracion-m2.mjs` 11/11 en 27,2 s, 0 JS: botón visible, propuesta/cancelación, aplicación, Undo/Redo, reapertura y dos congelados duplicados que no aplican.
- Hallazgo real: PDF y paquete DOC-02 llamaban `revisarTablero(...renumerarAparatos:true)`, por lo que un congelado `-K2` con `numero=99` podía elevar otro aparato a `-K100` en el papel aunque el proyecto mostrase `-K1`. `test/renumeracion-documental-m2.test.ts` 1/1 exige PDF real, SVG ambas hojas, dossier HTML, Ingeniería JSON/BOM CSV, aparatos, marcadores y referencias con el texto persistido; valida antes/después de aplicar sin mutación y conserva el apóstrofo protector CSV DOC-07.
- TypeScript app/core verde; Node integrado **1654/1654**, 0 fallos/omitidos, 26,13 s. Build QA **423 módulos**/7,23 s. Browser tras el cambio: Ingeniería/documentación **13/13**, 36,5 s, y ZIP offline **17/17**, 17,6 s; ambos 0 JS. Primer intento de Ingeniería no lanzó Chromium por ruta predeterminada de Playwright no instalada; repetición con `PW_CHROME` local completó sin aserciones rojas. No se ejecutó campaña M9/CI del candidato, ni se aceptan todavía referencias cruzadas PLC/circuito, cruces colineales o el hito M2 completo.

## ESQ-03/08 — frontera multihoja, solapes y referencias ancladas

- `test/cruces-esquema-solapes.test.ts` y sus consumidores cubren solape colineal positivo parcial/total entre hilos eléctricos independientes. SVG/PDF/DXF conservan la línea y añaden `SOLAPE SIN RESOLVER`, sin mutar topología. El hit-target SVG no afirma que el tramo ambiguo pertenezca a uno solo; el elemento permanece accesible por teclado. No existe aún desvío manual validado ni solución universal segura; ESQ-03 sigue PARCIAL. Una salida automática lateral en solape parcial puede fabricar dos contactos en T y nuevos cruces, por lo que no se afirma que una simple capa/offset resuelva el caso.
- `qa/esquema-aceptacion-m2.mjs` **18/18** en **316 s** aislado, 0 errores JavaScript y cierre de servidor/Chromium. Comprueba dos hojas y un KM único con bobina/polos/aux, navegación, conexión/desconexión por UI, ruta pendiente sin longitud ni malla 3D, simulación que responde al circuito, borrado/restauración de vista y Undo/Redo. En `w24`, selección del solape total usó Enter sobre grupo SVG accesible; el ratón no tiene punto exclusivo en esa región. Otra corrida simultánea con 1671 tests Node duró 401,2 s por contención y no se presenta como rendimiento normal.
- `test/esquema-aparatos-sin-vista-m2.test.ts`, `test/esquema-pendientes-export-m2.test.ts` y `test/reponer-representacion-esquema-m2.test.ts` exigen aviso de vista/conexión pendiente y reposición por ID sin otro aparato. El PDF de QA se renderizó con Poppler y se corrigió separación vertical de avisos/nota IEC; los temporales no quedaron en Git. La reposición exige casilla válida y revalida el mismo documento tras diálogo.
- `test/referencias-esquema-m2.test.ts` **6/6**: PLC nativo con semántica explícita frente a rótulo engañoso, canal/terminal en hojas distintas, trayecto de alimentación identificado, reordenación de arrays, guardar/cargar, anclaje ausente/duplicado y topología ambigua. `qa/referencias-esquema-m2.mjs` **11/11** en **11,0 s**, 0 JS: índice desplegable, navegación por IDs estables y diagnóstico visible; no invoca edición por hook privado. `test/paquete-referencias-esquema-m2.test.ts` **2/2**: CSV de PLC/terminal y circuito/hoja de la misma revisión, orden de arrays estable, diagnóstico en vez de asignación arbitraria. El conjunto documental focal con regresiones previas fue **9/9**. El alcance del circuito es `TRAYECTOS_ALIMENTACION_IDENTIFICADOS`, no retorno exhaustivo.
- La batería Node integrada **1671/1671**, 0 fallos/omitidas y 29,18 s, además de TypeScript y build QA 423 módulos, se ejecutó **antes** de los cambios finales UI/CSV. Después, el core TypeScript y el subconjunto documental focal 9/9 pasaron; falta repetir la integración completa, el ZIP offline y la campaña M9 sobre candidato final. No mezclar evidencias de SHAs distintos.

## ESQ-09 y salida de revisión — checkpoint respaldado en `5f4ed5b`

- `qa/esquema-navegacion-m2.mjs` **8/8**, 0 errores JS, 28,9 s: desde la misma identidad persistente navega entre esquema, tablero, Datos Técnicos e Issue Center; cuando hay varias vistas exige elección y no adivina por designación. Corrige además una ficha técnica cuya revisión exacta faltaba: muestra ID/hash NO DISPONIBLE y no sustituye silenciosamente por el primer aparato compatible.
- El pie de SVG/PDF declara simbología genérica no certificada; el texto anterior sugería IEC 60617/61439 sin acreditar biblioteca/licencia ni conjunto verificado. El cambio de texto pasó 7/7 pruebas focales. ESQ-06 sigue PARCIAL: no equivale a una biblioteca profesional certificada.
- El ZIP de revisión ampliado con referencias PLC↔terminal, circuitos↔hojas y diagnósticos se abrió desde la UI, verificó entradas CRC/SHA-256 y abrió índice/SVG/dossier/Ingeniería desde `file://` sin solicitudes HTTP(S): `qa/paquete-documental-offline.mjs` **17/17**, 0 JS, 20,0 s tras este corte. Esto no acredita inspección humana del impreso ni campaña M9.

## ESQ-10 — resultado eléctrico en el plano, sin segundo estado

- `769333c` integra un proyector puro sobre `ResultadoSimulacion`: distingue diseño, simulación sin snapshot, resultado inestable y resultado válido; solo este último puede colorear hilos/representaciones. Las referencias interhoja usan el mismo ID de conductor y no crean otro circuito. 6/6 pruebas focales, 0 fallos.
- `qa/esquema-estado-m2.mjs` **21/21**, 0 errores JS, **115,9 s** con Chrome: START/STOP desde controles visibles, cotejo de hilos y aparatos con el resultado vigente, descarga SVG libre de atributos efímeros y Proyecto inmutable. Reemplazar un arranque directo en marcha por el ejemplo de bomba con IDs `km1`/`m1` repetidos reinicia la maniobra y muestra solo el nuevo resultado.
- App/core TypeScript verdes y build QA **425 módulos** en **6,88 s** antes de la regresión final. La captura temporal del plano energizado mostró capa ámbar/encabezado legibles; también mostró distribución legacy concentrada a la izquierda y solapes advertidos. No sustituye aprobación humana. `ResultadoSimulacion` no porta ID/revisión, de modo que la frescura depende del reinicio/recalculo del panel y de leer el snapshot vigente en cada rAF; falta endurecer el contrato de identidad si otros consumidores desacoplados lo utilizan.

## ESQ-07 — folios M2 explícitos, checkpoint de rama respaldado en `c505d2a`

- `cbc28be` introduce un plan puro de crear/editar/mover/eliminar hojas por ID, con previsualización, bloqueo de hoja ocupada y revalidación al aplicar; `3f615ff` distingue columnas explícitas de heredadas y permite restablecer la herencia sin dejar vistas fuera. `35d2bb9` evita pérdida silenciosa de vistas al cargar un documento M2 con IDs/números de folio inválidos o duplicados; los datos editoriales reparables generan diagnóstico y revisión. Las pruebas focales del motor alcanzaron 29/29 y las del cargador 115/115, en sus respectivas fronteras, sin reemplazar la batería integrada final.
- `qa/esquema-folios-m2.mjs` pasó **22/22**, 0 errores JavaScript, **174,6 s** con Chrome tras `c505d2a`: ocultación en legacy, alta/cancelación sin mutación, selección por ID y teclado con foco conservado, reordenación, edición/clase/columnas, un Undo/Redo, rechazo de reducción fuera de rejilla y borrado de hoja poblada, guardado/reapertura, herencia restaurada y borrado confirmado de hoja vacía. El arnés cerró página, Chromium y servidor en `finally`; la inspección global de procesos del sistema no pudo completarse por acceso denegado.
- TypeScript de app y build QA **426 módulos** en **6,80 s** verdes sobre la interfaz de ese checkpoint. La captura temporal a 1600×920 mostró el registro y controles legibles; el panel ocupa hasta 35vh y el ID largo no bloquea, pero no acredita revisión humana. En ese corte aún faltaba propagar clase/título a los entregables (abordado abajo); tamaño de página por folio y campaña M9 siguen pendientes. **ESQ-07 y M2 no están aceptados**.

## ESQ-07 — metadatos del folio en los entregables

- `c7008c6` lleva clase opcional solo del folio M2 al montaje; el cajetín SVG/PDF muestra proyecto, título, clase editorial y número sin inferir clase legacy. `cc0dddf` rotula la hoja DXF R12 e indexa por número/ID/título/clase los SVG del paquete. Una clase editorial no certifica función eléctrica ni cumplimiento normativo.
- Pruebas focales de cajetín y paquete **5/5**: persistencia M2, legacy sin clase inventada, escape SVG de título hostil, DXF sin inyección de grupos, índice HTML con escape y orden independiente de arrays. App/core TypeScript y build QA **427 módulos** verdes sobre el árbol combinado de este corte. PDF A3 y SVG fueron renderizados e inspeccionados: las dos líneas del cajetín y la procedencia permanecen legibles. La fuente jsPDF WinAnsi no garantiza glifos Unicode arbitrarios.
- La primera pasada de `qa/paquete-documental-offline.mjs` detectó dos enlaces al mismo SVG, contradiciendo el contrato de una entrada por archivo; **no se silenció**. `fe92b51` conserva el enlace con título en la tabla de folios y omite ese duplicado de la lista genérica. Repetición de ese corte: **17/17**, 0 errores JavaScript, 0 peticiones HTTP(S), **10,1 s**, CRC/SHA-256 y `file://` verdes. Entonces ESQ-07 continuaba parcial por tamaño de página por folio; su incremento posterior se detalla debajo. La campaña M9 y revisión humana del impreso seguían pendientes.

## ESQ-05 — traslado de una vista M2 sin alterar el circuito

- `test/mover-representacion-esquema.test.ts` comprueba el plan puro en cuatro casos: posición válida, no-op, casilla ocupada/límites, IDs ambiguos y plan envejecido —incluido un cambio de orden del array durante la confirmación—. La aplicación reemplaza solo la representación identificada; preserva las referencias de aparatos/conductores y no crea un elemento eléctrico nuevo. El guardado/reapertura del traslado se comprueba en el QA de navegador, no en este test unitario.
- `qa/esquema-mover-vistas-m2.mjs` recorrió la UI real con Chrome: **18/18**, **90,4 s**, 0 errores JavaScript y exit 0 sobre el corte `2e8d496`. La prueba observó el mismo nodo SVG durante `pointermove`, grafo inalterado antes de `pointerup`, una sola captura Undo al soltar en casilla libre, rechazos sin Undo para ocupada/fuera de rejilla, clic solo selector, Escape y `pointercancel`; después ejercitó Undo/Redo, traslado al folio 2 y persistencia tras recarga. Cerró página/Chromium/servidor en 376/143/0 ms respectivamente; no mide procesos ajenos al arnés.
- App TypeScript y build QA **427 módulos** quedaron verdes tras integrar el gesto. La primera batería Node **1705/1705**, 0 fallos/omitidos, **18,72 s**, fue previa al cambio final de UI; posteriormente se repitió la batería completa sobre el árbol con UI y ajuste QA: **1705/1705**, 0 fallos/omitidos, **18,53 s**, exit 0. La campaña M9 requiere todavía atribución al candidato final; el 18/18 de ESQ-05 es evidencia focal, no aceptación del hito. En este corte aún faltaban alineación/grupo/copia/giro y formato físico por folio (abordado en el incremento siguiente).
- Las suites M2 afectadas por la adición de bornes SVG seleccionables se repitieron en el checkpoint posterior a `2e8d496`: `qa/esquema-representaciones-m2.mjs` exit 0, **6 hitos** impresos, 0 JS, **19,3 s**; `qa/esquema-folios-m2.mjs` **22/22**, 0 JS, **139,4 s**; `qa/esquema-estado-m2.mjs` **21/21**, 0 JS, **92,0 s**. La primera falló inicialmente porque un selector `[data-representacion]` contaba tanto `.simbolo` como sus bornes hijos; `0eea749` lo acotó a `.simbolo[data-representacion]` sin cambiar contrato de producto ni cantidad de hitos. Estos resultados no acreditan por sí solos la campaña M9 final ni se atribuyen al SHA previo `2e8d496`.

## ESQ-07 — formato A3/A2 por folio, frontera focal posterior a `b88c6db`

- `test/formato-papel-hoja-m2.test.ts` cubre A2 persistente frente a A3 heredado/explícito, creación y restablecimiento de herencia, rechazo de formato M2 inválido, diagnóstico legacy, coordenadas de símbolos/enlaces en hojas mixtas y orden invertido de arrays. `test/esquema-formato-exportaciones-m2.test.ts` exige `viewBox` SVG y `/MediaBox` por página PDF para A2 594×420 mm y A3 420×297 mm; la prueba de índice del paquete exige la dimensión de cada hoja. La rejilla de arrastre A2 se observa en el QA navegador. El campo opt-in no migra proyectos legacy a otro papel ni altera el circuito.
- La inspección visual del primer pase **11/11** detectó un encuadre A2 demasiado pequeño tras cerrar Folios; `096354b` recalculó el zoom sin modificar el papel ni el circuito. `qa/esquema-formato-folios-m2.mjs` se amplió con aserción de área visible y pasó **12/12**, **86,3 s**, 0 errores JavaScript sobre ese ajuste: A3 implícito previo, selector A2, preview/cancelación sin mutación, confirmación con un Undo, encuadre A2 sin recorte, rejilla de arrastre A2, SVG entregable, segundo folio A3 y guardar/reabrir sin alterar dispositivos/conductores. Las otras regresiones navegador se ejecutaron sobre el corte anterior al ajuste de zoom: mover vistas **18/18**, **89,7 s**; representaciones **6 hitos**, **21,9 s**; gestión de folios **22/22**, **136,0 s**; paquete offline **17/17**, **5,2 s**, 0 JS y 0 solicitudes HTTP(S). Son ejecuciones focales, no evidencia exacta del candidato integrado ni revisión humana de impresión.
- Sobre el checkpoint con `096354b`, app TypeScript verde, batería Node integrada **1711/1711** en **18,72 s**, 0 fallos/omitidos, exit 0, y build QA **427 módulos** en **5,85 s**. Se preservaron las etiquetas editoriales/procedencia y la identidad de cada folio en el paquete. Otros formatos de papel no se ofrecen ni se presentan como soportados; la campaña M9 final y aceptación M2 siguen pendientes.

## ESQ-05 — selección múltiple y alineación, checkpoint focal de 2026-09-25

- `564f890`: plan puro por IDs para alinear 2–64 representaciones del **mismo folio**, con **9/9** tests focales de propuesta sin mutación, aplicación/revalidación y casos inválidos. `ce22858`: selección visible por Shift+clic/Shift+Enter y acción de alineación. Cancelar, no-op y rechazo no escriben Proyecto ni historial; aplicar crea exactamente **un Undo**. Al navegar de folio se limpia la selección. El QA comprueba guardar/reabrir y que aparatos, conductores y conexiones del grafo eléctrico permanecen intactos.
- `qa/esquema-alinear-vistas-m2.mjs` pasó **14/14** en **102,9 s**, 0 errores JavaScript. Regresiones afectadas: `qa/esquema-mover-vistas-m2.mjs` **18/18** en **88,4 s** y `qa/esquema-folios-m2.mjs` **22/22** en **136,0 s**, ambas 0 errores JavaScript. `qa/esquema-representaciones-m2.mjs` completó sus **6 hitos**, exit 0 y 0 JS sobre el mismo árbol.
- App/core TypeScript verdes; build QA **428 módulos** en **6,11 s**; Node integrado **1720/1720**, 0 fallos, 0 skipped, **21,1 s**. La comprobación de ocupación cubre cajas de símbolos, **no** etiquetas ni hilos: no demuestra ausencia de todo solape documental. Localizar/pan, copia y giro de ESQ-05 siguen abiertos. Esto es evidencia focal, **no** aceptación M2, campaña M9, medición GPU física ni aprobación humana de la salida impresa; no se declara 1.0.

## ESQ-05 — localización y encuadre de vista, checkpoint focal de 2026-09-25

- El primer recorrido ampliado de `qa/referencias-esquema-m2.mjs` tenía **14 comprobaciones** y detectó **dos fallos funcionales**: navegar a una vista dejaba la representación fuera del viewport y no se podía alcanzar el borde izquierdo de la hoja. No se rebajaron esas aserciones. Después de `da49d63`, el ajuste UI/CSS de `9309541` centra la vista seleccionada por ID con zoom alto, permite llegar a los dos bordes y deja foco de teclado en la vista exacta al pulsar Enter; un enlace sin ID gráfico reinicia el scroll. `Proyecto` y el contador Undo permanecen intactos.
- Repetición focal: referencias **16/16**, **14,4 s**, 0 errores JS. Regresiones sobre el corte: mover **18/18**, **81,0 s**; formato A2/A3 **14/14**, **82,8 s**; navegación de issues **8/8**, **22,1 s**; alineación **14/14**, **97,7 s**, exit 0. Todas registraron 0 errores JavaScript. El `finally` de alineación tardó decenas de segundos, sin procesos QA huérfanos detectados; esa latencia de arnés no acredita fluidez de producto.
- App TypeScript verde; build QA **428 módulos** en **6,01 s**; Node integrado **1720/1720**, 0 fallos y 0 skipped en **18,214 s**. Localizar/encuadrar no implementa pan continuo, copia o giro; esas fronteras ESQ-05 siguen pendientes. No hubo campaña M9, aceptación M2, medición GPU física ni aprobación visual humana. `main` continúa en V9, sin publicación 1.0.

## ESQ-05 — pan de lienzo sin editar el circuito, checkpoint focal de 2026-09-25

- `27e77e0` implementa pan con foco sobre el lienzo mediante Space+primario o botón medio. El gesto se captura antes de bornes/símbolos y solo varía `scrollLeft`/`scrollTop`; no muta `Proyecto` ni añade Undo. `pointerType: touch` queda fuera de la captura, sin prueba de gesto táctil real; las flechas siguen disponibles con foco del lienzo. Se limpian captura, tecla y listeners en `pointerup`, `pointercancel`, `lostpointercapture`, `blur`, Escape y cierre; se suprime el clic residual para no conectar ni seleccionar accidentalmente.
- Primera pasada de `qa/esquema-pan-m2.mjs`: **roja por ausencia de foco de lienzo**, no se reinterpretó como éxito. Repetición final **15/15**, **38,4 s**, 0 errores JavaScript: Space perdido al cambiar foco, cancelación sintética, Escape, cierre/reapertura, clic posterior de borne, Space de control, flechas y constancia de documento/historial. Regresiones después de integrar pan: conexión pendiente **19/19** (**53,0 s**), mover vistas **18/18** (**86,6 s**), referencias **16/16** (**16,0 s**), alineación **14/14** (**97,8 s**, exit 0 tras cierre del arnés más lento de lo normal, sin procesos QA huérfanos) y formato A2/A3 **14/14** (**80,5 s**); todas 0 JS.
- TypeScript app/core verde; build QA **428 módulos** en **6,05 s**; Node **1720/1720**, 0 fallos/omitidos, **18,515 s**. La evidencia es focal: copia/pega, giro, ESQ-06 y solape colineal siguen pendientes; no hubo campaña M9, aceptación M2, medición GPU física o inspección humana de impresión. `main` sigue siendo V9 y no se declara `FUNCTIONAL_COMPLETE` ni 1.0.

## ESQ-06 — evidencia disponible y procedencia NO VERIFICADA

- El requisito en `docs/avance/TABLEROSTUDIO_MASTER_SPEC.md` §10.3 pide origen/licencia declarados para bibliotecas, admite símbolos genéricos y personalizados sin certificación y exige reglas consistentes de terminales, textos, cruces y referencias. El código observado en `src/motores/esquema.ts` construye `simboloDe`/`bloqueFuncional` mediante primitivas y bornes del modelo; `app/esquema-svg.ts` y `app/esquema-pdf.ts` imprimen `NOTA_SIMBOLOGIA_ESQUEMA`, que avisa que la simbología genérica no certifica normas ni fabricación. El test existente `test/esquema-simbologia-m2.test.ts` comprueba el aviso y la ausencia de dos antiguas frases que insinuaban IEC; la evidencia histórica consignó **7/7** focales para ese cambio, **no repetidos aquí**.
- `package.json` declara la licencia del repositorio como `GPL-2.0-or-later`; no existe en el contrato actual de los símbolos un registro por entrada que distinga autor/origen, licencia declarada, documento comprobado y derecho de redistribución verificado. Por tanto, la licencia del repositorio **no se extrapola** a una biblioteca ajena. El README menciona un conversor futuro de símbolos de QElectroTech y una «licencia libre», pero esa frase de hoja de ruta no demuestra que se haya importado el recurso, ni su licencia concreta, ni el permiso para publicarlo. La palabra «IEC» en un comentario de `simboloDe` tampoco verifica conformidad técnica.
- **No se ejecutaron tests ni se hizo revisión legal/normativa en este corte documental.** Está probado únicamente el texto de no certificación en los entregables por la regresión histórica; procedencia y derechos por símbolo quedan **NO VERIFICADOS**. ESQ-06 sigue PARCIAL, sin aceptación M2, certificación IEC/NCh ni afirmación de biblioteca profesional completa.

## ESQ-06 — inventario local y procedencia verificable en producto, 2026-09-25

- El checkpoint posterior a la nota documental incorporó `PlantillaSimboloEsquema` y `SimboloEsq.plantilla` como proyección **no persistente** de la geometría que se dibuja. La resolución de plantilla decide el mismo `switch` de trazos; no se mantiene una tabla paralela de etiquetas comerciales. Los IDs `esq.*.v1` cubren los 22 `TipoDispositivo`, bloques funcionales y vistas parciales. El fallback sigue siendo una caja genérica: no transforma una válvula o resistencia en simbología específica inexistente. Los trazos y bornes eléctricos no se alteraron.
- `test/esquema-plantillas-m2.test.ts` comprueba la relación 22/22, casos de bobina/contacto/bloque, montaje legacy/M2, persistencia/reapertura y vistas inválidas sin atribución. `test/esquema-procedencia-export-m2.test.ts` comprueba los atributos y título SVG escapados, inventario PDF y ausencia de afirmación de certificación. La batería Node integrada pasó **1726/1726**, 0 fallos/omitidos, **18,39 s**; TypeScript app/core y build QA **428 módulos** en **6,04 s** también pasaron. El QA nuevo de navegador pasó **10/10** en **2,8 s**, 0 JS, y la regresión M2 de representaciones completó **6 hitos** en **19,2 s**, exit 0 y 0 JS. El primer intento del QA nuevo falló por asumir que el panel se cerraba al cambiar de selección; el panel conserva deliberadamente su estado abierto y se corrigió la prueba sin omitir comprobaciones.
- El PDF/SVG mantienen la advertencia visible. La ficha declara origen técnico local y la licencia **del proyecto** indicada en `package.json`; `licenciaVerificada:false` no acredita cadena de derechos por símbolo. `conformidadNormativa:NO_VERIFICADA` no equivale a homologación IEC/NCh. No se importó una biblioteca ajena ni se evaluó licencia de QElectroTech. No hay todavía autoría personalizada de símbolos esquemáticos, validación normativa, campaña M9 completa o revisión humana de papel. **ESQ-06 PARCIAL y M2 NO ACEPTADO.**

## ESQ-05 — copia segura y giro 180°, evidencia focal de 2026-09-25

- Los checks de `test/copiar-representacion-esquema.test.ts` prueban que una vista conectable del mismo aparato **no** se puede duplicar sin crear ambigüedad en el codec. La copia válida crea identidad eléctrica y gráfica nuevas, mantiene el grafo existente, respeta folio/rejilla/cajas y conserva giro si el origen lo tenía. El punto físico se preflighta antes de `capturar()`. La puerta y vistas desdobladas requieren contratos específicos posteriores.
- `test/girar-representacion-esquema.test.ts` verifica inversión de pines alrededor del centro, caja estable, orientación legacy ausente, guardar/cargar y rechazo de `90°` no soportado. El plan puro envejece si cambian vistas, aparatos u hojas y no se puede falsificar ni reutilizar. Textos se reubican sin invertir el glifo; SVG/PDF/DXF consumen el mismo montaje geométrico.
- En el árbol combinado: app/core TypeScript verde, Node **1734/1734** en **19,281 s**, 0 fallos/omitidos, build QA **430 módulos** en **5,96 s**. La prueba de navegador `qa/esquema-copiar-vista-m2.mjs` **14/14**, 0 errores JS, **23,5 s con cierre de recursos**; el QA de representaciones M2 completó sus **6 hitos** y salió con código 0. La primera ejecución después de cambiar HTML usó `app/dist` anterior y no vio los botones; reconstruir el build resolvió ese error de arnés. Un recorrido intermedio completó 14/14 pero tardó más de lo normal en cerrar Chromium; la última ejecución terminó limpia y cronometró el cierre.
- El push de `e037259` y `acb2bcf` a `roadmap/1.0-functional` fue normal, tras comprobar 0 commits remotos nuevos. Se necesitó acceder al almacén de credenciales del host; la credencial aislada de esta sesión no era válida. Los commits de giro `7ec0885` y `cae2afe` son el siguiente checkpoint de la misma rama. `main` no se tocó.
- No hay prueba de giro 90°, símbolos personalizados, edición completa de vistas desdobladas, impresión humana, GPU física ni campaña M9. La prueba focal no equivale a aceptar M2 ni a declarar 1.0.

## M0/R1 — primera medición repetida con GPU física, 2026-09-25

- Sobre `138491f` limpio, fixture R1 de 30 aparatos/100 conductores con SHA-256 `f860d627ebd6ed926b5bcddcc715ebd862efd71469e0ea67f2523311fa77523f`. Navegador local reportó **ANGLE/NVIDIA GeForce RTX 5070 Ti/Direct3D11**; `R1_GL=hardware`, sin flags SwiftShader. La sonda terminó `completo:true`, 0 errores JS y 0 advertencias, con cierre de sus recursos. El primero de los dos recorridos focales midió selección 157,9 ms y drag 3802,2 ms; una tarea larga llegó a 3467 ms.
- Segundo recorrido del mismo SHA con `R1_MUESTRAS=5`, `R1_APERTURAS=2`: selección **p50 106,6/p95 153,2 ms** (muestras 153,2/107,7/106,6/104,5/105,7); drag de seis pasos **p50 3734/p95 3817,3 ms** (3817,3/3731,9/3786,8/3688,4/3734). Total de la campaña 45,1 s; no es una certificación multi-equipo. La GPU física reduce drásticamente la latencia host frente a SwiftShader, pero sigue existiendo una ruta global síncrona de varios segundos al soltar.
- El contrato de §18.3 busca selección <100 ms y ausencia de bloqueos largos recurrentes en R1. **R1 NO ACEPTADO**: las cinco selecciones quedaron sobre 100 ms y los cinco drags tardaron ~3,7–3,8 s. Se requiere trabajo causal sobre el reparto/rendimiento o mover el cómputo fuera del hilo principal con estado visible y cancelación; no basta con ocultar el coste en un timeout. No se tocó el producto en esta medición.

## M0/R1 — recálculo diferido y frontera offline, 2026-09-25

- El reparto global se ejecuta en un Worker **inline** tras mover un aparato o una unión manual, y tras crear/quitar una unión. Mientras tanto se ocultan las rutas antiguas que ya no corresponden al documento, se anuncia el estado y existe un botón visible de cancelar que revierte el gesto por Undo. La respuesta se adopta solo si coinciden documento y firma; cambios en pines de imagen, terminales, montaje de puerta, entradas, conductores o caja invalidan rutas antiguas. Una respuesta con ID duplicado o geometría no finita se rechaza. El trabajo sigue siendo global y no se afirma haber reducido su coste total.
- Medición repetida final del gesto de aparato sobre el mismo R1/RTX 5070 Ti/D3D11, cinco selecciones y cinco drags, `R1_APERTURAS=2`: selección host **p95 154 ms** frente a listener `pointerup` **p95 46,3 ms**; drag host **p95 378,5 ms** frente a **3817,3 ms** antes, listener `pointerup` **p95 37,4 ms**, render directo **p95 16,3 ms** en 30 cuadros, **0 tareas largas**. Rutas listas **p95 4523,6 ms**. `completo:true`, 100/100 geometrías, 0 advertencias y 0 errores JS. El host incluye Playwright/protocolo; no es tiempo puro de interacción.
- Focal R1 añadió una unión por doble clic y arrastró su tirador como una persona; el gesto manual respondió en **472,6 ms**, modificó el modelo y terminó con 100/100 geometrías; Undo/Redo de aparato y cancelación restituyeron rutas idénticas. El primer intento del arnés intentó arrastrar un tramo sin unión, gesto que el producto reserva a orbitar, y el segundo tuvo un nombre de variable incorrecto al informar; ambas causas se corrigieron en QA sin debilitar el caso. El recorrido final salió `completo:true`, 0 JS.
- En un HTML temporal empaquetado desde build QA, abierto por `file://` sin servidor, el Worker completó el mismo R1 focal: gesto de aparato **403 ms**, rutas **4540 ms**, selección listener **47,4 ms**, 0 tareas largas/JS, cancelación y geometrías correctas. El empaquetador ahora rechaza un segundo bundle JS y autoriza solo `worker-src blob:` para ejecutar el Worker ya incluido en el script verificado por hash. El entregable de producción `qa/empaquetado.mjs` pasó su recorrido completo V3–V9, Build ID `C806F0371C`, 0 JS y 0 peticiones HTTP externas; ese artefacto precede el ajuste final de validación de rutas y la extensión del Worker a uniones, que requieren un Build ID posterior.
- `qa/cables.mjs` pasó creación, uniones, arrastre, Undo/borrado y coherencia 63/63 tras diferir esos gestos; `qa/cables-fusion.mjs` pasó antes de la última extensión: 59/59 puntos frontales y 186/186 clics semánticos. Node llegó a **1736/1736** y app/core TypeScript verdes antes de la extensión manual; la batería y el gate final deben repetirse sobre el árbol completo. La sonda antigua `qa/agarre.mjs`, fuera del gate de 13 suites, registró fallos en una expectativa de selección durante `pointerdown` y en barridos angulares 0/0; se interrumpió antes de su barrido masivo. No sirve como evidencia verde ni se atribuye al Worker sin una reproducción actual; queda por modernizar o retirar con justificación.
- **R1 sigue NO ACEPTADO:** el Worker hace el gesto utilizable en esta GPU, pero esperar ~4,5 s por reparto es un coste real; el hardware software-rendered probado aparte continuó lento. Falta evidencia de otra plataforma, creación de cables, inspección humana de fluidez y campaña integrada M9. No se declara `FUNCTIONAL_COMPLETE` ni 1.0.

### Cierre integrado del incremento M0/R1 y vínculo V8

- App/core TypeScript verdes; Node **1736/1736**, 0 fallos/omitidos, **19,05 s**. Build QA **431 módulos** en **6,44 s**. Gate oficial **13/13 suites**, **273 comprobaciones**, 0 fallos/timeouts, **10:04**. `qa/cables-fusion.mjs` conservó 0 ejes 3D coincidentes, **59/59** puntos frontales, **186/186** clics semánticos y 0 errores JS. `qa/fixture-puerta.mjs` conservó 7/7 geometrías, tres aperturas, PE/bonding/campo separados y 0 JS. Son regresiones ejecutadas después de extender el Worker a uniones.
- `qa/datos-tecnicos-importacion.mjs` pasó **55/55** en **76,20 s**, 0 fallos/timeouts/skipped/JS. La primera ejecución de `qa/empaquetado.mjs` encontró un **fallo real de UI**: “Vincular” desde la biblioteca heredaba el ID de un conductor que había quedado seleccionado en Instalación y mostraba “Producto incompatible” en vez del selector de protección. La acción de vínculo nuevo ahora limpia esa entidad previa; abrir un vínculo explícito por ID conserva la verificación de compatibilidad. No se retiró ninguna comprobación; la repetición del HTML final pasó V3–V9 con 0 JS y 0 peticiones externas.
- Build de producción **431 módulos**; `app/empaquetar.mjs` generó `dist-final/TableroStudio.html` y `desktop/app.html` byte a byte iguales, Build ID **`8254F5160F`**. `herramientas/verificar-entrega.mjs` reconstruyó en temporal y comprobó ambos archivos actualizados y bytes idénticos entre entradas LF y CRLF. La primera ejecución de ese verificador en el sandbox no pudo leer la configuración Vite por aislamiento de rutas; la repetición autorizada terminó exit 0, sin cambiar el worktree.
- Focal final R1 en HTML `file://` temporal construido con sonda QA: selección listener **47,4 ms**, drag de aparato listener **37,0 ms**, gesto host **403,9 ms**, rutas **4594,9 ms**, **0 tareas largas**; unión manual movida con gesto host **427,4 ms**, 100/100 geometrías, Undo/Redo/cancelación correctos, 0 JS/advertencias, `completo:true`. El HTML de producción se probó aparte por `qa/empaquetado.mjs`; el focal con sonda no se hace pasar por ese binario exacto. La ruta global de varios segundos, software rendering y revisión humana de fluidez siguen pendientes: **M0/R1 NO ACEPTADO**. Tampoco se ejecutó la campaña funcional M9 completa ni se declara 1.0.

## M0/R1 — proyección de mensaje y navegador Windows, 2026-09-25

- `test/ruteo-asincrono.test.ts`: **4/4** focales; compara la firma y todas las rutas de los cinco tableros principales y el fixture de puerta frente a una copia estructurada del mensaje mínimo. El caso de imagen de 4 MB mantiene pin `u/v`, profundidad de componente y rutas, no modifica la fuente y reduce la serialización a <1 %. No afirma que el solver global sea más rápido.
- TypeScript app/core verde, Node integrado **1738/1738** en **19,66 s**, 0 fallos/omitidos. Build QA **432 módulos** en **6,64 s**. `qa/cables.mjs` terminó exit 0, **63/63** geometrías y 0 errores JS. `qa/benchmark-r1.mjs --focal` con Chromium 153 sobre NVIDIA RTX 5070 Ti/D3D11 terminó `completo:true`, 100/100 geometrías, 0 advertencias/JS, 0 tareas largas; gesto host **396,8 ms**, listener `pointerup` **36 ms**, rutas listas **4569,2 ms**, unión manual host **434,1 ms**. Son mediciones n=1 del fixture sin fotografías; no demuestran una mejora de tiempo respecto al checkpoint previo ni aceptación multiequipo.
- El primer intento del benchmark no abrió navegador: Playwright 1.61.1 buscaba su revisión predeterminada 1228, mientras el host tenía Chromium 1243; configurar `PLAYWRIGHT_BROWSERS_PATH` tampoco funcionó porque el helper reconocía `chrome-win` y no `chrome-win64`. Se añadió ese layout en los dos helpers QA. El recorrido posterior usó explícitamente el Chromium local y terminó limpio. El runtime 1.61.1 y el navegador instalado reportaron revisiones distintas; su compatibilidad se observó en este QA, no se afirma que sean la misma revisión.
- Producción **432 módulos**, Build ID **`1A8AAE69FE`**, ambos HTML versionados byte a byte iguales. `qa/empaquetado.mjs` pasó su recorrido offline V3–V9, 0 errores JS y 0 peticiones externas, sin servidor. El gate de 13 suites del checkpoint previo no se volvió a ejecutar: no mezclar sus 273 comprobaciones con este SHA.

## M2/ESQ-05 — copia de aparato desdoblado, 2026-09-25

- Plan puro y UI crean un único aparato con vistas de bobina/polos/auxiliar en sus folios; los IDs gráficos nuevos nunca duplican un borne y ningún conductor se copia. Destinos fuera de hoja, caja ocupada, IDs incompletos, ejemplo de solo lectura y grupo modificado se rechazan antes de mutar. Unit tests incluyen orden de arrays, origen desde contactos, persistencia y SVG de ambos folios con designación nueva.
- La inspección visual de `qa/_salida/esquema-grupo-mando.png` y `qa/_salida/esquema-grupo-potencia.png` encontró el defecto de designación duplicada `-KM1` en la primera build; tras la corrección, la copia figura `-KM2` en Mando y Potencia. La revisión fue de imágenes del render SVG en pantalla, **no** de impresión física ni certificación de simbología. La tipografía al encuadrar el folio entero aún parece pequeña y necesita evaluación de papel real.
- TypeScript app/core verde; Node integrado **1745/1745**, 0 fallos/omitidos; build QA **433 módulos**. `qa/esquema-copiar-grupo-m2.mjs` **13/13** en **22,7 s** después de comprobar el destino con la designación final y rechazar una vista parcial aislada; Undo/Redo, reapertura y 0 JS. `qa/esquema-copiar-vista-m2.mjs` anterior **14/14** en **28,5 s**, 0 JS; `qa/esquema-representaciones-m2.mjs` seis hitos, 0 JS. La evidencia del gate completo aún pertenece al SHA previo; M2 y 1.0 no se aceptan por este corte.
- `qa/empaquetado.mjs` probó el HTML de producción por `file://` hasta V9, exit 0, 0 errores JS y 0 peticiones HTTP externas. La prueba terminó después de los cambios de producto; la última modificación posterior fue solo una aserción unitaria SVG y documentación. La batería Node se repitió **1744/1744** tras esa aserción.
- Después de esa evidencia se endureció el plan de pegado: el preflight monta la copia con su rótulo final (evita medir cajas con la etiqueta vieja) y rechaza una vista funcional única no completa. App/core, Node **1745/1745**, build QA y QA de grupo **13/13** volvieron a pasar. El HTML de producción Build **`4631B6B6BE`** es idéntico en `desktop`/`dist-final`, reproducible con LF/CRLF y pasó `qa/empaquetado.mjs` desde `file://` por V3–V9, 0 JS y 0 solicitudes HTTP externas. `qa/mejoras.mjs`, diagnóstico no incluido en el gate, falló antes de Ctrl+D por buscar el catálogo con el cajón Añadir cerrado; no se utiliza como evidencia del producto.

## Seguimiento focal — sonda de productividad actualizada, 2026-09-25

- La sonda `qa/mejoras.mjs` conserva sus 24 comprobaciones de búsqueda y Ctrl+D, pero abre el cajón Añadir y vuelve a Seleccionar mediante los botones reales. Espera el documento de IndexedDB, registra errores JS y cierra página/Chromium/servidor también en error. `qa:mejoras` construye la app en modo QA para no depender de una build previa que podría carecer de `window.qa`.
- Tras reconstruir QA: **24/24** en **69,6 s**, 0 errores JS, exit 0; duplicó un fusible colocado cuatro veces en cadena con designaciones distintas, sin solapes ni cables fantasma; Ctrl+Z quitó la última copia. El caso de buscador se ejecutó en el mismo tablero. Esta prueba no pertenece al gate histórico, pero ya es una señal focal válida de los gestos que comprueba. Los fallos previos fueron del arnés y no se reutilizan como evidencia verde.

## ESQ-06 — dibujo personal con procedencia declarada, 2026-09-25

- `test/simbolo-personal-m2.test.ts` cubre separación del dibujo frente al perfil/bornes, revisión congelada, carga, plano SVG/PDF, importación hostil, paquete V5 y `.tscomp` V4 sin downgrade. El PDF añade una página de procedencia por el conjunto de plantillas personales usadas; los planos sin símbolos personales conservan el formato previo. La prueba focal nueva y la regresión de procedencia anterior pasaron **6/6**.
- App/core TypeScript y `tsc` verdes; batería completa Node **1749/1749** en **19,5 s**, 0 fallos/omitidos; build QA **434 módulos** en **6,74 s**. La invocación literal `pnpm test` se bloqueó en la verificación/instalación de dependencias del wrapper local (`ERR_PNPM_IGNORED_BUILDS`), antes del script; los pasos directos equivalentes sí concluyeron. El intento generó dos YAML no versionados que se inspeccionaron y retiraron.
- `qa/simbolo-personal-m2.mjs`: **12/12** en **66,4 s**, 0 errores JS, exit 0 y cierre de Chromium/servidor. Una persona crea un componente funcional con imagen/bornes, activa el dibujo personal, declara autor/licencia, ve la previsualización, guarda, exporta `.tscomp`, lo importa en almacenamiento limpio, coloca una instancia, descarga proyecto V5, activa vistas M2, inspecciona procedencia y exporta SVG/PDF. `qa/esquema-procedencia-m2.mjs` siguió **10/10** en **2,8 s**.
- La inspección de screenshot del asistente vio forma y terminales coherentes; la del plano M2 vio el símbolo personal en la hoja sin alterar la función. El PDF de dos páginas descargado por la UI se renderizó con Poppler (`pdftoppm`, página de anexo) y se inspeccionó: declaración y lista legibles sin recortes ni solapes. A escala de hoja completa, texto/bornes siguen pequeños en pantalla: **no hay prueba de legibilidad impresa**. Tampoco hay autorización verificada para dibujos ajenos, certificación IEC/NCh ni campaña M9 sobre este incremento. ESQ-06 aporta una herramienta utilizable, no una biblioteca certificada; **M2 y 1.0 siguen abiertos**.

### Entrega offline posterior a ESQ-06

- Producción Vite **434 módulos**; HTML único `dist-final/TableroStudio.html` y `desktop/app.html` iguales, Build ID **`8CE0372AE7`**. `qa/empaquetado.mjs` pasó desde `file://` sus recorridos históricos V3–V9, con persistencia, simulación, ingeniería, datos técnicos, diseño asistido y exportación; exit 0, 0 errores JS, 0 solicitudes HTTP externas obligatorias. `herramientas/verificar-entrega.mjs` comprobó ambos archivos actuales y reproducibles con entradas LF/CRLF, exit 0.
- Este smoke del archivo entregable no repite el editor de símbolos personales bajo `file://`; esa frontera la cubre la QA focal con servidor y el formato portable. La campaña integrada M9 y aceptación de legibilidad impresa siguen pendientes.

## M2 — solape colineal y selección humana, 2026-09-25

- El diagnóstico de `cruces-esquema.ts` distingue solape de cruce puntual y de nudo verdadero. En el arranque directo hay un conductor `w24` sin segmento de tinta exclusivamente seleccionable por puntero: la superposición gráfica persiste. La UI muestra el número de solapes y los IDs implicados; la persona puede escoger `w24` con ratón y conservar el inspector, incluso cuando el hit-target sobre tinta sería ambiguo. No se modifica la topología ni se finge un nuevo recorrido. El cálculo cuadrático por hoja se reutiliza en el SVG interactivo, sin repetirlo durante la misma actualización.
- `qa/esquema-aceptacion-m2.mjs` pasó **20/20** en **160,8 s**, 0 errores JavaScript; sus dos comprobaciones nuevas verifican advertencia, selector por ID, selección y topología inalterada. El primer intento falló solo por una expresión QA sensible a mayúsculas (`Sin resolver` en el DOM); se corrigió esa expresión sin relajar los predicados del producto y se repitió completa. TypeScript app/core y Node **1749/1749** pasaron, 0 fallos/omitidos; build QA **434 módulos**.
- HTML de producción Build **`FC9FC704D4`**, mismo contenido en `dist-final/TableroStudio.html` y `desktop/app.html`. `qa/empaquetado.mjs` terminó V3–V9 desde `file://` con exit 0, 0 errores JS y sin solicitudes HTTP externas obligatorias. `herramientas/verificar-entrega.mjs` confirmó frescura y reproducibilidad LF/CRLF. **No se ha eliminado el solape colineal:** no se acepta ESQ-03, M2, M9 ni `FUNCTIONAL_COMPLETE` por esta mitigación.

## FIN-02 — identificación honesta de preversión, 2026-09-25

- `package.json` mantiene un número técnico `1.0.0` heredado, pero mientras M0–M9 no estén aceptados «Acerca de» ya no lo presenta como release: muestra «TableroStudio · preversión funcional en desarrollo» y Build ID. La prueba `qa/empaquetado.mjs` exige ambos textos desde el artefacto real, sin depender del código fuente.
- TypeScript app verde; build de producción **434 módulos**. Entregables idénticos Build **`EEB93B4CAF`**. El smoke offline completo V3–V9 terminó **TODO OK**, exit 0, sin errores JS ni solicitudes HTTP externas obligatorias; comprobó explícitamente la nueva etiqueta. `herramientas/verificar-entrega.mjs` confirmó frescura y reproducibilidad LF/CRLF. El cambio no equivale a freeze ni publicación de 1.0.
- Diagnóstico M2 de la siguiente frontera sobre el arranque directo recién activado: **32 solapes colineales en fuerza, 18 en mando** y **7 penetraciones de caja de símbolo** por líneas rectas (4+3). Los primeros se obtuvieron con `solapesColinealesSinResolver`; las penetraciones se contaron con intersección estricta segmento ortogonal/interior de `SimboloEsq` (margen de 0,1 mm). Este censo no constituye un test de aceptación: el router aún necesita corregirse y verificarse visualmente en los tres entregables.

## M2 — corredores editoriales y borne lateral, 2026-09-25

- El montaje M2 explícito tiende los hilos por ID estable, fuera de interiores de símbolos y con penalización de tinta compartida. Un pin en el costado abandona perpendicularmente su caja; los corredores tienen separación gráfica de 3 mm. Solo un borne **idéntico** admite una salida común de hasta 3 mm; una coincidencia de 60 mm sigue siendo error. El trazado es proyección en papel: los `conductores`, bornes, rutas físicas y potenciales permanecen idénticos. Cuando no hay corredor seguro, el fallback conserva el dibujo previo y añade `ruta-grafica-sin-corredor`; ningún trayecto se inventa como conexión nueva.
- Con el mismo diagnóstico puro sobre los cinco ejemplos principales ya convertidos a M2: arranque directo **0 solapes/0 penetraciones**, bomba-boya **0/0**, estrella-triángulo **0/0**, control-24v **0/0**, UMA-cubierta **2 solapes advertidos/0 penetraciones**. Ninguno quedó sin corredor. En el arranque directo se eliminaron los **32+18** solapes y las **7** penetraciones del censo previo; `w24` dejó de depender de una selección semántica alternativa y recibe un punto de ratón en su trazo. La prueba invierte arrays, guarda/carga, desdobla KM1 y conserva identidad/topología. La UMA **no** está resuelta del todo: el aviso es real y visible, no se rebajó el detector.
- TypeScript app/core verde; Node integrado **1756/1756** en **19,89 s**, 0 fallos/omitidos; focal de router y cruces **11/11**. Build QA **435 módulos**, **6,22 s**. QA humana por ratón `qa/esquema-aceptacion-m2.mjs` **20/20**, **147,9 s**, 0 JS/timeout; incluye desconectar, reconectar desde bornes, lista, arranque por KM y Undo/Redo. Regresiones afectadas sobre la build del primer corredor: mover vistas **18/18**, **83,4 s**; copiar grupo **13/13**, **23,4 s**; representaciones seis hitos; formato A2/A3 **14/14**, **80,4 s**. Los ajustes finales de salida lateral se validaron con Node, build y aceptación M2; **no** atribuirles retrospectivamente esas otras tres QA de navegador.
- Medición Node local tras la salida lateral, diez montajes calientes por ejemplo (p50/p95, ms): arranque directo **25,6/39,6**; bomba **32,7/36,1**; estrella-triángulo **119,9/123,9**; control-24v **48,3/49,5**; UMA **163,7/170,5**. No es un benchmark de interacción en GPU ni garantía para tableros arbitrariamente densos. El gesto de mover vista repinta solo al soltar; su `pointermove` usa preview, por lo que el coste del montaje no se paga en cada muestra de puntero.
- Se renderizaron las dos hojas PDF A3 del arranque directo con el generador vectorial y Poppler a 170 dpi. Se inspeccionó la imagen: no se observaron penetraciones, pero aparatos y rutas quedan agrupados a la izquierda y el rótulo es pequeño al encuadrar toda la hoja. La generación local usó un shim DOM para invocar el mismo `esquemaComoBlob`; no equivale a una descarga completa de UI ni a aprobación física del impreso. Poppler avisó de fuentes `Symbol`/`ArialUnicode` no presentes en ese entorno, aunque produjo las imágenes. No se declara M2 visualmente aceptado.
- Producción **435 módulos**; HTML único Build ID **`70F1E528AE`**, `desktop/app.html` y `dist-final/TableroStudio.html` idénticos. `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9, 0 errores JS y 0 peticiones HTTP externas obligatorias. `herramientas/verificar-entrega.mjs` confirmó bytes actuales y reproducibilidad LF/CRLF. No se ejecutó el gate histórico completo ni M9/CI sobre este incremento; `main` permanece V9 y **no** se declara `FUNCTIONAL_COMPLETE` ni 1.0.

## M5 — soporte DIN numérico, 2026-09-25

- `planMedidasRiel` propone sin mutar X/Y/largo en mm y el mismo delta para cada `rielId`, ordenado por identidad y no por posición de array; rechaza placa, largo útil, duplicados, aparato ausente y choques de huella con holgura editorial de 3 mm. El inspector y el formulario general lo aplican en una sola transacción/Undo. Campos vacíos no se convierten en cero, y el formulario no redondea medidas decimales al repintar. No se declara capacidad de carga, fijación DIN certificada ni espacio de servicio del fabricante.
- `test/medidas-riel.test.ts` **5/5** (horizontal/vertical, inversión de arrays, rechazo, no-op y guardar/cargar). Node completo **1761/1761**, 0 fallos/omitidos; TypeScript app/core verde. `qa/montaje-numerico-m5.mjs` **17/17**, 0 JS, con ratón/inputs reales, Undo/Redo, rechazo atómico, autoguardado y recarga. Build QA **436 módulos**. `qa/se-guarda-solo.mjs` y `qa/sin-fantasmas.mjs` verdes sobre la ruta de dimensiones; `qa/riel.mjs` volvió verde tras corregir su localizador de agarre, que apuntaba al tirador de redimensionamiento y no al tramo móvil. La primera ejecución roja de esa sonda tenía 0 mm de desplazamiento y no se interpretó como producto corregido por la sonda.
- Producción **436 módulos** y HTML único Build **`45E70EAE65`**. Smoke offline `qa/empaquetado.mjs` **TODO OK** para V3–V9 desde `file://`, IndexedDB funcional, 0 JS/HTTP externo obligatorio; entrega web/escritorio idéntica. `herramientas/verificar-entrega.mjs` comprobó frescura y reproducibilidad LF/CRLF. Primer intento de smoke sin `PLAYWRIGHT_BROWSERS_PATH` no arrancó Chromium local; el mismo artefacto pasó al indicar su ruta. El `pnpm` de este host es un wrapper de Codex que intentó consultar npm sin acceso y se interrumpió; se ejecutaron directamente sus comandos equivalentes locales (`tsc` app/core y Node), no se atribuye falsamente un `pnpm test` verde.
- Esto cubre solo la traslación numérica del soporte. No acredita giro con dependientes, reglas de snap por modo, medidas certificadas, biblioteca VIS-01 ni aceptación visual M5. `main` permanece V9; no se ha ejecutado M9 ni declarado 1.0.

### Incremento siguiente: aparato y prevención de giro engañoso

- `planPosicionAparato` valida identidad única, medidas finitas, borde de placa, holgura, largo útil del riel y montaje personalizado. Un aparato DIN solo avanza por el eje de su `rielId` persistente; uno atornillado sin riel puede moverse libremente sin adquirir anclaje por cercanía. La puerta usa otro espacio de coordenadas y no se edita desde esta herramienta de placa. No certifica fijación mecánica ni transforma la orientación del propio aparato.
- `test/posicion-aparato.test.ts` **4/4**: inversión de arrays, decimales, serialización, horizontal/vertical, choques, placa atornillada, DIN sin anclaje y veto de coordenadas de puerta. El Node integrado posterior pasó **1765/1765**, 0 fallos/omitidos; TypeScript app/core verde. QA visible ampliada `qa/montaje-numerico-m5.mjs` **27/27**, 0 JS: aparato, malla 3D/cables, Undo/Redo, recarga, rechazo de desanclaje/choque/campo vacío y veto de giro de riel ocupado sin historial. La regresión M1 de pieza personalizada **33/33**, 0 JS, incluye posición decimal atornillada y reapertura. Build QA **437 módulos**.
- Girar un riel con aparatos todavía sería una mentira física porque el modelo no representa giro de cuerpo/borne. Se bloquea con aviso; giro de estructuras vacías exige encajar en placa. Es una medida de integridad, no implementación de MON-01 completo. La ejecución visible ampliada fue más lenta en este host (~4 min) pero continuó avanzando por las 27 comprobaciones, sin timeout.
- Producción **437 módulos**, HTML único Build **`8EE6122AE7`**. `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9, con IndexedDB, exportación y 0 JS/HTTP externo obligatorio. `herramientas/verificar-entrega.mjs` confirmó bytes actuales, igualdad web/escritorio y reproducibilidad LF/CRLF. El host tardó más que en el checkpoint previo, pero la campaña siguió progresando y cerró con exit 0. Estas comprobaciones focales no son la campaña M9 completa ni una aceptación visual humana.

### MON-04 parcial: lista de selección utilizable

- La lista de aparatos internos filtra por términos normalizados de marcado/ID/descripción/fabricante/referencia sin alterar `Proyecto`; el contador conserva visible el total cuando hay filtro y el vacío explica que no se eliminaron piezas. Una fila admite Enter/Espacio y tiene acción explícita de enfocar usando la selección y cámara existentes. No se añadieron IDs visuales ni otra verdad de inventario.
- `qa/lista-dispositivos-m5.mjs` **9/9**, 0 JS: búsqueda sin acento, selección por ratón/teclado, foco sobre pieza, cero coincidencias, Escape y Proyecto intacto. La primera iteración falló en una tolerancia QA de 1 mm entre caja enfocada con rótulo y bulto sin rótulo (diferencia medida 2,7 mm); la aserción final exige pivote a menos de 5 mm del bulto y desplazamiento real de cámara. No se cambió el producto para tapar esa diferencia. Build QA **437 módulos**, `qa/montaje-numerico-m5.mjs` posterior **27/27**, 0 JS. Ocultar/aislar/bloquear siguen pendientes: buscar no es ocultar.
- Node integrado posterior **1765/1765**, 0 fallos/omitidos y TypeScript app/core verde. Producción **437 módulos**; HTML único Build **`B8DC47363E`**. `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9, IndexedDB/descargas funcionales, 0 JS/HTTP externo obligatorio. `herramientas/verificar-entrega.mjs` confirmó igualdad web/escritorio y reproducibilidad LF/CRLF. No se ha ejecutado aún una campaña M9 integral ni revisión humana de la selección en varias resoluciones.

### MON-04 parcial: visibilidad de sesión con picking coherente

- `src/modelo/vista-montaje.ts` define ocultación y aislamiento por ID fuera de `Proyecto`, con selección desde lista que recupera la pieza y cambio de documento que limpia el overlay. La escena aplica el mismo predicado a cuerpo y borne; el picking de Three se filtra explícitamente porque `visible=false` no basta para sus raycasts. Los cables y la conectividad permanecen visibles e intactos; la UI lo explica. No se presenta como lock de edición ni como aislamiento de cableado.
- `test/vista-montaje.test.ts` **4/4**: reversibilidad, orden de IDs, limpieza de IDs viejos y recuperación desde lista. `qa/lista-dispositivos-m5.mjs` ampliada **19/19**, 0 JS: interacción por UI, cuerpo oculto, mismo píxel ya no seleccionable, bornes que no reaparecen al entrar en Cablear, aislamiento/restauración, proyecto/historial intactos y otro ejemplo con KM1 visible. Node integrado posterior **1769/1769**, 0 fallos/omitidos; TypeScript app/core verde. Build QA **438 módulos** y regresión de montaje numérico posterior **27/27**, 0 JS. Falta bloqueo de edición, recuperación de puerta/campo en la lista y aceptación visual humana.
- La primera solicitud de empaquetado fue interrumpida por un `401 Unauthorized` del servicio de aprobación del entorno, antes de ejecutar Vite; se recuperó la herramienta y se repitió sin cambiar el producto. Producción **438 módulos**; HTML único Build **`BA45F21D52`**. `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9, IndexedDB/descargas funcionales, 0 JS y 0 HTTP externo obligatorio. `herramientas/verificar-entrega.mjs` confirmó bytes actuales, igualdad web/escritorio y reproducibilidad LF/CRLF. Esto cierra el empaquetado del checkpoint, no la aceptación M5/M9 ni la versión 1.0.

### MON-04 parcial: la misma lista recupera placa, puerta y campo

- El cajón de Seleccionar dejó de omitir `campo: true`; cada fila usa el ID de `Proyecto` y distingue ubicación derivada de `colocaciones`/`campo`, no un duplicado 3D. La búsqueda incluye tipo y ubicación, además de marcado, descripción y referencia. `qa/lista-dispositivos-m5.mjs` **24/24**, 0 JS: motor de campo localizable/seleccionable y piloto de puerta oculto/recuperado en el ejemplo estrella-triángulo, además de las 19 comprobaciones anteriores. Build QA **438 módulos**; `qa/fixture-puerta.mjs` posterior **TODO OK**, 0 JS, sin cambio de clases, mazo ni bonding. TypeScript app/core y Node integrado posteriores **1769/1769**, 0 fail/skip. Producción **438 módulos**, Build **`F294BAD245`**, `qa/empaquetado.mjs` **TODO OK** desde `file://` para V3–V9, 0 JS/HTTP externo obligatorio; `verificar-entrega.mjs` confirmó igualdad web/escritorio y reproducibilidad. Bloqueo de edición y aceptación humana de la vista siguen pendientes.

### M6 parcial: ruta de referencia XYZ opt-in

- Fixture V9 de arranque: un trazado manual XYZ se carga sin conversión implícita; XY sin Z permanece ambiguo. El formato 3 persiste nodos ordenados con ID, marco, modo y geometría. Dos rutas manuales coincidentes no se expulsan ni unen topologías; invertir arrays y recargar conserva los mismos segmentos. El tubo M6, la medición XYZ y el picking consumen la misma polilínea recta. El marco fijo se veta para puerta/campo/sin colocación, también si un componente de campo tiene una colocación contradictoria.
- `test/ruta-fisica-m6.test.ts` **8/8**; tests integrados **1777/1777**, 0 fail/skip, 30,48 s tras la última corrección. TypeScript app/core verde; build QA y producción **439 módulos**. QA visible `qa/ruta-fisica-m6.mjs` **12/12**, 27,7 s, 0 errores JS: adopción/cancelación, longitud visible, edición X sin alterar Y/Z, Undo/Redo y recarga. El paquete offline legacy pasó **17/17** en 22,8 s antes del endurecimiento final de `campo`; esa regla no altera el exportador pero este resultado no se presenta como nueva ejecución posterior. La regresión `qa/cables-fusion.mjs` pasó sobre el corte previo a esa misma regla: 0 pares con eje 3D coincidente, 59/59 frontal, 186/186 picking, 0 JS; tampoco se atribuye al artefacto final de forma retroactiva.
- HTML único final Build **`63412C6EC1`**: `qa/empaquetado.mjs` **TODO OK** desde `file://` para V3–V9, IndexedDB/descargas y 0 errores JS/solicitudes HTTP externas obligatorias. `herramientas/verificar-entrega.mjs` confirmó `desktop/app.html` y `dist-final/TableroStudio.html` actuales, idénticos a la reconstrucción y reproducibles entre LF/CRLF. No se ejecutó la campaña completa M9 sobre esta nueva ruta; no hay aprobación visual humana de rutas densas/canaletas/puerta ni cierre M6.

### M6 parcial: no inventar impedancia y avisar interferencia manual

- La prueba CAB-27 usa una fuente física activa: tras retirar la longitud declarada de un conductor con ruta XYZ, el solver conserva su rama con origen `NO_MODELADO` y no calcula un conductor físico a partir de la distancia 2D entre aparatos. Las rutas legacy conservan su estimación anterior. Esta defensa no adopta todavía la longitud XYZ para V9 ni define reservas/puntas.
- `diagnosticoRutaManual` consulta la peor cercanía con otro cable y la peor invasión de sólido y de partes de canaleta para la ruta seleccionada, solo cuando la persona pulsa el botón. La prueba rápida conserva literalmente nodos/IDs tras una coincidencia manual; la QA visible atraviesa un tercer aparato intencionalmente y verifica aviso sin mutación. `qa/ruta-fisica-m6.mjs` **13/13**, 35,2 s, 0 JS. TypeScript app/core y Node **1777/1777** en 29,0 s, 0 fail/skip; build QA/producción **439 módulos**.
- HTML único Build **`7158E5AD54`**: `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9, con IndexedDB/exportación y 0 JS/HTTP externo obligatorio; `verificar-entrega.mjs` confirmó ambos HTML actuales, bytes reproducibles LF/CRLF. No se repitieron aún los gates históricos completos M9 ni una inspección humana de curvas, ductos o puerta; CAB-10/11/27 y M6 siguen parciales.

### M6 parcial: inserción topológica y cancelación completa del gesto

- El oráculo rápido CAB-06/07 usa un recorrido que visita dos veces el mismo XYZ. La reproyección legacy inserta en el tramo equivocado; los índices de vértice M6 insertan después de la pasada efectivamente seleccionada. La prueba focal de M6 pasó **9/9**. La QA visible de doble clic, profundidad, Undo/Redo y persistencia, junto con Escape previo al umbral, Escape tras preview y pérdida de foco, pasó **20/20** en **69,7 s**, 0 JS, servidor/Chromium cerrados. El test de pérdida de foco despacha el evento de navegador; Escape y doble clic son gestos de teclado/ratón reales.
- TypeScript app/core y batería completa Node **1778/1778**, 0 fail/skip, **28,65 s**; build QA **439 módulos**. Montaje numérico **27/27**, 0 JS. Fusión/picking legacy en tres ejemplos: **0** ejes 3D coincidentes, **59/59** frontal, **186/186** clics semánticos, 0 JS; se ejecutó antes del pequeño añadido posterior de cancelación al perder foco y no se atribuye a una build idéntica a la última.
- HTML único Build **`321C8D0B28`**: smoke offline V3–V9 `qa/empaquetado.mjs` **TODO OK**, 0 JS y 0 HTTP externo obligatorio. `herramientas/verificar-entrega.mjs` confirmó `desktop/app.html` y `dist-final/TableroStudio.html` frescos, idénticos y reproducibles para LF/CRLF. M6, campaña histórica M9, aceptación visual humana y `FUNCTIONAL_COMPLETE` permanecen pendientes.

### M6 parcial: edición visible de un tramo interior

- `desplazarTramoInteriorM6` propone sin mutar el original un delta XYZ sobre dos nodos contiguos; una prueba independiente verifica IDs, tercer nodo intacto, bornes invariantes y rechazo atómico de índice/valor/dominio inválido. El formato y la creación visible comparten el máximo de 128 nodos. `test/ruta-fisica-m6.test.ts` **11/11** y batería Node **1780/1780**, 0 fallos/omitidos, **27,4 s**.
- QA navegador `qa/ruta-fisica-m6.mjs` **27/27**, **64,6 s**, 0 JS: campos vacíos no generan Undo, mover un tramo edita sus dos nodos y no el tercero, Undo/Redo único, borrar por ID/recuperar y guardar/reabrir. El inspector a 1024 px no desborda; se revisó captura del panel visible, no constituye aceptación visual humana de ruta industrial densa. La ejecución fue anterior al cambio textual que retiró el icono de mano de la etiqueta del recorrido, sin cambio de lógica.
- HTML único Build **`22C108D172`**: producción 439 módulos, las dos copias están frescas y el verificador reproduce los mismos bytes LF/CRLF. El smoke V3–V9 de este Build y la campaña histórica M9 completa quedan **NO VERIFICADOS** en este checkpoint; el último smoke verde pertenece al Build `321C8D0B28`. CAB-07 y M6 siguen parciales.

### M6 parcial: aviso del recorrido al confirmar un gesto

- `app/main.ts` ejecuta `diagnosticoRutaManual` una vez, después de adoptar la respuesta del Worker de ruteo vigente; no lo ejecuta por cada `pointermove`. El aviso conserva la ruta confirmada y no crea otra operación Undo. La QA usa ratón real: el nodo final permanece fuera de `obstaculo` (X < 205 mm), pero el tramo lo cruza; se ve `Invade aparato obstaculo` y Undo restaura el nodo anterior.
- App/core TypeScript verdes; Node **1780/1780**, 0 fallos/omitidos, **26,1 s**; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **29/29**, **74,7 s**, 0 JS, navegador/servidor cerrados. Un intento intermedio expiró en precondición porque `app/dist` contenía producción sin `window.qa` tras empaquetar; se recompiló QA y la misma regresión pasó sin reducir cobertura. La build de producción 439 módulos generó Build **`248AC1EB63`** y `verificar-entrega.mjs` confirmó frescura e igualdad byte a byte de `desktop/app.html`/`dist-final/TableroStudio.html`, además de reproducibilidad LF/CRLF. El smoke `qa/empaquetado.mjs` completó V3–V9 desde `file://`, `TODO OK`, 0 JS y 0 HTTP externo obligatorio.
- No se ejecutaron nuevamente `qa/cables-fusion.mjs`, montaje M5 ni el gate M9 integral después de este aviso. El diagnóstico es informativo y categórico; no comprueba radios, capacidad de ducto, calor, sujeción o taller. **CAB-10/11, M6 y 1.0 permanecen NO ACEPTADOS.**

### M6 parcial: preview local de sólidos y profundidad real

- El oráculo geométrico de `primerSolidoEnTramosDelNodo` detecta un aparato cruzado por el segmento aunque el nodo esté fuera, descarta los sólidos de los dos aparatos conectados y no confunde un cruce X/Y con colisión si el recorrido está en otra Z. `primerSolidoEnPunto` aplica el mismo volumen 3D al punto. El preview reutiliza la ruta provisional ya dibujada y cajas construidas al iniciar el gesto, sin reconstrucción global para esta comprobación.
- TypeScript app/core verdes, tests M6 **12/12** y Node integrado **1781/1781**, 0 fallos/omitidos, **30,3 s** en la ejecución final. Build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **31/31**, **101,7 s** sobre el ajuste final del dominio, 0 JS, con gesto real, aviso textual durante drag, diagnóstico después, Undo y caso contrario a 70 mm Z. Sonda opcional de 30 movimientos dentro de Chromium: listener total **55,5 ms**, mediana **2,0 ms**, p95 **3,2 ms**, máximo **3,6 ms** en este fixture pequeño antes del ajuste final del límite de área; no acredita tablero denso ni otros equipos.
- Build offline **`98DE003B29`**, 439 módulos, con dos HTML byte a byte actuales y reproducibles LF/CRLF según `verificar-entrega.mjs`. `qa/empaquetado.mjs` ejecutó V3–V9 desde `file://` sobre ese Build y terminó `TODO OK`, 0 errores JS y 0 solicitudes HTTP externas obligatorias. Quedan NO VERIFICADOS el gate histórico M9 posterior, la capacidad visual de canaleta densa, radios y aceptación humana. **M6 y 1.0 NO ACEPTADOS.**

### M6 parcial: corredor útil y diente durante el drag

- El oráculo focal coloca un conductor de radio 2 mm en una canaleta 40 × 40 mm: recorrido axial e ingreso por ranura sin invasión; ingreso por diente con invasión. La QA visible usa además el radio real del cable del proyecto. El primer intento del fixture, X=75 mm, quedó rojo: el tramo oblicuo tangente no invadía para ese radio (la consulta pura dio `undefined` a 1,5 mm y `diente` a 2 mm). Mover el fixture a X=74 mm produjo invasión real; no se alteró el criterio de detección ni la aserción.
- TypeScript app/core verdes, test M6 **13/13**, Node integrado **1782/1782**, 0 fallos/omitidos, **30,8 s**. Build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **34/34**, **180,8 s** en la última repetición tras añadir captura de consola al segundo contexto, 0 JS: conserva 31 comprobaciones anteriores y añade ranura libre, diente advertido, ausencia de errores en el segundo contexto; ambos contextos y servidor cerrados. La medición sintética previa del checker local fue ~0,48 ms/consulta para 500 mm y ocho ductos y ~1,3 ms para 5 m; no equivale a p95 del listener en escena densa. La build QA concurrente con el smoke tardó **5:47** por contención de recursos, frente a ~8 s aislada; terminó sin fallo. No ejecutar ambos en paralelo para benchmarks.
- Build offline **`7E3B4FD8F9`** verificado fresco, idéntico web/escritorio y reproducible LF/CRLF. `qa/empaquetado.mjs` recorrió V3–V9 desde `file://` y terminó `TODO OK`, 0 JS y 0 solicitudes HTTP externas obligatorias. Capacidad, agrupación, radio mínimo, inspección visual densa y gate M9 integral quedan **NO VERIFICADOS**. **CAB-11/M6 y 1.0 NO ACEPTADOS.**

### M6 parcial: proximidad local y exención acotada del borne

- La prueba rápida reproduce el falso negativo antiguo: dos segmentos de 80 mm con borne inicial común obtenían `undefined` porque la distancia mínima se medía en el tornillo. Tras recortar la zona física compartida, se advierte la coincidencia externa; un segmento de 8 mm dentro de ella sigue permitido. El mismo oráculo comprueba profundidad Z distinta, contacto de otro tramo y consulta limitada al nodo movido.
- TypeScript app/core, tests M6 **14/14** y Node completo **1783/1783**, 0 fail/skip, **33,9 s**; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **37/37**, **473,6 s**, 0 JS en la ejecución final: contacto visible durante gesto real, Escape restaura ambas rutas, ranura/diente conservan su contrato. `qa/cables-fusion.mjs` pasó sobre la misma build QA: 0 pares fusionados en los tres ejemplos, 59/59 cables con punto frontal, 186/186 picking semántico y 0 JS. La pasada M6 anterior a la corrección del borne tardó 237,1 s; no atribuir la variación total de host al algoritmo sin medición pareada.
- Producción **439 módulos**, HTML Build **`C92FD75BF4`**. `verificar-entrega.mjs` confirmó ambas copias frescas, idénticas y reproducibles LF/CRLF. `qa/empaquetado.mjs` recorrió V3–V9 sobre este Build desde `file://` y terminó `TODO OK`, 0 errores JS y 0 peticiones HTTP externas obligatorias; navegador y servidor cerrados. No se ejecutó el gate M9 integral ni aceptación visual humana de ducto denso/curvas/puerta. CAB-24 (localidad durable), CAB-25 (radios) y CAB-27 (adopción de longitud) siguen pendientes; **M6 y 1.0 no están completos**.

### M6 parcial: equivalencia de preview y ruta confirmada

- El oráculo crea dos nodos manuales consecutivos con el mismo XYZ. La ruta provisional y la definitiva son profundamente iguales en extremos, puntos, geometría e índices; ambos IDs persisten aunque se dibuje un solo vértice. Tras mover uno, el índice nuevo coincide sin leer geometría vieja de caché. No cambia el modelo eléctrico ni la ruta legacy.
- TypeScript app/core, test M6 **15/15**, Node completo **1784/1784**, 0 fail/skip, **26,1 s**; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **37/37**, **234,3 s**, 0 JS sobre el constructor único. `qa/cables-fusion.mjs` del mismo código pasó 0 ejes coincidentes en tres ejemplos, 59/59 frontal, 186/186 picking, 61/61 sin fantasmas y 0 JS. La build de producción **439 módulos** produjo HTML **`5ED0A3B393`**; `verificar-entrega.mjs` confirmó copias actuales, idénticas y reproducibles LF/CRLF. `qa/empaquetado.mjs` recorrió V3–V9 sobre este Build desde `file://` y terminó `TODO OK`, 0 JS y 0 peticiones HTTP externas obligatorias. M9 integral, curvas, localidad durable y aceptación visual humana siguen **NO VERIFICADOS**.

### MON-04 parcial: bloqueo de edición de sesión

- La lista ofrece Bloquear/Desbloquear por ID con estado accesible. La selección, foco y visibilidad siguen disponibles; el veto protege ficha, colocación, arrastre simple/múltiple, borrado, controles del frontal y adopción de revisión. El riel comprueba sus anclados; la edición global de estructura revierte si desplaza una pieza bloqueada. Se retiran los tiradores de la pieza o riel bloqueado. Ocultar/restaurar no cambia el bloqueo y abrir otro tablero lo limpia. No se persiste ni afecta al circuito, informes, simulación o Undo. Conectar/quitar conductores sigue permitido porque esos son objetos independientes.
- `test/vista-montaje.test.ts` pasó **6/6**; la suite Node integrada final alcanzó **1786/1786**, 0 fail/skip, 29,9 s. TypeScript app/core verde. Build QA del código final **439 módulos**. `qa/lista-dispositivos-m5.mjs` pasó **32/32**, 0 JS sobre el código final. `qa/montaje-numerico-m5.mjs` pasó también sobre ese código **27/27**, 0 JS, conservando anclajes, Undo/Redo y persistencia. Dos intentos intermedios de la suite de lista agotaron 20 s localizando una fila tras cambiar de ejemplo con un filtro antiguo; no fueron aserciones de producto. La prueba final lee el bloqueo efímero mediante observación QA, mientras la acción de bloquear y su efecto de edición se ejercitan por UI.
- Producción final **439 módulos**; HTML Build **`94AE79D753`**. `verificar-entrega.mjs` confirmó copias web/escritorio frescas, idénticas y reproducibles LF/CRLF. `qa/empaquetado.mjs` terminó **TODO OK** desde `file://` para V3–V9 sobre este Build, 0 errores JS y 0 peticiones HTTP externas obligatorias. No se ejecutó el gate integral M9 ni aceptación visual humana; **M5 y 1.0 no están completos**.

### CAB-24: contraejemplo aislado de redistribución

- Comando: `node qa/diagnostico-localidad.mjs` después de compilar. Resultado: `w4` cambia a manual XYZ; de 27 cables ajenos comparables, **8** cambian puntos/nodos/Z (`w1`, `w2`, `w24`, `w27`, `w5`, `w6`, `w8`, `w9`). Los 8 siguen distintos de la ruta original tras guardar/cargar; el ruteo del proyecto modificado sí es determinista al reabrir. `--assert-locality` confirma el rojo real con exit **1**. Esta sonda no se añadió al gate verde ni se relajó para ocultar el incumplimiento.
- `git diff --check` y la sonda focal sin bandera pasaron; no se modificó producto. No se ejecutó una campaña de build/navegador porque solo se añadieron una sonda diagnóstica y documentación, y la condición funcional sigue roja. M6/1.0 continúan no aceptados.

### CAB-24: núcleo de plan automático V4, sin adopción UI

- `test/plan-ruta-automatica.test.ts` **8/8**. El plan en series planas vuelve a producir exactamente los mismos puntos XYZ, nodos y anclajes. Con los 28 cables del arranque directo fijados, cambiar solo `w4` deja los otros 27 iguales; cargar e invertir arrays no cambia el resultado. Los cinco ejemplos se capturan/reabren sin variación de geometría. Un ducto cercano altera la firma local; un aparato lejano no. La importación rechaza versión 3 con plan V4, dos escritores, contexto obsoleto y datos hostiles, sin rerouting reparador.
- App/core TypeScript verde; Node completo **1794/1794**, 0 fail/skip, **26,8 s**. Build QA **441 módulos**, **7,5 s**. `pnpm test` invocó una verificación/instalación de paquetes de red y falló por EACCES antes de ejecutar tests; se detuvo ese wrapper y se ejecutaron los binarios locales equivalentes. No hubo QA de navegador sobre esta frontera sin UI. No se afirma cierre de CAB-24, M6, M9 ni versión 1.0.

### CAB-24: edición visible, persistencia y revisión local

- `test/plan-ruta-automatica.test.ts` pasó **12/12**: incluye alta de conductor de 6 mm² en borne ya ocupado sin desplazar 28 planes aceptados, sin eje prolongado compartido con `w4`, y roundtrip; solo se asigna el cable objetivo; declarar la caja implícita no invalida rutas físicas idénticas. El origen del plan no incorpora la lista de otros conductores del borne: esta es una reserva fija, no una solicitud de redistribución colectiva. El lector admite plan anterior junto al estado `pendiente` para revisión, pero no lo representa como vigente.
- `qa/localidad-planes-m6.mjs` pasó **11/11**, **81,1 s**, 0 JS: importa el ejemplo editable, cambia sección de `w4` desde el inspector, comprueba 27 rutas ajenas exactas, persiste/reabre, mueve una canaleta por el formulario real, observa **15/28** rutas dependientes pendientes sin alterar las restantes, reabre, confirma una propuesta del inspector y verifica Undo/Redo y vecinos inmóviles. `qa/ruta-fisica-m6.mjs` pasó **37/37**, **233,0 s**, 0 JS; ahora Escape compara el proyecto completo y no solo el nodo. Chromium y los servidores de ambas suites cerraron.
- TypeScript app/core verde, Node integrado **1798/1798**, 0 fallos/omitidos, **38,0 s**; build QA **441 módulos** en **8,25 s**. `qa/cables-fusion.mjs` volvió a pasar sobre la misma build: cero ejes coincidentes, 59/59 frontal, 186/186 picking, 61/61 sin fantasmas, 0 JS. No se atribuye al código nuevo una ejecución histórica previa. `pnpm test` como wrapper no se reintentó por el problema de resolución del gestor en este host; se ejecutaron directamente `tsc --noEmit -p tsconfig.app.json`, `tsc` y `node --test dist/test/*.test.js`.
- Permanecen **NO VERIFICADOS**: alta desde UI de varios cables en mismo borne, elección de alternativas físicas en canaleta densa, movimiento de aparato sobre un plan, costo de snapshot/autosave a escala, inspección visual humana y gate M9 completo. CAB-24/M6/1.0 no se declaran terminados.

### CAB-24: alta visible y cambio de anclaje

- `qa/localidad-planes-m6.mjs` se amplió sin sustituir los 11 casos previos. La versión actual pasó **16/16** en **156,8 s**, sin errores JS: formulario de conexión visible de `-Q1:2` a `-KM1:3/L2`, sección 6 mm², plan nuevo persistente, 28 planes ajenos inmóviles y coincidencia 3D con `w4` menor o igual a la zona permitida del borne. Guardar y reabrir conserva los 29. El mismo flujo mueve una canaleta (**16/29** pendientes), confirma una propuesta, luego mueve `q1` por inspector; `w4` queda pendiente sin ruta dibujada en el anclaje antiguo y Undo restituye anclaje/plan.
- TypeScript app/core y Node completo **1798/1798**, 0 fallos/omitidos, **37,4 s**, mediante binarios locales. Build QA **441 módulos**, **8,82 s**. No se repitieron M6/fusión tras este último cambio focal de creación de cable; sus resultados verdes anteriores permanecen como evidencia de la versión previa, no del corte actual. Faltan estrés/autosave en escenas densas, varias altas desde un mismo borne, elección de alternativas y aceptación visual de la canaleta. CAB-24/M6/1.0 permanecen parciales.

### CAB-24: localidad en tablero denso

- `qa/localidad-densa-m6.mjs` toma el ejemplo estrella-triángulo, con **61 conexiones y 59 rutas físicas**. La edición UI de `w18` preserva exactamente las otras 60 rutas, genera 59 planes y la reapertura con el mismo almacenamiento conserva esos planes. La versión ampliada pasó **5/5** en **48,4 s** tras pasar 5/5 en 52,6 s antes de reforzar la comprobación de identidad; 0 JS, servidor y Chromium cerrados. El segundo cambio UI (color) persistió con el mismo ID y sin snapshot extra (**1/1**). Documento antes del segundo cambio: **299 972 bytes**; asignación hasta 59 planes **3263/2876 ms**; espera del primer guardado **793/1379 ms**. No son mediciones de p95 ni de cuota de IndexedDB.
- La medición Node aislada sobre cinco ejemplos situó el documento con planes entre **72 559** y **301 459 bytes**, y la asignación entre **119** y **2093 ms**. `docs/avance/CAB24_LOCALIDAD.md` registra el desglose y los límites. La captura de la escena densa no sustituye una revisión desde varios ángulos de radios, holgura y cruces. No se ejecutó el gate M9 integral ni se declara CAB-24, M6 o 1.0 aceptado.

### CAB-24: snapshots densos en repositorio transaccional

- `test/planes-snapshots-m6.test.ts` pasó **1/1** en **4,75 s**: 61 conexiones, 59 planes V4, veinte snapshots retenidos, poda determinista del primero, restauración de los planes exactos y aislamiento del segundo tablero. Documento: **301 581 bytes JSON**; veinte snapshots: **6 031 696 bytes JSON**, excluyendo overhead de IndexedDB y assets. El tiempo de test incluye asignación de rutas y no se usa como latencia de autosave. Se requiere aún medición de cuota/latencia IndexedDB real y revisión visual densa antes de aceptar CAB-24/M6.
- App/core TypeScript verdes; Node integrado **1799/1799**, 0 fallos/omitidos, **29,8 s**. No se volvió a construir el navegador: este incremento solo cambia tests y documentación sobre el producto ya compilado. El gate M9 integral no se ejecutó en este corte.

### CAB-24: secuencia de cinco conductores y distinción borne/contacto

- La primera aserción nueva falló con **4,970 mm** de coincidencia de eje entre el cuarto cable añadido y el primero. La localización exacta fue el último tramo hasta el mismo borne destino, dentro de `radioZonaSalidaBorne`: era un falso positivo del oráculo, no una fusión prolongada. `longitudCoincidenteFueraDeBornes3D` utiliza la misma exención geométrica que la detección de contacto; un caso sintético exige 82 mm de eje coincidente con un solo borne compartido y 64 mm con ambos, a partir de una recta de 100 mm y zona de 18 mm por extremo. La secuencia pasó sin coincidencia de eje fuera de bornes; los planes existentes permanecieron exactos tras cada alta, reapertura e inversión de arrays.
- La misma geometría produce **tres contactos de aislaciones fuera de la zona** según `conflictosDe`. Se informan como límite real de fabricabilidad; la prueba de no fusión no los convierte en ruta aprobada. El editor muestra un aviso al aceptar un nuevo plan con contactos/invasiones, sin ejecutar el diagnóstico durante `pointermove`; `qa/localidad-planes-m6.mjs` pasó **17/17**, **152,4 s**, 0 errores JS y comprobó que el mensaje no fuera tapado por «Cable conectado». CAB-23 sigue pendiente de preview, alternativas y decisión ANTES de aceptar una propuesta focal conflictiva.
- TypeScript app/core y suites focales de volumen/planes **148/148** verdes; Node integrado **1801/1801**, 0 fallos/omitidos, **31,2 s** en la repetición final posterior al aviso UI. Build QA **441 módulos**, **8,64 s**. `qa/cables-fusion.mjs` posterior al cambio de colisiones: 0 ejes coincidentes en los tres ejemplos, 59/59 frontal, 186/186 picking semántico, 61/61 sin fantasmas y 0 JS; duró aproximadamente 159 s. `qa/localidad-densa-m6.mjs` pasó **5/5**, **51,5 s**, 0 JS, ya con el aviso UI. `qa/ruta-fisica-m6.mjs` pasó **37/37**, **251,4 s**, 0 JS, conservando Escape, Undo/Redo, canaleta y proximidad. El gate M9 integral y aceptación visual del caso de cinco conductores siguen sin ejecutarse; no se afirma 1.0 completa.

### CAB-24: normalización de borneras y guardado del circuito VFD

- Reproducción roja: el circuito VFD armado por UI llegó a 99 comprobaciones industriales correctas, pero `esperarPersistencia()` rechazó el documento. La cadena de causa señaló un plan automático obsoleto; un diagnóstico focal encontró cero planes obsoletos antes de cargar y doce después de normalizar el documento, sin reparaciones. `leerTerminales` reordenaba las claves de los bloques del catálogo; la firma anterior usaba el texto de `JSON.stringify` en lugar del contenido físico. Una prueba Node nueva reprodujo el rechazo antes de la corrección.
- La firma nueva escribe bloques en orden estable y la comparación acepta firmas V4 previas con orden de claves distinto, sin alterar el plan guardado. La misma prueba verifica que mover `desde` de 0,1 a 0,2 sí marca el plan como obsoleto y que el renderer puede usar la firma antigua. El cargador conserva el rechazo de planes con cambios reales.
- App/core TypeScript y Node **1805/1805** (0 fallos/omitidos, 20,1 s); build QA **443 módulos** (7,03 s). La QA industrial completa pasó **123/123** en **307,3 s**, 14 cables VFD guardados, cambio de ejemplo, 0 JS. Su primera repetición tras corregir persistencia detectó una lectura de armadura 3D antes del siguiente frame; la aserción ahora espera hasta 4 s el mismo movimiento físico y pasó en la repetición completa. `qa/localidad-planes-m6.mjs` **20/20** en **128,3 s**, 0 JS. `qa/cables-fusion.mjs` verde sobre la misma build, 0 ejes coincidentes, 59/59 frontal, 186/186 picking semántico, 61/61 sin fantasmas, 0 JS.
- Esta evidencia es focal al defecto de firmas y sus fronteras inmediatas. No sustituye la campaña M9 completa ni demuestra alternativas de ruta, radios, metraje, cuota IndexedDB o inspección visual de canaleta densa. CAB-23, CAB-24, M6 y versión 1.0 siguen sin aceptación integral.

### CAB-23 parcial: ruta propuesta visible y cancelable

- El plan se calcula sobre una copia antes de tocar el documento vigente. El diálogo de alta automática aparece incluso con cero avisos y muestra extremos, ID, 46 puntos/75 mm de referencia en la captura de arranque directo, contactos/invasiones, número de rutas previas que quedarían fijadas y límites de fabricabilidad. El SVG frontal y la línea celeste 3D temporal provienen de los mismos XYZ; no son geometrías persistentes. La captura local mostró tablero y diálogo simultáneos, pero no demuestra holgura/radio/capacidad de ese cable.
- `qa/localidad-planes-m6.mjs` pasó **23/23** en 130,7 s, **23/23** en 135,7 s con captura y **23/23** en 142,0 s tras el último cambio textual, 0 errores JS. Exige plan intacto al cancelar, trazo 3D temporal del mismo número de puntos, limpieza al aceptar/cancelar, vecinos exactos y reapertura. `qa/prender-desde-cero.mjs` pasó con 13 cables persistidos y arranque/paro del motor, usando los dos caminos reales de conexión. `qa/simulacion-industrial.mjs` pasó **123/123** en **310,6 s**, 0 JS, con aceptación de 14 cables VFD y guardado. Estas dos últimas suites preceden a un ajuste puramente textual del diálogo, no a cambios de ruta o semántica.
- App/core TypeScript, **1805/1805** tests Node en 22,1 s, 0 fail/skip, y build QA **443 módulos** en 9,5 s pasaron después del ajuste textual. No se ejecutó el gate histórico completo M9 sobre este checkpoint. **No existen todavía alternativas automáticas distintas ni longitud de corte verificada. CAB-23, M6 y 1.0 siguen parciales.**
