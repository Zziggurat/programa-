# Evidencia funcional — matriz inicial

Estados: `YA_EXISTE_POR_VERIFICAR`, `PENDIENTE`, `EN_CURSO`, `IMPLEMENTADO`, `VERIFICADO`, `BLOQUEADO_EXTERNO`. «Existe» describe código o antecedente; no implica aceptación de todo el flujo.

| Flujo | Reutilización acreditable | Brecha principal | Evidencia a producir | Estado |
|---|---|---|---|---|
| FLU-01 · proyecto durable | `gestor-documentos`, repositorio IndexedDB, snapshots, QA multiproyecto V9 | estado visible de revisión/error y búsqueda cotidiana | dos documentos independientes, copia, error de almacenamiento y reapertura desde UI | EN_CURSO |
| FLU-02 · componente propio | definiciones con perfiles, assets SHA-256, snapshot de instancia y QA contactor | asistente reversible, montaje/puertos completos y vínculo técnico versionado | dos proyectos, edición de definición, imagen independiente, importación limpia | EN_CURSO |
| FLU-03 · esquema/tablero | `esquema-svg`, `esquema-pdf`, topología del proyecto | edición diaria multihoja sin segunda conectividad | edición desde esquema, cruce no conectado, undo, PDF | PENDIENTE |
| FLU-04 · simular/analizar | motores V2–V9 y QA industrial/física/Ingeniería | cobertura de casos objetivo y oráculos independientes | SIM-01…10, calidad de datos y límites | YA_EXISTE_POR_VERIFICAR |
| FLU-05 · propuesta V9 | `src/diseno-asistido`, preview/aplicación transaccional | integración y vigencia en nuevos flujos | BASE conservada, invalidación y decisión aplicada | YA_EXISTE_POR_VERIFICAR |
| FLU-06 · montaje/cables | escena Three.js, edición/canaletas/mazo existentes | anclaje, XYZ manual fiel, ruta única/migración y 3D profesional | fixtures V9, drag real, colisiones informativas, densidad | PENDIENTE |
| FLU-07 · revisión/documentos | exportadores, dossier, Ingeniería | paquete coherente y revisión obsoleta visible | identidad/cantidades, PDF renderizado, CSV/HTML hostil | PENDIENTE |
| FLU-08 · CAD/instalación | `app/mundo*`, extractor y derivado existente | pipeline trazable, unidad/Z, vínculos y derechos | manifiesto y QA sintético; original privado local | PENDIENTE |

## Activos y entorno de partida

- DWG privado: 22.657.062 bytes, cabecera `AC1032`, SHA-256 `7d2e1adefee31d4917d0cc6aba7f033b2ff7d89e7d378ebe04295ec04a47d1de`; inspección de contenido geométrico y licencia pendientes. No copiar al árbol público.
- `datos/cubierta.json` existente dice derivar de `Cubierta.dxf`, informa alturas supuestas, pero no identifica hash/revisión del original ni derechos. La relación con el DWG actual es `NO VERIFICADA`.
- Referencia de pruebas V9: 1412 unitarias, gate histórico 13/267 y entrega offline reportados para `4c2924c`; no son resultados del candidato funcional nuevo.
- Candidato M1 antes de checkpoint: typecheck y compilación TypeScript verdes; 1419/1419 tests unitarios en 60,9 s, 0 fallos y 0 omitidos. Build QA de Vite: 399 módulos, 5,85 s. `qa/flujo-m1.mjs`: 6/6 comprobaciones, 0 errores JS, ~18,4 s; `qa/componentes-personalizados.mjs`: recorrido completo de borrador, búsqueda/filtro, contactor, portabilidad y equivalencia eléctrica verde, 0 errores JS. Se usó Chromium instalado explícitamente, sin instalar dependencias. Esta evidencia deberá repetirse si cambia la frontera probada.
- Hallazgo y corrección M1: al copiar un ejemplo, la revisión inicial almacenada no contenía los números de conductor que ya se mostraban en el editor después del montaje. La creación ahora numera el candidato antes de persistirlo; el test de gestor exige equivalencia entre primera revisión y vista. El QA de copia conserva todos los campos declarados del ejemplo y comprueba identidad/contenido durable sin rechazar defaults legítimos añadidos por el cargador.
- Riesgo CMP-06 que motivó el incremento: una instancia conserva `{definicionId, revision}` y una copia profunda de su función, mientras la biblioteca anterior solo conservaba latest por ID. El exportador empaquetaba esa versión aunque el proyecto referenciara otra. El archivo append-only y el cierre exacto eliminan esa sustitución silenciosa para nuevas revisiones; **no** resuelven historial perdido antes de V3 ni adopción explícita de cambios por instancias conectadas.
- Incremento CMP-06: archivo append-only de revisiones y migración IndexedDB V2→V3 probada en Chromium real (2/2, 0 errores JS). Los tests de repositorio/codec comprueban actualización r1→r2, exportación exacta, despublicación sin pérdida del histórico, importación r1 con latest local r2, rollback y rechazo de paquetes falsos/incompletos. El paquete V1 aún no representa dos revisiones de un mismo ID; ese caso falla explícitamente. Las revisiones anteriores sobrescritas antes de V3 no se recuperan por inferencia.
- Incremento integrado M1: TypeScript y build QA (401 módulos, 5,87 s) verdes; 1442/1442 unitarias en 82,4 s, 0 fallos/omitidos. `qa/flujo-m1.mjs` 9/9 (A/B, búsqueda, revisión visible, copia de ejemplo y contenido durable), `qa/estado-documento-m1.mjs` 10/10 (nombre/revisión, fallo IndexedDB, reintento, solo lectura y barra a 1024/1366/1920), `qa/componentes-teclado.mjs` 12/12 (foco, inercia, Escape, borrador, lectura lenta/fallida de imagen), `qa/revisiones-componentes-idb.mjs` 2/2; 0 errores JS. La importación individual ahora confirma asset+definición+histórico atómicamente; tests de repositorio 35/35. El archivo `.tscomp` V1 de r2+ se recrea como r1 local y la UI lo informa; NO conserva el historial original.
- `qa/componentes-personalizados.mjs` completó creación PNG, 11 terminales/cables, simulación de polo/NA/NC, recarga, export/import de proyecto y comparación nativa: `TODO OK`, 0 errores JS, **antes** de la nueva importación individual atómica; repetir su frontera antes de aceptar M1. La identidad del `.tscomp` reimportado se confirmó como copia nueva porque la original archivada no puede reciclarse. `qa/multiproyecto.mjs` completó A/B/ejemplo/snapshots/restauración/duplicado/eliminación/reapertura: `TODO OK`, 0 errores JS; ese resultado no se invalida por el cambio del importador individual. Son comprobaciones focales, no campaña M9 ni aceptación humana.
- `qa/correcciones.mjs` auxiliar encontró un falso negativo del harness: solo preguntaba por el primer punto agarrable de cada cable, luego lo descartaba si caía fuera de zona, y omitía gestos bajo `if`. Ahora espera copia/rutas reales, busca puntos válidos con margen y muestra creciente, exige los gestos sin saltarlos y cierra Chromium/servidor. Ejecución focal posterior: 29/29, 0 errores JS, ~107 s. No se atribuye ese fallo inicial al producto.
- Perfil de rendimiento M0 (medición asistida, una ejecución): Windows 10.0.26200, i5-14400/16 hilos, 15,7 GB RAM; Chromium 153.0.8010.12, 1440×900, DPR 1, render SwiftShader (GPU física no verificada). En «Arranque estrella-triángulo» (17 aparatos, 61 conductores): editor interactivo 1,364 s, abrir ejemplo con confirmación visible 9,336 s, heap JS 20,5→112,3 MB, 30 cuadros mediana 8,1 ms y peor 9,4 ms, validación visible 6,284 s, 0 errores JS. No es R1 (30/100), ni p95 estadístico de apertura. El Chromium instalado no coincide con la revisión pedida por Playwright 1.61.1 y se indicó ejecutable explícito; no se instaló otra versión.
- Medición M0 complementaria en 1366×768, mismo ejemplo y Chromium SwiftShader (tiempos end-to-end Playwright+render, no solo handler): selección frontal 20 muestras p50 703 ms/p95 777 ms; drag frontal del piloto R, seis pasos, 8 muestras p50 6,98 s/p95 7,17 s; confirmación de autoguardado tras soltar 8 muestras p50 736 ms/p95 767 ms; Guardar/exportar JSON por botón 10 muestras p50 364 ms/p95 373 ms. 0 errores JS, navegador y servidor cerrados. El drag es una señal de riesgo para M5/M6 y requiere perfilado propio antes de atribuir causa. Captura del modal «Mis Tableros» a 1366×768 inspeccionada: búsqueda/acciones visibles sin recortes; aceptación visual humana pendiente.
- Escena sintética R1 en `qa/lib/densidad-r1.mjs`: 30 aparatos/100 conductores, extremos y bornes coherentes, 100/100 geometrías renderizadas, fixture SHA-256 `f860d627ebd6ed926b5bcddcc715ebd862efd71469e0ea67f2523311fa77523f`; **no es un tablero certificado**. `qa/benchmark-r1.mjs` está fuera del gate. En Windows/i5-14400/SwiftShader (NO GPU física), segunda ejecución separada de 707,1 s: importar 15,0 s; reapertura n=2 p50 34,0/p95 34,6 s; selección n=5 p50 3,70/p95 3,83 s; drag real de seis pasos n=5 p50 38,25/p95 38,63 s; flush n=5 p50 2,57/p95 8,73 s; Guardar JSON n=5 p50 26,76/p95 27,09 s; validar n=3 p50 15,97/p95 16,22 s; render directo n=30 p95 33,8 ms. Hubo 134 long tasks durante drag/persistencia (máximo 12,34 s), 0 errores JS, Chromium/servidor cerrados. **Rendimiento R1 NO ACEPTADO**; falta optimización causal y prueba en GPU física. El primer intento de benchmark fue rojo por un gesto inválido del harness (aux-b no estaba seleccionado), corregido seleccionándolo antes de arrastrar; sus cinco reaperturas p50 32,52/p95 39,52 s y selecciones p50 3,61/p95 3,80 s se conservan como run distinto, no se mezclan con los cuantiles finales.
- Perfil causal corto R1 (`R1_FOCAL=1`, ejecución separada de n=1, mismo fixture/hash): selección host 3,77 s pero listeners canvas 7,1 ms en down y 55,9 ms en up; drag host 27,60 s, seis movimientos 14,17 s y suelta 11,83 s. El listener de `pointerup` ocupó 10,97 s; contadores del editor registraron un reparto, una firma y **98/100 tubos reconstruidos** al soltar. CDP midió 11,48 s de script y 1,7 ms de layout durante el drag; las seis llamadas pointermove consumieron en el canvas p95 0,8 ms, de modo que no se debe culpar a ese handler sin más análisis de render/sincronización. Confirmar persistencia costó 708,5 ms al host frente a 0,5 ms dentro del navegador, compatible con coste de protocolo/serialización, no prueba de autosave lento. 30 long tasks (peor 10,97 s), 0 errores JS, proceso Chromium/servidor cerrados. Exploratorio: Playwright 1.61.1 usó Chromium 1243 explícito por falta de la revisión esperada; WebGL fue SwiftShader, **no certifica GPU física ni demuestra p95 estadístico con n=1**. Prioridad causal: aislar el coste de reconstrucción completa al soltar, preservando reparto, longitudes, picking y undo.
- Segundo perfil focal R1, separado del anterior y con igual fixture/hash: el hook de rutas dio 98 trazados antes/después de mover `aux-b`; **solo 10 cambiaron puntos** y 88 conservaron puntos/radio/color declarado, mientras se reconstruyeron 98 tubos. Los 10 fueron `dol-w3`, `dol-w28` y ocho reservas del bornero; radio y color no cambiaron. La comparación usa puntos que el hook redondea a 0,1 mm: demuestra una oportunidad de geometría incremental, pero no equivale a una comparación bit a bit de floats internos ni a una prueba de identidad visual. Drag host 26,32 s, `pointerup` canvas 10,565 s, CDP script 11,00 s, 0 errores JS y Chromium/servidor cerrados. Mantener reparto global determinista y medir una reconciliación de mallas por firma exacta antes de aceptarla.
- Reconciliación R1 (`6f4b222`, perfil focal nuevo n=1, mismo fixture/hash, SwiftShader): el cálculo global conserva 10 rutas cambiadas y 88 estables; se reconstruyeron **10 tubos para esas 10 rutas**, frente a 98 anteriormente. El desglose del `pointerup` fue 11.846,5 ms en canvas, de los cuales `rutasDeCables` empleó 11.788,4 ms y reconciliar geometría solo 14,6 ms. Drag host 31.273,9 ms: **no hay reducción de latencia demostrada** frente a las ejecuciones anteriores ni p95 estadístico. Picking humano de `dol-w3`, hover, undo/redo de rutas idénticas, 100 geometrías tras redo, 0 errores JS y cierre de Chromium/servidor fueron correctos. `qa/cables.mjs` mantuvo 63/63 cables y clones de resaltado estables 5→5→5→5 durante cuatro movimientos de preview (0 JS). Los 5 tests unitarios de reconciliación pasaron. El cuello medido es el repartidor global, no la construcción de mallas; R1 **sigue NO ACEPTADO** y requiere intervención focal posterior sin sacrificar determinismo/ocupación, además de GPU física.
- M1 focal: asistente de seis pasos `qa/asistente-componentes-m1.mjs` 19/19, biblioteca con teclado 12/12, adopción A/B 24/24 y `qa/componentes-personalizados.mjs` largo exit 0, todos sin errores JS. Capturas de Bornes y Revisión inspeccionadas a 1024/1366 px: al entrar los pasos quedan visibles, filas de bornes accesibles por scroll y Validar/Guardar en el pie final. La inspección visual humana del usuario sigue pendiente.
- M1 mecánica: `qa/montaje-componente-m1.mjs` ampliado 31/31 y build QA 406 módulos verdes, 0 errores JS: rechazo visible sobre canaleta, colocación atornillada sin DIN, anclajes, picking de la malla en la posición del modelo, drag a canaleta y riel con reversión exacta, drag a hueco válido sin `rielId`, inspector y persistencia/picking tras recarga; legacy marcado NO_EVALUABLE. La ampliación descubrió un **bug real**, no un timeout: `reconstruirDispositivoUno` omitía actualizar el registro `escenario.aparatos`, por lo que picking, arrastre y malla podían quedar desacoplados del modelo. Se corrigió el registro y las etiquetas junto con la regla de arrastre de placa. El primer intento de esta ampliación quedó rojo tras 10 checks por no encontrar punto real de agarre; se conservó como diagnóstico y no se atribuyó erróneamente al harness.
- M1 ficha técnica y portabilidad, frontera previa a UI de bloques: 169/169 pruebas focales del núcleo con V8 PRODUCTO+curva exactos, hashes/familia/cierre de dependencias, V1 legado, `.tscomp` V2, paquete proyecto V3 y rollback de importación. QA de ficha visible 11/11, 0 errores JS: selección de ficha, origen/procedencia y exportación/importación individual en almacenamiento limpio. Esa ejecución aún no acreditaba puertos multibloque ni aceptación visual humana; el incremento posterior se informa debajo.
- M1 puertos/ficha tras integración UI: typecheck app/core, build QA de 406 módulos y 33/33 tests focales verdes. `qa/asistente-componentes-m1.mjs` 19/19 y `qa/ficha-componente-m1.mjs` 18/18, 0 fallos, timeouts, skipped o errores JS; el segundo comprobó ID eléctrico distinto de rótulo, ancla u/v sobre imagen, bloque con lado/orden/tramo, avisos NO_EVALUABLE, ficha PRODUCTO V8 exacta y `.tscomp` V2 reimportado con activo en almacenamiento limpio. Capturas a 1024 px de bloques y ficha inspeccionadas: controles y pie alcanzables por scroll; no equivalen a aceptación humana del usuario. Se mantuvo la regla de que la ficha vincula una revisión exacta en la definición, sin afirmar autenticidad ni sustituir parámetros eléctricos automáticamente.
- M2 primer corte (evidencia histórica previa a las vistas opt-in): `test/esquema.test` + `test/esquema-svg-interaccion.test` 51/51 verdes, typechecks y build QA verdes. `qa/esquema-desconexion-m2.mjs` exit 0: inspección por ID/bornes en esquema incluso sobre ejemplo de solo lectura, veto antes de confirmar, cancelación sin mutación, desconexión mediante la acción central, Ctrl+Z/Y, guardado/reapertura y 0 errores JS. SVG exportado sigue vectorial sin hit-target interactivo. Este corte no acreditaba ESQ-01/02/03 completos; la frontera actual se detalla en el checkpoint posterior.
- M1 carcasa/recorte, checkpoint posterior: `a955637` añade carcasa opcional separada de función y bornes, snapshot de instancia, render dimensional, `.tscomp` V3 y paquete de proyecto V4 sin degradación silenciosa; 49/49 pruebas focales de persistencia/portabilidad. `5a98001` añade escala 100–400 % y encuadre desde controles visibles con PNG derivado acotado; bornes u/v quedan fijos, guardar exige aplicar el recorte, y el usuario es informado de que una futura edición parte del derivado. QA `qa/recorte-componentes-m1.mjs` con teclado real: 8/8, 128,8 s, 0 errores JS y Chromium/servidor cerrados. Capturas revisadas: ancla X1 visible y pie alcanzable, pero no sustituyen aceptación humana. UI de selección de carcasa sigue pendiente.
- M2 vistas opt-in, checkpoint posterior: `6f04a10` guarda representaciones por ID y hoja estable, resuelve bobina/polos/contactos sobre un solo aparato, rechaza vistas ambiguas/hostiles y conserva legacy. Pruebas core focales 149/149. `2ce0141` añade selección y arrastre por ID gráfico, undo/redo, borrado solo de vista, referencias cruzadas y pendientes visibles; `qa/esquema-representaciones-m2.mjs` verificó 6/6 hitos, 0 errores JS, con guardado/reapertura y cierre de Chromium/servidor. Typechecks app/core y build QA verdes; combinación rápida de recorte/carcasa/SVG/vistas: 25/25, 0 omitidas. No hay todavía creación/conexión desde la UI del esquema ni campaña completa sobre este HEAD.

## Evidencia del checkpoint funcional de 2026-09-23 noche

- M1 CMP-04: `9289fb9` expone en el asistente la carcasa legacy/riel/caja, acabado y retirada explícita, sin tocar perfil/bornes. QA `qa/carcasa-componentes-m1.mjs` 15/15 en 88,4 s, 0 JS, con selección y aspecto revisado en capturas. El montaje de carcasa no declara aptitud física cuando faltan datos: NO_EVALUABLE.
- M1 §9.4: `test/aceptacion-integral-m1.test.ts` prueba contactor propio en dos documentos, revisión r2 que cambia solo PNG, instancia B fijada a r1, adopción solo A, 28 cables idénticos, simulación antes/después, ficha exacta y asset en paquete importado en repositorio limpio; 1/1 focal y 61/61 combinado con adopción/core/repositorio. Tras pasar el preview de `<img>` a `<canvas>`, se corrigieron dos sondas obsoletas sin quitar comprobaciones: asistente 19/19 y adopción 24/24, 0 JS. `qa/componentes-personalizados.mjs` largo se repitió sobre el candidato integrado: `TODO OK`, 0 JS, con 11 conductores creados por clic, polo/NA/NC en reposo y marcha, recarga, paquete en navegador limpio, activo PNG y comparación con contactor nativo. El nativo solo declara NA; el NC propio se acredita por su perfil explícito, no inventando un borne nativo. Aceptación visual humana sigue pendiente.
- M2 ESQ-01/02: activación legacy todo-o-nada y desdoblamiento funcional desde UI (`b552b95`, `4072c0d`). La activación conserva metadatos de hojas existentes y muestra en preview diferencias título/columnas; rechaza casillas que no caben. `resolverComportamiento` adapta KM legacy sin persistir un perfil falso. 23/23 tests focales; QA de navegador 5/5, 0 JS, con dos hojas, cancelación, undo/redo, navegación sin perder formulario y reapertura; dispositivo y conductores únicos. No acredita todavía conexión nueva desde esquema, cruce no unido, renumeración ni M2 completo.
- M4 SIM-03: fallo runtime inyectado `salida-sensor-abierta` solo para sensor con contacto/salida digital; abre la rama de mando por el perfil y no persiste en `Proyecto`. Bomba de nivel: KM cae y motor se detiene al fallar la boya; test de detector PNP nativo/importado y testigo 3D desde `ResultadoSimulacion`. Focal simulación 86/86 y test visual; `qa/bomba-fallo-m4.mjs` 9/9 en 29,64 s, 0 JS, browser/servidor cerrados. Si se retira el fallo manteniendo la boya accionada, el control automático puede volver a pedir marcha: el QA retira primero la demanda. No se afirma que toda avería real de sensor falle abierta ni que exista interlock de reinicio en ese circuito.
- M0 R1: `12a79f3` retira físicamente barras obsoletas y poda por AABB conservando tangencias; `d1f2041` reemplaza Set por sello por segmento con prueba de multicíelda/overflow. 5/5 unitarios y 5/5 SHA-256 exactos del reparto (28, 23, 59, 98 y 98 rutas), sin cambiar puntos/radio/profundidad/orden. Comparación pareada aislada del último corte Node R1+drag: 6865→4518 ms medianas (−34,2 %, tres pares, carga variable). Medición independiente final: 4978 ms para R1+drag; no es el p95 de navegador. R1 sigue NO ACEPTADO hasta repetir gesto real y medir GPU física.
- Campaña integrada después de estos cortes: typecheck app/core, 1529/1529 pruebas (`node --test` TAP, 25,57 s, 0 fallos/omitidas), build QA 409 módulos/6,46 s. La primera campaña fue roja por el test estático `marcas-industriales` obsoleto: exigía `marca(p.id)` aunque el producto había separado `rotuloVisibleBorne` de `borne.id` en M1. La prueba ahora verifica rótulo visible vinculado al ID y fallback al ID; 7/7 focal y repetición integral verde. No se escondió el rojo ni se cambió el producto para satisfacer el regex.
- M4 SIM-10, diagnóstico sin implementación: la física usa longitud manual o distancia recta XY estimada; la ruta 3D del dibujo no alimenta `simularFisicaProyecto`. La sección sí altera R/X al recalcular. El editor tampoco invalida siempre el snapshot de simulación al confirmar edición local de sección/ruta. Tomar directamente la caché de geometría como longitud eléctrica sería costoso y semánticamente falso antes del contrato M6. Pendiente definir longitud declarada/derivada con origen y revisión, y compartirla con física/Ingeniería/documentos; no presentar SIM-10 como verificado.

## Evidencia focal posterior — 2026-09-23

- M0 R1: `qa/benchmark-r1.mjs` pasó un focal n=1 sobre el mismo fixture SHA `f860d627…` tras sustituir la espera implícita de navegación de Playwright por un clic humano y espera explícita de modo Trabajo. Importación 8,33 s, selección 4,70 s, drag host 24,43 s (seis movimientos 16,76 s, `pointerup` host 5,76 s/canvas 4,83 s), reparto 4,76 s, reconciliación 21 ms; 10/98 rutas y 10 mallas cambiadas. Hover/picking y undo/redo exactos verdes; 0 JS, navegador/servidor cerrados. Comparar 11,79→4,76 s de reparto con otro SHA permite ubicar la oportunidad, no afirmar una mejora p95 ni aceptación R1: SwiftShader, n=1 y sin GPU física. El primer intento rojo se agotó esperando navegación tras un clic que sí cambió de modo; no fue una aserción del producto.
- M4 SIM-10: `qa/seccion-en-vivo-m4.mjs` usa biblioteca, copia y controles visibles. Con energía activa, sección 2,5→6 mm² actualizó R de `0,137928` a `0,05747 Ω` sin apagar; Undo y Redo restauraron y reaplicaron tanto documento como física. 8/8, 0 JS, 35,5 s, navegador/servidor cerrados. La primera ejecución real encontró el bug de Undo: diseño 2,5 mm² con simulación todavía en 6 mm²; la corrección añade recálculo del panel al sustituir Proyecto. Esto **no** cierra longitud de ruta→física.
- M3 DOC-02 (identidad, no paquete): `test/documentacion-multivista-m3.test.ts` 1/1. Tres símbolos del mismo KM ocupan dos hojas y una sola fila de índice/BOM; tres conexiones reales conservan extremos y terminales, mientras un cable interhoja produce dos referencias gráficas sin duplicar material. HTML e Ingeniería concuerdan en conteo. DOC-01 sigue abierto por falta de ID/revisión real en HTML/PDF; DOC-02 completo requiere reunir artefactos de una misma revisión.
- Integración en `49c9db4`: typecheck app/core, 1530/1530 pruebas Node en 31,15 s, 0 fail/skip, build QA 409 módulos en 7,81 s. Los QA largos M9 y aceptación humana no se repitieron; la evidencia anterior queda en su SHA.

## Evidencia focal nueva — 2026-09-23, ESQ-02/DOC-01

- `2c9c433`: `estadoRutaFisica: 'pendiente'` pertenece a la conexión eléctrica V2, no a una ruta renderizada. El cargador V1 conserva el legacy, el V2 mantiene el discriminante tras guardar/cargar y los estados desconocidos/contradictorios se diagnostican. Tests de ruta, física, DRC, puerta, ficha e Ingeniería focales 24/24 junto con procedencia; el filtro de Ingeniería impide que un snapshot físico ajeno aporte 999 m a una conexión pendiente.
- `02dfd2c`: importación visible del fixture V2, un cable físico y una conexión pendiente. Inspector/lista/resumen no muestran longitud u ocupación inventadas; cambiar sección prevista no materializa cable. Reapertura conserva estado y sección. `qa/ruta-pendiente-m2.mjs`: 9/9, 0 fallos, 0 errores JS, 9,9 s; servidor y navegador cerrados.
- `b57fe47`: HTML, dossier PDF y esquema PDF incluyen procedencia confirmada de documento y Build ID, distinguen revisión editorial y muestran ejemplo efímero sin identidad falsa. El PDF deriva numeración sobre copia; el primer preview no cambia bytes del proyecto ni revisión. Descarga y preview comparten SHA exacto; edición exige nueva vista y segundo clic. `qa/procedencia-documental-m3.mjs`: 10/10, 0 fallos, 0 errores JS, 83 s; servidor/Chromium cerrados. Tests focales de procedencia y documento pasaron.
- Tras los cortes combinados: TypeScript app/core verdes; `node --test --test-reporter=dot dist/test/*.test.js` terminó exit 0; build QA 410 módulos verde. Este resultado no reemplaza campaña histórica de navegador M9, ni inspección humana/GPU física.
- Límites explícitos: todavía no se puede crear la conexión pendiente desde el esquema; DOC-01 no se extiende aún a SVG/DXF/CSV ni compone un paquete único de revisión. El master spec sigue EN_CURSO y `FUNCTIONAL_COMPLETE` no se declara.

## Evidencia focal posterior — 2026-09-24, ESQ-02 visible y R1

- La frase anterior «todavía no se puede crear» corresponde al checkpoint `b57fe47`; el corte `8ebbcc5` añade ahora el gesto visible. En el esquema opt-in, un pin accesible inicia la conexión, el origen puede cruzar hojas y la confirmación crea exactamente un `Conductor` pendiente en el Proyecto común. Cancelar, duplicado y solo lectura no mutan; desconectar, Undo/Redo y recargar preservan la identidad. `qa/esquema-conexion-pendiente-m2.mjs` recorrió estas fronteras en navegador: **19/19**, 0 errores JS, 90,6 s. El test focal de propuesta rechaza anclas ambiguas sin elegirlas por orden de array. No se acreditan aún ESQ-03, renumeración ni M2 completo.
- `4bb190c` prefiltra sólidos por la envolvente XY ampliada de la ruta antes de la prueba exacta de suelo/colisión; no cambia orden ni comparación final. `qa/rejilla-equivalencia-r1.mjs` mantuvo **5/5** hashes exactos. Las parejas Node del mismo proceso, 6153→5010 ms y 6241→4805 ms; R1-drag 5280→4410 ms, son exploratorias y no reemplazan el p95 de navegador ni la medición en GPU física. **R1 NO ACEPTADO.**
- Batería integrada sobre estos cortes: app/core TypeScript verde, **1551/1551** tests Node en **28,8 s**, 0 fallos/omitidos, build QA **410 módulos** en aproximadamente **6,8 s**. Estas pruebas no equivalen al gate histórico M9. DOC-01 sigue parcial fuera de HTML/dossier/esquema PDF; M4 no recibió política de longitud de ruta en este corte.
- Regresiones de navegador posteriores al corte M0/M2: `qa/cables-fusion.mjs` **verde** en 189,5 s, 0 fusiones 3D, 59/59 puntos frontales, 186/186 clics correctos; `qa/activar-representaciones-m2.mjs` **verde** en 41,3 s después de acotar sus selectores a `.simbolo`, sin relajar condiciones; `qa/esquema-desconexion-m2.mjs` **verde** en 111,1 s; `qa/fixture-puerta.mjs` **verde** en 19,6 s con cierre/apertura y 0 fantasmas. Las cuatro reportaron 0 errores JS. `git ls-files --others --exclude-standard` no mostró artefactos. La inspección de líneas de comando de procesos Windows se denegó por permisos; los procesos QA propios terminaron con exit 0 y el harness cerró servidor/navegador.

## Evidencia focal posterior — 2026-09-24, procedencia extendida y cruce no unido

- El estado anterior de DOC-01 limitado a HTML/PDF pertenece al checkpoint previo. El código validado quedó en `e0cbb3a`, `557150d`, `a717a7a` y `d65a1e3`: la procedencia confirmada/efímera se propaga a esquema SVG/DXF, placa DXF, rótulos PDF, Ingeniería V7 JSON/HTML/cuatro CSV y Diseño V9 exportado, sin reemplazar sus snapshots o límites técnicos. La UI confirma la revisión antes de exportar; un ejemplo no recibe identidad durable falsa. `qa/procedencia-documental-m3.mjs` pasó **16/16** en **89,207 s** en la repetición final, 0 errores JS; `qa/esquema-dossier.mjs` recorrió la edición real de datos del proyecto, preview obsoleto, guardado y PDF con **28/28** en **134,8 s** antes del ajuste gráfico en T, 0 errores JS. `qa/datos-proyecto.mjs` quedó verde incluyendo el campo IP vacío y el rechazo visible de un IP mal formado. Estos resultados no acreditan por sí solos inspección humana de impresión ni el paquete DOC-02.
- Cruces ESQ-03: `crucesSinUnion` deriva intersecciones puntuales interiores y en T; `tramosVisiblesDeHilo` recorta el conductor que debe separarse y el nudo solo aparece cuando varios hilos comparten un borne real. SVG, PDF y DXF dibujan el hueco coherente; el área seleccionable SVG coincide con la tinta visible. La regresión incluye conductor independiente sobre la coordenada de nudo real ajeno, incluso extremo colineal: se recorta para no sugerir unión. Pruebas focales de cruces **20/20**; `qa/esquema-desconexion-m2.mjs` pasó con exit 0 después del ajuste en T, con guardado/reapertura y sin errores JavaScript. La inspección visual automatizada de SVG/PDF verificó geometría y legibilidad antes de la T; no sustituye aceptación humana. Solapes colineales de longitud positiva, nudo DXF relleno, renumeración y flujos multihoja completos siguen pendientes.
- App/core TypeScript verdes; batería Node integrada posterior a T **1584/1584** en **30,949 s**, 0 fallos/omitidos; build QA **411 módulos** en **6,45 s**. `qa/ingenieria-documentacion.mjs` **12/12**, 0 errores JavaScript, **35,1 s**; su arnés ignora solo el 404 del favicon y no silencia otros errores. No se atribuye esta evidencia a CI ni al gate histórico M9. M0/R1, M1, M2 y M3 siguen EN_CURSO; `FUNCTIONAL_COMPLETE` NO declarado. DOC-02 aún necesita una política canónica de BOM: `src/motores/documentacion.ts` agrupa por fabricante/referencia/descripcion y `src/ingenieria/documentacion.ts` también distingue tipo/perfil/modelo físico; no asumir equivalencia de cantidades sin reconciliarla.

## Incremento DOC-02 de 2026-09-24 — evidencia y límites

- BOM canónica `c5b5fb7`: 7 regresiones nuevas de identidad comercial/técnica/propia, variantes nominales, orden y ausencia de referencia; los consumidores históricos de dossier e Ingeniería conservaron sus contratos públicos. La batería combinada antes del ensamblador pasó **1603/1603**; posterior al ensamblador pasó **1604/1604**, 0 fallos/omitidas.
- ZIP/manifiesto `8f72137`: 6 tests de rutas seguras, duplicados, CRC, reproducibilidad y SHA-256; lector Python `zipfile` verificó lectura y CRC de un ZIP real. Lista IO `2ed20a3`: 6 pruebas de PLC nativo/propio, bornes conectados/desconectados, orden, perfiles legacy/hostiles y no duplicación de vistas. Ensamblador: 1 prueba focal con texto hostil, conexión interhoja, aparato multivista y DRC no aprobado.
- App/core TypeScript verdes; build QA **417 módulos** en **6,24 s**. Los cortes `ef3683e` (ensamblador/UI) y `89c38bc` (QA) corresponden al código probado. `qa/ingenieria-documentacion.mjs` descargó desde la UI el paquete ZIP de un ejemplo efímero y comprobó firma ZIP, manifiesto y lista IO: **13/13**, **24,8 s**, 0 errores JS, exit 0 y cierre de navegador/servidor. Esta sonda no sustituye una prueba de importación/extracción offline en otra sesión ni la campaña M9/CI.
- Límite DOC-02: los conductores individuales del paquete no se suman como manguera; aún falta un caso explícito de manguera 3×/4× en aceptación. El vínculo entre igual SKU y variantes nominales aparece como filas separadas, sin motivo visible en tablas heredadas. DOC-03…09, aceptación humana y `FUNCTIONAL_COMPLETE` permanecen abiertos.

## Incremento DOC-02 posterior — paquete offline, obsolescencia y explicación de BOM

- `45de890`: los mismos campos nominales que forman la clave de variante alimentan un resumen legible en ambos consumidores BOM. Un SKU 16 A / 20 A conserva dos partidas y los CSV/HTML/PDF explican la separación. Test de orden inverso y JSON roundtrip; página 6/10 del dossier A4 sintético inspeccionada con render Poppler, sin recortes en ambas tablas. El aviso de autoTable de 14 unidades procede de otra tabla preexistente del dossier (`construirDossier`, anterior al BOM), no de la nueva tabla de variantes; tratarlo en DOC-06, no ocultarlo.
- `76190f1`: test del contrato 3×/4× de obra frente a siete conductores de Proyecto. No se agregan metros de manguera al ZIP sin entidad persistente; el alcance de la lista dice explícitamente que son conductores individuales. Importar las tiradas de cubierta globales al ZIP contaminaría identidad A/B. La lista de compra de manguera asociada a un Proyecto está NO MODELADA.
- Build QA 417 módulos/8,29 s; TypeScript app/core; `node --test` 1606/1606, 0 fallos y 0 omitidos, 25,845 s. Navegador del mismo código: Ingeniería/documentación 13/13/34,4 s, offline 17/17/25,9 s, obsolescencia A/B 6/6/60,4 s, esquema/dossier exit 0; 0 errores JS reportados y cada arnés cerró Chromium/servidor. El primer run Ingeniería fue rojo por una regex que exigía la cabecera CSV antigua; el producto ya emitía `Variante declarada`. La aserción se actualizó para exigir esa columna y un valor, no para relajar el contrato.
- DOC-02 tiene ahora evidencia focal de coherencia, portabilidad y stale rejection, no una aprobación humana ni cierre M3. DOC-03 requiere baseline de revisión emitida: las fotografías de recuperación son podables y no equivalen a una entrega.

## Incremento M3 DOC-03…09 — 2026-09-24, evidencia focal (no aceptación del hito)

- DOC-03: `12c602a` archiva fotografías de diseño y hashes de emisiones verificables fuera del ciclo de poda de autosave; `0550b19` compara aparatos, conexiones, secciones, protección, ficha técnica, ruta y criterio sin inferir geometría AUTO; `1650d88` integra descarga, selección local del ZIP, declaración explícita de entrega, consulta tras eliminar el tablero y comparación en UI. La repetición integrada de `qa/revision-documental-m3.mjs` pasó **20/20** en **127,1 s**, sin errores JS, con reapertura/aislamiento A/B y consulta del archivo tras borrar A. No prueba recepción del destinatario ni conserva los bytes del ZIP.
- DOC-04/05: `f9c3a17` prepara marcadores de aparatos y ambos extremos de cada hilo; `acaee28` amplía bornes a terminales de aparatos, con columna `BORNERA`/`APARATO` sin duplicar vistas ni hilos. `7c451b0` y `3ca4591` separan ruta ortogonal 2D, reserva, puntas, redondeo, longitud eléctrica manual y propuesta estimada de corte en la misma revisión. El corte verificado permanece vacío. Focales de marcadores **7/7** y desglose de longitudes **4/4**, con orden de arrays y roundtrip.
- DOC-06/07/09: `43b749d` documenta y prueba escape CSV (focal **27/27**); `1f52d16` corrige paginación del dossier, anchuras de tablas sin avisos autoTable y frases que insinuaban aprobación; `87d7477` divide BOM HTML en dos tablas vinculadas por partida, usa estados legibles y cabeceras repetidas; `1a89791` exige que un borrador con fallo DRC se emita con el error visible. El dossier PDF sintético de 14 páginas se renderizó y se inspeccionaron ficha, continuación, térmico y DRC sin clipping; Poppler informó ausencia local de fuentes `Symbol`/`ArialUnicode` en el visor, no fallo de generación. El HTML se imprimió con Chromium A4 para fixture de puerta (**3 páginas**) y estrella-triángulo (**9 páginas**): 0 caracteres fuera del margen de 14 mm, filas íntegras, cabeceras repetidas y BOM legible. Queda espacio en blanco de paginación en páginas finales; revisión humana del impreso aún pendiente.
- DOC-08 y texto hostil PDF: `c96ddb1` añade CSP defensiva a Ingeniería y pruebas de texto hostil, recursos locales, no ejecución HTML/SVG y rechazo de informe HTML como orden de edición (**2/2**). `88aaca6` comprueba en PDF real acentos, comillas, saltos y marcadores hostiles como operadores de texto, sin acciones ejecutables (**1/1**); la inspección pypdf/Poppler corroboró legibilidad. `6925081` pide permiso visible para logo/imágenes del dossier antes de preparar el ZIP: repetición integrada del QA navegador **8/8** en **23,0 s**, cancelación sin descarga ni archivo y aceptación con PDF que incorpora la imagen; 0 JS. El ZIP no adjunta assets crudos. El PDF WinAnsi no garantiza fidelidad de Unicode arbitrario.
- Validación integrada sobre el código de `d086297`: typechecks app/core y Node **1640/1640** en **40,18 s** totales, 0 fallos, 0 skipped; build QA **421 módulos** en **7,46 s**. Dos aserciones DOC-02 antiguas esperaban la estructura/frase anterior del HTML/CSV; `d086297` las acotó a la tabla de conexiones y a la afirmación explícita de no sumar manguera, conservando el contrato. `pnpm test` del entorno intentó reinstalar dependencias desde npm con red restringida y movió enlaces a `node_modules/.ignored`; se detuvo, se restauraron solo esos paquetes locales y se ejecutó el equivalente exacto del script con `tsc` app/core y Node. El árbol Git versionado no fue afectado.
- Navegador sobre esa build: paquete offline **17/17** en **10,3 s** (0 HTTP/JS), Ingeniería **13/13** en **37,9 s**, ZIP obsoleto A/B **6/6** en **58,1 s**, procedencia **16/16** en **90,4 s**, todos con 0 errores JS. El dossier editable pasó su recorrido completo tras indicar `PYTHON` del runtime local: preview, formato, imágenes 2D/3D, descarga, texto extraído del PDF y reapertura, sin JS. El primer intento de esa suite llegó a la extracción y falló por `spawnSync python EPERM` del alias del entorno, no por una aserción; no se ocultó. Tras los QA no quedaron procesos Chromium/Node del runtime de pruebas. La campaña histórica M9 y revisión humana del impreso no se ejecutaron/aceptaron en este corte. M3 sigue EN_CURSO; `FUNCTIONAL_COMPLETE` no se declara.

## Selección por impacto

- M1: modelo/persistencia/componentes/assets y QA multiproyecto/componentes, más entrega offline al tocar portabilidad.
- M2: esquema, IDs, bornes, conectividad, simulación y QA esquema/dossier.
- M3: documentos, Ingeniería, impresión/PDF, entrega y texto hostil.
- M4: motores de simulación, física, PLC e Ingeniería; QA industrial/automatización/física/equipos.
- M5–M6: geometría, picking, edición, canaletas, puerta, persistencia, longitudes, Ingeniería/V9 y QA visual/fusión.
- M7: mundo/planta, fixtures CAD sintéticos, seguridad de imports y prueba privada local separada.
- M8–M9: recorridos integrados, accesibilidad/teclado, offline, CI, artifacts y revisión visual/humana pendiente.

Si un cambio cruza identidad, conectividad, anclajes, longitudes o el resolver técnico, ampliar la prueba en esa frontera; no limitarse a la suite del archivo editado.

## ESQ-04 — borrado de vista frente a aparato eléctrico

- `test/eliminar-dispositivos-m2.test.ts`: 7/7 focales para plan sin mutación, dependencias multivista/multihoja, datos técnicos y ajenos, ausencia legacy, proyecto cambiado durante confirmación, solo lectura, selección múltiple/orden inverso y reapertura.
- `qa/esquema-eliminacion-m2.mjs`: 12/12 por interacción visible y almacenamiento real, 28,5 s, 0 fallos y 0 errores JS. Comprueba «Borrar vista» sin perder aparato, «Eliminar aparato» con preview de tres conexiones/dos hojas/datos, cancelar sin historial, eliminación de dependencias, Undo/Redo y recarga sin huérfanos.
- TypeScript app/core verde; Node integrado 1647/1647, 0 fallos/omitidos, 94,6 s bajo concurrencia con navegador. Build QA 422 módulos/8,03 s. QA M2 previos afectados: desconexión exit 0 (6 hitos, ~103 s) y activación de vistas exit 0 (5 hitos, ~40 s), ambos 0 errores JS y cierre de Chromium/servidor. No atribuir a este SHA la campaña histórica completa, CI ni aceptación humana de M2.

## ESQ-08 — renumeración explícita y salida documental coherente

- `test/renumeracion-esquema-m2.test.ts` 6/6: preview puro, bobina multivista como ancla, reservas textuales congeladas, orden inverso, colisiones, rechazo de plan alterado/proyecto cambiado/ejemplo, referencias por ID y guardar/cargar. `qa/esquema-renumeracion-m2.mjs` 11/11 en 27,2 s, 0 JS: botón visible, propuesta/cancelación, aplicación, Undo/Redo, reapertura y dos congelados duplicados que no aplican.
- Hallazgo real: PDF y paquete DOC-02 llamaban `revisarTablero(...renumerarAparatos:true)`, por lo que un congelado `-K2` con `numero=99` podía elevar otro aparato a `-K100` en el papel aunque el proyecto mostrase `-K1`. `test/renumeracion-documental-m2.test.ts` 1/1 exige PDF real, SVG ambas hojas, dossier HTML, Ingeniería JSON/BOM CSV, aparatos, marcadores y referencias con el texto persistido; valida antes/después de aplicar sin mutación y conserva el apóstrofo protector CSV DOC-07.
- TypeScript app/core verde; Node integrado **1654/1654**, 0 fallos/omitidos, 26,13 s. Build QA **423 módulos**/7,23 s. Browser tras el cambio: Ingeniería/documentación **13/13**, 36,5 s, y ZIP offline **17/17**, 17,6 s; ambos 0 JS. Primer intento de Ingeniería no lanzó Chromium por ruta predeterminada de Playwright no instalada; repetición con `PW_CHROME` local completó sin aserciones rojas. No se ejecutó campaña M9/CI del candidato, ni se aceptan todavía referencias cruzadas PLC/circuito, cruces colineales o el hito M2 completo.

## ESQ-03/08 — frontera multihoja, solapes y referencias ancladas

- `test/cruces-esquema-solapes.test.ts` y sus consumidores cubren solape colineal positivo parcial/total entre hilos eléctricos independientes. SVG/PDF/DXF conservan la línea y añaden `SOLAPE SIN RESOLVER`, sin mutar topología. El hit-target SVG no afirma que el tramo ambiguo pertenezca a uno solo; el elemento permanece accesible por teclado. No existe aún desvío manual validado ni solución universal segura; ESQ-03 sigue PARCIAL. Una salida automática lateral en solape parcial puede fabricar dos contactos en T y nuevos cruces, por lo que no se afirma que una simple capa/offset resuelva el caso.
- `qa/esquema-aceptacion-m2.mjs` **18/18** en **316 s** aislado, 0 errores JavaScript y cierre de servidor/Chromium. Comprueba dos hojas y un KM único con bobina/polos/aux, navegación, conexión/desconexión por UI, ruta pendiente sin longitud ni malla 3D, simulación que responde al circuito, borrado/restauración de vista y Undo/Redo. En `w24`, selección del solape total usó Enter sobre grupo SVG accesible; el ratón no tiene punto exclusivo en esa región. Otra corrida simultánea con 1671 tests Node duró 401,2 s por contención y no se presenta como rendimiento normal.
- `test/esquema-aparatos-sin-vista-m2.test.ts`, `test/esquema-pendientes-export-m2.test.ts` y `test/reponer-representacion-esquema-m2.test.ts` exigen aviso de vista/conexión pendiente y reposición por ID sin otro aparato. El PDF de QA se renderizó con Poppler y se corrigió separación vertical de avisos/nota IEC; los temporales no quedaron en Git. La reposición exige casilla válida y revalida el mismo documento tras diálogo.
- `test/referencias-esquema-m2.test.ts` **6/6**: PLC nativo con semántica explícita frente a rótulo engañoso, canal/terminal en hojas distintas, trayecto de alimentación identificado, reordenación de arrays, guardar/cargar, anclaje ausente/duplicado y topología ambigua. `qa/referencias-esquema-m2.mjs` **11/11** en **11,0 s**, 0 JS: índice desplegable, navegación por IDs estables y diagnóstico visible; no invoca edición por hook privado. `test/paquete-referencias-esquema-m2.test.ts` **2/2**: CSV de PLC/terminal y circuito/hoja de la misma revisión, orden de arrays estable, diagnóstico en vez de asignación arbitraria. El conjunto documental focal con regresiones previas fue **9/9**. El alcance del circuito es `TRAYECTOS_ALIMENTACION_IDENTIFICADOS`, no retorno exhaustivo.
- La batería Node integrada **1671/1671**, 0 fallos/omitidas y 29,18 s, además de TypeScript y build QA 423 módulos, se ejecutó **antes** de los cambios finales UI/CSV. Después, el core TypeScript y el subconjunto documental focal 9/9 pasaron; falta repetir la integración completa, el ZIP offline y la campaña M9 sobre candidato final. No mezclar evidencias de SHAs distintos.

## ESQ-09 y salida de revisión — checkpoint respaldado en `5f4ed5b`

- `qa/esquema-navegacion-m2.mjs` **8/8**, 0 errores JS, 28,9 s: desde la misma identidad persistente navega entre esquema, tablero, Datos Técnicos e Issue Center; cuando hay varias vistas exige elección y no adivina por designación. Corrige además una ficha técnica cuya revisión exacta faltaba: muestra ID/hash NO DISPONIBLE y no sustituye silenciosamente por el primer aparato compatible.
- El pie de SVG/PDF declara simbología genérica no certificada; el texto anterior sugería IEC 60617/61439 sin acreditar biblioteca/licencia ni conjunto verificado. El cambio de texto pasó 7/7 pruebas focales. ESQ-06 sigue PARCIAL: no equivale a una biblioteca profesional certificada.
- El ZIP de revisión ampliado con referencias PLC↔terminal, circuitos↔hojas y diagnósticos se abrió desde la UI, verificó entradas CRC/SHA-256 y abrió índice/SVG/dossier/Ingeniería desde `file://` sin solicitudes HTTP(S): `qa/paquete-documental-offline.mjs` **17/17**, 0 JS, 20,0 s tras este corte. Esto no acredita inspección humana del impreso ni campaña M9.

## ESQ-10 — resultado eléctrico en el plano, sin segundo estado

- `769333c` integra un proyector puro sobre `ResultadoSimulacion`: distingue diseño, simulación sin snapshot, resultado inestable y resultado válido; solo este último puede colorear hilos/representaciones. Las referencias interhoja usan el mismo ID de conductor y no crean otro circuito. 6/6 pruebas focales, 0 fallos.
- `qa/esquema-estado-m2.mjs` **21/21**, 0 errores JS, **115,9 s** con Chrome: START/STOP desde controles visibles, cotejo de hilos y aparatos con el resultado vigente, descarga SVG libre de atributos efímeros y Proyecto inmutable. Reemplazar un arranque directo en marcha por el ejemplo de bomba con IDs `km1`/`m1` repetidos reinicia la maniobra y muestra solo el nuevo resultado.
- App/core TypeScript verdes y build QA **425 módulos** en **6,88 s** antes de la regresión final. La captura temporal del plano energizado mostró capa ámbar/encabezado legibles; también mostró distribución legacy concentrada a la izquierda y solapes advertidos. No sustituye aprobación humana. `ResultadoSimulacion` no porta ID/revisión, de modo que la frescura depende del reinicio/recalculo del panel y de leer el snapshot vigente en cada rAF; falta endurecer el contrato de identidad si otros consumidores desacoplados lo utilizan.

## ESQ-07 — folios M2 explícitos, checkpoint de rama respaldado en `c505d2a`

- `cbc28be` introduce un plan puro de crear/editar/mover/eliminar hojas por ID, con previsualización, bloqueo de hoja ocupada y revalidación al aplicar; `3f615ff` distingue columnas explícitas de heredadas y permite restablecer la herencia sin dejar vistas fuera. `35d2bb9` evita pérdida silenciosa de vistas al cargar un documento M2 con IDs/números de folio inválidos o duplicados; los datos editoriales reparables generan diagnóstico y revisión. Las pruebas focales del motor alcanzaron 29/29 y las del cargador 115/115, en sus respectivas fronteras, sin reemplazar la batería integrada final.
- `qa/esquema-folios-m2.mjs` pasó **22/22**, 0 errores JavaScript, **174,6 s** con Chrome tras `c505d2a`: ocultación en legacy, alta/cancelación sin mutación, selección por ID y teclado con foco conservado, reordenación, edición/clase/columnas, un Undo/Redo, rechazo de reducción fuera de rejilla y borrado de hoja poblada, guardado/reapertura, herencia restaurada y borrado confirmado de hoja vacía. El arnés cerró página, Chromium y servidor en `finally`; la inspección global de procesos del sistema no pudo completarse por acceso denegado.
- TypeScript de app y build QA **426 módulos** en **6,80 s** verdes sobre la interfaz de ese checkpoint. La captura temporal a 1600×920 mostró el registro y controles legibles; el panel ocupa hasta 35vh y el ID largo no bloquea, pero no acredita revisión humana. En ese corte aún faltaba propagar clase/título a los entregables (abordado abajo); tamaño de página por folio y campaña M9 siguen pendientes. **ESQ-07 y M2 no están aceptados**.

## ESQ-07 — metadatos del folio en los entregables

- `c7008c6` lleva clase opcional solo del folio M2 al montaje; el cajetín SVG/PDF muestra proyecto, título, clase editorial y número sin inferir clase legacy. `cc0dddf` rotula la hoja DXF R12 e indexa por número/ID/título/clase los SVG del paquete. Una clase editorial no certifica función eléctrica ni cumplimiento normativo.
- Pruebas focales de cajetín y paquete **5/5**: persistencia M2, legacy sin clase inventada, escape SVG de título hostil, DXF sin inyección de grupos, índice HTML con escape y orden independiente de arrays. App/core TypeScript y build QA **427 módulos** verdes sobre el árbol combinado de este corte. PDF A3 y SVG fueron renderizados e inspeccionados: las dos líneas del cajetín y la procedencia permanecen legibles. La fuente jsPDF WinAnsi no garantiza glifos Unicode arbitrarios.
- La primera pasada de `qa/paquete-documental-offline.mjs` detectó dos enlaces al mismo SVG, contradiciendo el contrato de una entrada por archivo; **no se silenció**. `fe92b51` conserva el enlace con título en la tabla de folios y omite ese duplicado de la lista genérica. Repetición: **17/17**, 0 errores JavaScript, 0 peticiones HTTP(S), **10,1 s**, CRC/SHA-256 y `file://` verdes. La campaña integrada M9 y revisión humana del impreso siguen pendientes; ESQ-07 continúa parcial por tamaño de página por folio.

## ESQ-05 — traslado de una vista M2 sin alterar el circuito

- `test/mover-representacion-esquema.test.ts` comprueba el plan puro en cuatro casos: posición válida, no-op, casilla ocupada/límites, IDs ambiguos y plan envejecido —incluido un cambio de orden del array durante la confirmación—. La aplicación reemplaza solo la representación identificada; preserva las referencias de aparatos/conductores y no crea un elemento eléctrico nuevo. El guardado/reapertura del traslado se comprueba en el QA de navegador, no en este test unitario.
- `qa/esquema-mover-vistas-m2.mjs` recorrió la UI real con Chrome: **18/18**, **90,4 s**, 0 errores JavaScript y exit 0 sobre el corte `2e8d496`. La prueba observó el mismo nodo SVG durante `pointermove`, grafo inalterado antes de `pointerup`, una sola captura Undo al soltar en casilla libre, rechazos sin Undo para ocupada/fuera de rejilla, clic solo selector, Escape y `pointercancel`; después ejercitó Undo/Redo, traslado al folio 2 y persistencia tras recarga. Cerró página/Chromium/servidor en 376/143/0 ms respectivamente; no mide procesos ajenos al arnés.
- App TypeScript y build QA **427 módulos** quedaron verdes tras integrar el gesto. La primera batería Node **1705/1705**, 0 fallos/omitidos, **18,72 s**, fue previa al cambio final de UI; posteriormente se repitió la batería completa sobre el árbol con UI y ajuste QA: **1705/1705**, 0 fallos/omitidos, **18,53 s**, exit 0. La campaña M9 requiere todavía atribución al candidato final; el 18/18 de ESQ-05 es evidencia focal, no aceptación del hito. Alineación/grupo/copia/giro y el formato físico por folio siguen pendientes.
- Las suites M2 afectadas por la adición de bornes SVG seleccionables se repitieron en el checkpoint posterior a `2e8d496`: `qa/esquema-representaciones-m2.mjs` exit 0, **6 hitos** impresos, 0 JS, **19,3 s**; `qa/esquema-folios-m2.mjs` **22/22**, 0 JS, **139,4 s**; `qa/esquema-estado-m2.mjs` **21/21**, 0 JS, **92,0 s**. La primera falló inicialmente porque un selector `[data-representacion]` contaba tanto `.simbolo` como sus bornes hijos; `0eea749` lo acotó a `.simbolo[data-representacion]` sin cambiar contrato de producto ni cantidad de hitos. Estos resultados no acreditan por sí solos la campaña M9 final ni se atribuyen al SHA previo `2e8d496`.
