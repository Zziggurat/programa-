# Campaña funcional hacia 1.0 — estado vigente

## Referencia y alcance

- Contrato: `docs/avance/TABLEROSTUDIO_MASTER_SPEC.md` (versión 1.0, 2026-09-22). El original aportado tenía SHA-256 `350ce427bcd0e4989a424def5334f664edeee90808a20a068def0bfe6e13bea8`.
- Baseline verificado: `main`, `origin/main` y `tablerostudio-v9` en `4c2924c4d12f4dc0982a7f0bf28d94e52b00366f`. Repositorio `Zziggurat/programa-`.
- Rama de desarrollo: `roadmap/1.0-functional`. `main` sigue como referencia estable. No publicar una versión 1.0 al terminar esta campaña.
- Estado terminal buscado: `FUNCTIONAL_COMPLETE`, antes de los encargos separados de arquitectura/datos y Project Breaker.

## Hitos

| Hito | Estado | Frontera actual |
|---|---|---|
| M0 · preparación | EN_CURSO | R1 30/100 conserva 5/5 hashes exactos. Mover aparatos y editar uniones de cable recalcula rutas en Worker inline con estado visible, invalidación y cancelación por Undo, también en HTML offline. En NVIDIA RTX 5070 Ti/D3D11: cinco drags dieron gesto host p95 378,5 ms (antes 3817,3), listener `pointerup` p95 37,4 ms y render p95 16,3 ms, sin tareas largas. Selección host p95 154 ms pero listener p95 46,3 ms; las rutas completas aún tardan p95 4523,6 ms. **R1 NO ACEPTADO** hasta verificar experiencia y coste residual. |
| M1 · flujo y componentes | EN_CURSO | Carcasa opcional seleccionable desde el asistente, recorte/escala visibles, ficha exacta, bornes/bloques, adopción A/B y portabilidad. Contrato integrado contactor A/B+PNG r2+paquete limpio probado; QA asistente 19/19, adopción 24/24, carcasa 15/15 y recorrido largo contactor+simulación+importación verde. Falta aceptación visual/humana y campaña M9; no se declara M1 aceptado. |
| M2 · esquema | EN_CURSO | Vista M2 opt-in sobre la misma identidad eléctrica; folios A3/A2, referencias por ID, renumeración con preview, navegación bidireccional, estado de simulación derivado y cruces puntuales sin unión. ESQ-05 permite mover, alinear, localizar, pan/zoom y girar 180°; copiar una vista completa o un aparato desdoblado crea otra identidad y todas sus vistas sin conductores, con un Undo y folios preservados. ESQ-06 añade dibujo vectorial personal opt-in con procedencia declarada, revisión y portabilidad, sin certificar derechos ni IEC. El recorrido editorial por corredores despeja cuatro ejemplos principales; la UMA aún tiene dos solapes visibles, sin confundirlos con unión ni cable físico. Quedan control manual para casos densos, legibilidad impresa, revisión humana/licencias y edición cotidiana completa; giro 90° solo si se justifica geométricamente. M2 NO aceptado. |
| M3 · documentación | EN_CURSO | DOC-01/02 conservan procedencia y ZIP único verificable. DOC-03 archiva emisiones declaradas y compara revisiones; DOC-04 cubre aparatos, bornes y extremos; DOC-05 separa longitud eléctrica, ruta 2D y corte propuesto; DOC-06/07 mejoran impresión y CSV; DOC-08 pide permiso visible para imágenes del PDF y prueba portabilidad offline; DOC-09 evita aprobación implícita y emite borradores con fallos. Son incrementos focales, no aceptación M3: faltan revisión humana del impreso y campaña integrada sobre el último SHA. La manguera de Proyecto sigue NO MODELADA. |
| M4 · simulación práctica | EN_CURSO | SIM-03: fallo visible de boya, caída de KM y bomba parada (QA 9/9). SIM-10 sección declarada: cambio visible 2,5→6 mm² refresca física energizada y Undo/Redo, QA 8/8 sin JS. Ruta→física espera política de longitud M6; no aceptar SIM-10 completo ni los demás SIM por arrastre. |
| M5 · montaje/3D | EN_CURSO | MON-01/02 parcial: rieles y aparatos se editan en mm con un solo Undo, anclaje DIN explícito y rechazo atómico. QA visible de montaje 27/27 en su último corte; componente propio atornillado 33/33. Un riel ocupado no gira silenciosamente. MON-04 reúne placa, puerta y campo en lista filtrada con foco/teclado, ocultación/aislamiento y bloqueo de edición efímeros por identidad; QA focal 32/32, 0 JS. Faltan gizmos/ejes, snap explícito, giro real, MON-05…07, revisión visual de la biblioteca y aceptación humana. |
| M6 · cables | EN_CURSO | Ruta XYZ manual opt-in, literal y versionada; adopción legacy con Z, edición numérica, Undo y persistencia. Preview y resultado comparten vértices; sólidos, plástico de canaleta y proximidad 3D producen avisos locales sin expulsión. Planes automáticos V4 fijan rutas y CAB-23 ofrece alternativas focales sin mover vecinos. Referencia geométrica, longitud eléctrica y corte siguen separados. Faltan marcos móviles, curva/radios, canaleta densa/capacidad, aceptación integral de localidad/planes, rendimiento denso y política de longitud; M6 NO aceptado. |
| M7 · CAD/mundo | PENDIENTE | DWG privado disponible; licencia/revisión del derivado publicado sin acreditar. |
| M8 · UX integrada | PENDIENTE | Consolidar recorridos, no abrir subsistemas duplicados. |
| M9 · cierre funcional | PENDIENTE | No iniciar hasta completar y verificar los flujos obligatorios. |

## Último checkpoint CAB-23 — propuesta de cable visible, 2026-09-26

- La conexión automática por formulario y por bornes ahora exige revisar la propuesta incluso cuando el diagnóstico no tiene avisos. Un documento borrador contiene el plan; la línea 3D y el SVG frontal muestran sus mismos puntos XYZ. Cancelar no incorpora el conductor ni los planes vecinos y limpia la línea, también al terminar el gesto 3D. La confirmación conserva la comprobación de base antes de una única operación Undo.
- El diálogo identifica extremos, referencia espacial, avisos y límites no verificables; no confunde esa referencia con longitud de corte. En la captura del arranque directo el tablero y el preview permanecen visibles. No se ha hecho inspección humana exhaustiva de ocupación, radio ni fabricación.
- TypeScript app/core, Node **1805/1805**, build QA **443 módulos**, `qa/localidad-planes-m6.mjs` **23/23** (última pasada 142,0 s), `qa/prender-desde-cero.mjs` con 13 cables y `qa/simulacion-industrial.mjs` **123/123** (310,6 s) verdes, 0 errores JavaScript. La QA de localidad se repitió después del último ajuste textual; la industrial y la de conexión desde cero se ejecutaron antes de ese ajuste textual, sobre el mismo comportamiento.
- **CAB-23 parcial:** solo existe una propuesta automática por alta; faltan alternativas físicas seleccionables. CAB-24, M6, M9 y versión 1.0 permanecen sin aceptación integral. El siguiente paso es obtener alternativas del mismo motor de rutas sin cambiar planes aceptados ni presentar desplazamientos cosméticos como opciones físicas.
- Contrato posterior de BASE: la propuesta guarda la foto exacta del documento; aplicación pura rechaza cambios y reaplicación, y el editor veta además un cambio de sesión. App/core TypeScript y Node **1805/1805** verdes; build QA **443 módulos**; localidad visible **23/23** en 132,0 s y montaje desde cero con 13 cables y motor funcional, ambos sin errores JS. No se repitió la QA industrial completa tras esta extracción; no usarla como evidencia del último SHA.
- La revisión de un plan pendiente comparte ahora la misma vista XYZ temporal: cancelar conserva el documento completo, aceptar limpia el overlay y Undo/Redo preservan los vecinos. `qa/localidad-planes-m6.mjs` pasó **26/26** en 146,9 s, 0 JS; app TypeScript, build QA de 443 módulos y Node **1805/1805** verdes. Sigue siendo una única propuesta, no una selección de alternativas.
- Continuación posterior: el mismo router ofrece hasta tres trayectorias separadas para un alta, sin mover planes vecinos; el selector actualiza línea 3D, proyección frontal y perfil lateral, y solo la opción confirmada se guarda. La referencia ahora mide XYZ del plan, no la aproximación frontal legacy. Se enumeran los IDs de planes anteriores que quedarían fijados. Contrato, límites y evidencia en [CAB23_PROPUESTAS.md](CAB23_PROPUESTAS.md). Sobre el último cambio: app/core TypeScript, **1807/1807** Node, build QA 443 módulos y localidad **28/28**, 0 JS. Las QA industrial, desde cero y fusión pasaron inmediatamente antes del último añadido informativo de IDs; no son evidencia del SHA final. **CAB-23 necesita validación física/visual más amplia; M6/M9/1.0 no aceptados.**

## Siguiente acción y recuperación

1. R1: Worker inline evita bloqueos largos en mover aparatos y editar uniones, con Undo/cancelación, pero el reparto global tarda ~4,5 s por gesto. Revisar la experiencia mientras espera, la creación de cables y el hardware de referencia adicional; no confundir latencia host Playwright con listener real ni aceptar R1 por una sola GPU.
2. M2: la copia de aparatos desdoblados conserva bobina, polos y auxiliares por ID entre folios. El router editorial despeja cuatro de cinco ejemplos principales sin penetrar cajas; la UMA mantiene dos solapes advertidos y necesita edición de trazo o una propuesta gráfica segura, nunca una unión ficticia. Verificar ESQ-06 en impresión física y procedencia/licencia de dibujos externos. Giro 90° solo con caja, anclajes y rutas demostrados. La vista de hoja completa sigue haciendo pequeños los rótulos; revisar PDF/SVG en papel real antes de aceptar M2. Preservar vínculos, rutas pendientes, etiquetas y archivo DOC-03.
3. M3: repetir campaña documental sobre el SHA integrado y someter el paquete impreso a revisión humana antes de aceptar el hito. El archivo de emisiones guarda proyecto+hash, no los bytes del ZIP; nunca presentarlo como copia recuperable ni aprobación. Tras M5/M6, reincorporar planos/longitudes físicas desde la misma revisión.
4. Cerrar evidencia M1 de producto y revisión humana de apariencia/montaje; no sustituirla por tests o capturas automatizadas.
5. M4: continuar SIM-01…10; la sección en vivo está cubierta, pero SIM-10 longitud de ruta espera la política de M6.
6. M5: continuar MON-01/02 con giro físico de cuerpos/bornes o acción explícita de reanclaje. El giro de riel ocupado se bloquea, no se presenta como capacidad terminada; los arrastres de cambio de largo tampoco comparten aún el plan puro. Después abordar snap explícito, metrología y biblioteca visual con imágenes reales del render.
7. MON-04: la lista filtrada permite seleccionar/enfocar, ocultar/aislar y bloquear la edición de aparatos de placa, puerta y campo sin alterar circuito, informes ni persistencia. El bloqueo de sesión veta ficha, posición, arrastre, borrado y movimientos indirectos por riel; no bloquea conexiones, que son entidades independientes. Los cables siguen visibles por diseño y la revisión visual humana está pendiente.
8. M6: los planes V4 ya conservan localidad y procedencia en los casos focales, incluso tras guardar/reabrir; CAB-23 ofrece varias rutas sin redistribuirlos. Antes de aceptar el hito falta estrés denso, entrada/capacidad de canaleta, marcos móviles, radios/curvas con geometría común a medición/picking/render y política explícita de longitud eléctrica/corte. No convertir automáticamente ruteo legacy en intención manual.

## Checkpoint de reanudación — 2026-09-23

- Rama: `roadmap/1.0-functional`; `main` y `origin/main` permanecen en V9 `4c2924c`.
- Cortes nuevos y separados: `a955637` (carcasa/portabilidad), `6f04a10` (vistas esquemáticas), `5a98001` (recorte UI), `2ce0141` (edición de vistas). `6f4b222` (reconciliación de mallas) ya estaba respaldado en la rama remota.
- TypeScript app y core: verde en el estado combinado. Pruebas focales combinadas: 25/25, 0 omitidas. QA visible de recorte 8/8 en 128,8 s y QA de vistas 6/6, ambos sin errores JavaScript, con navegador/servidor cerrados. Build QA focal de los cortes: verde.
- **No se ejecutó campaña completa de pruebas ni CI sobre este nuevo HEAD.** Estas comprobaciones focales no equivalen a aceptación M1, M2, M0 ni `FUNCTIONAL_COMPLETE`.

Esta campaña no incluye publicación automática del plano privado, arquitectura global, Project Breaker ni una release 1.0. Ninguna aceptación humana se presume realizada.

## Checkpoint de continuidad — 2026-09-23, M0/M3/M4 focal

- Rama `roadmap/1.0-functional` respaldada en remoto hasta `49c9db4`; `main` continúa en V9. Nuevos commits: `6c7052a` corrige solo el gesto visible del benchmark, `806d8cc` invalida el resultado eléctrico tras cambiar sección y tras Undo/Redo, `49c9db4` prueba identidad documental multivista. No se promocionó a `main`.
- Benchmark R1 focal en el mismo fixture 30/100 (SHA `f860d627…`), Chromium 1243/SwiftShader, n=1: selección host 4,70 s, drag host 24,43 s, seis movimientos 16,76 s, `pointerup` canvas 4,83 s, reparto 4,76 s, 10/98 rutas y 10 mallas modificadas; picking y undo/redo verdes, 0 errores JS. El primer intento rojo fue espera implícita de navegación del arnés tras un clic ya recibido, no aserción funcional; el segundo cerró navegador/servidor. **R1 NO ACEPTADO**: no hay GPU física ni cuantiles representativos.
- SIM-10 sección en vivo: `qa/seccion-en-vivo-m4.mjs` usa ejemplo y copia visibles, cambia 2,5→6 mm² en el inspector con el circuito energizado, comprueba R `0,137928→0,05747 Ω` y Undo/Redo; 8/8, 0 JS, 35,5 s, browser/servidor cerrados. El primer recorrido descubrió que Undo restauraba documento pero no snapshot eléctrico; quedó corregido, no silenciado. El cálculo de longitud a partir de ruta física sigue pendiente.
- M3: `test/documentacion-multivista-m3.test.ts` comprueba tres vistas de un contactor, una identidad/BOM, tres conexiones, dos referencias gráficas del enlace interhoja y exactitud de terminales en Ingeniería; 1/1 focal. No demuestra todavía procedencia uniforme ni paquete documental único.
- App/core TypeScript verdes; batería integrada posterior a esos tres commits: 1530/1530 en 31,15 s, 0 fallos y 0 skipped. Build QA 409 módulos en 7,81 s. El gate histórico completo M9 no se ha ejecutado sobre este HEAD.

## Checkpoint de continuidad — 2026-09-23, frontera M2/M3

- Rama `roadmap/1.0-functional` respaldada en `origin/roadmap/1.0-functional` hasta `b57fe47ddd65d8e4bff985478e2ef874dc165f60`; `origin/main` sigue en V9 `4c2924c`. Commits de este corte: `2c9c433` contrato físico de ruta pendiente, `02dfd2c` presentación y QA de navegador, `b57fe47` procedencia de HTML/PDF. El checkpoint documental previo `4d2731f` también quedó respaldado. No se promovió a `main`.
- ESQ-02: archivo V2 opt-in conserva conectividad eléctrica sin trazado físico, metros, ocupación, material ni malla 3D; ausencia del campo conserva proyectos V1. Un valor hostil o contradicción con trazado/longitud se diagnostica y congela guardado hasta revisión, no se degrada silenciosamente. Pruebas focales de modelo/Ingeniería 24/24; `qa/ruta-pendiente-m2.mjs` por UI de importación/inspector/reapertura 9/9 en 9,9 s, 0 JS. **Todavía no existe el gesto de conectar desde el esquema.**
- DOC-01 focal: HTML, dossier PDF y esquema PDF usan Project ID y revisión del repositorio solo tras `flush` confirmado; ejemplos quedan efímeros. La numeración PDF opera en copia. El dossier descarga exactamente el Blob mostrado; edición, cambio de identidad o revisión invalidan preview. `qa/procedencia-documental-m3.mjs` 10/10 en 83 s, 0 JS, browser/servidor cerrados. SVG/DXF/CSV y paquete documental completo **NO VERIFICADOS** para esta procedencia; DOC-01 sigue EN_CURSO.
- Typechecks app/core y batería Node integrada con exit 0 tras el corte; build QA 410 módulos verde. La campaña histórica M9, GPU física, aceptación visual humana y `FUNCTIONAL_COMPLETE` siguen pendientes.

## Checkpoint de continuidad — 2026-09-23 noche

- Rama `roadmap/1.0-functional`; `main` permanece en V9. Cortes locales nuevos desde `962bcc5`: `9289fb9` carcasa UI, `d59c87f` salida abierta de sensor, `12a79f3` y `d1f2041` rejilla, `dec2567`/`4262b37` aceptación M1, `0712c8e` test visual, `b552b95`/`4072c0d` activación M2 y `7e0bdb1` test de rótulos. Registrar SHA de respaldo remoto tras el push de la rama; no promover a main.
- Typecheck app/core verdes; 1529/1529 pruebas en 25,57 s, 0 fallos/omitidas. Build QA: 409 módulos en 6,46 s. Primera pasada completa roja por test estático obsoleto de serigrafía, corregido sin cambiar producto; la repetición íntegra es la evidencia válida.
- QA M4 boya 9/9 en 29,64 s, 0 errores JS; QA M2 5/5, QA asistente 19/19, adopción 24/24 y carcasa 15/15, 0 errores JS. El recorrido largo `qa/componentes-personalizados.mjs` pasó `TODO OK`: contactos polo/NA/NC, 11 cables, guardado/reapertura, paquete en navegador limpio y equivalencia nativa hasta el contrato que el nativo declara; 0 errores JS. No se ejecutó todavía todo el gate histórico M9 sobre este HEAD.
- `qa/rejilla-equivalencia-r1.mjs`: 5/5 escenarios idénticos (28/23/59/98/98 rutas); R1+drag 4,98 s en Node en este equipo. La mejora de CPU no acredita respuesta de arrastre ni certifica comportamiento en GPU física. No queda ningún proceso QA intencionalmente en ejecución.

## Checkpoint de continuidad — 2026-09-24, gesto M2 y coste de rutas M0

- Rama `roadmap/1.0-functional`, sin promoción a `main`. `4bb190c` acota los sólidos candidatos por envolvente XY de cada ruta antes de la comprobación geométrica exacta; `8ebbcc5` habilita conectar bornes desde el esquema y crea una sola conexión eléctrica con `estadoRutaFisica: 'pendiente'`, sin ruta, malla, longitud ni material supuestos. Las entradas anteriores describen el estado de sus propios SHA y se conservan como evidencia histórica.
- El gesto M2 parte de un borne real visible, conserva el origen entre hojas, exige confirmación, revalida extremos/duplicados/proyecto y permite cancelar; Undo/Redo, persistencia y reapertura mantienen una sola identidad eléctrica. `qa/esquema-conexion-pendiente-m2.mjs`: **19/19**, 0 fallos y 0 errores JS, 90,6 s. Es evidencia focal, no aceptación de ESQ-03 ni del hito M2 completo.
- Tras integrar esos cortes, typechecks app/core verdes; batería Node **1551/1551**, 0 fallos, 0 omitidos, 28,8 s; build QA **410 módulos** en aproximadamente **6,8 s**. No se atribuye a este SHA la campaña histórica completa de navegador ni CI.
- M0 conserva 5/5 salidas exactas del repartidor en los escenarios oficiales. Dos pares exploratorios en el mismo proceso Node: 6153→5010 ms y 6241→4805 ms; R1-drag 5280→4410 ms en otra pareja. No son cuantiles ni prueba en GPU física; **R1 NO ACEPTADO**. M3 DOC-01 sigue parcial para SVG/DXF/CSV y paquete; M4 longitud de ruta→física no se abordó en este corte y espera política persistente de origen/revisión, no medición de la malla renderizada.
- Regresiones de navegador afectadas por el cambio: fusión **verde** en 189,5 s (59/59 puntos frontales, 186/186 clics semánticos, 0 ejes 3D coincidentes); activación/desdoblamiento M2 **verde** en 41,3 s tras corregir únicamente un selector QA ambiguo; desconexión M2 **verde** en 111,1 s; fixture de puerta **verde** en 19,6 s, sin geometrías fantasma. Todas reportaron 0 errores JS. No se ejecutó la campaña M9 completa ni se afirma aceptación visual/GPU física.

## Checkpoint de continuidad — 2026-09-24, DOC-01 ampliado y cruces ESQ-03

- El código de este corte M2/M3 quedó separado en `e0cbb3a` (Ingeniería), `557150d` (harness), `a717a7a` (esquema) y `d65a1e3` (entregables), sin cambios de código después de la validación combinada. No acredita CI ni la campaña histórica de navegador. ESQ-03 distingue cruces puntuales interiores y en T sin conexión de nudos de borne real sin alterar `Proyecto`: el conductor que debe apartarse recibe un hueco en SVG, PDF y DXF y el hit-target SVG usa el mismo tramo visible. En el caso de una coincidencia sobre un nudo ajeno, el hilo independiente también queda separado gráficamente. Solapes colineales independientes, nudo DXF relleno, renumeración y aceptación multihoja siguen abiertos; M2 continúa EN_CURSO.
- DOC-01 reutiliza procedencia confirmada de repositorio o marca efímera explícita después de confirmar revisión/snapshot en la UI. La cobertura focal alcanza esquema SVG/DXF, placa DXF, rótulos PDF, Ingeniería V7 JSON/HTML/cuatro CSV y Diseño V9 exportado, además de HTML/dossier/esquema PDF previos. `qa/procedencia-documental-m3.mjs`: **16/16**, 0 errores JS, **89,207 s** en la repetición sobre el corte final; `qa/esquema-dossier.mjs`: **28/28**, 0 errores JS, **134,8 s** antes del ajuste gráfico en T; `qa/datos-proyecto.mjs` verde, incluyendo IP vacío y rechazo de IP mal formado; `qa/esquema-desconexion-m2.mjs` verde también después del ajuste en T. La revisión visual automatizada de SVG/PDF confirmó huecos de cruce y cajetín legibles, pero no sustituye aceptación humana de impresión.
- TypeScript app/core verdes; batería Node integrada posterior al ajuste de cruces en T **1584/1584** en **30,949 s**, 0 fallos/omitidos; build QA **411 módulos** en **6,45 s**. Es evidencia focal del árbol probado, no de un commit publicado ni del gate M9 completo. DOC-02 sigue pendiente: falta un paquete de revisión única y reconciliar las distintas claves de agrupación BOM de Documentación e Ingeniería. M0/R1, M1, M2, M3 y `FUNCTIONAL_COMPLETE` no se declaran aceptados.

## Checkpoint para reanudar — 2026-09-24 noche, DOC-02 incremental

- Tras el checkpoint anterior, `c5b5fb7` unificó la agrupación BOM de dossier/Ingeniería desde instancias persistentes y `8f72137` añadió ZIP32 portátil con manifiesto SHA-256; `2ed20a3` añadió lista de señales/IO vinculada a bornes y conexiones reales. `ef3683e` integró ensamblador e interfaz; `89c38bc` cubrió la descarga humana en navegador. Trabajar solo en `roadmap/1.0-functional`; `main` sigue siendo V9 estable. No publicar 1.0.
- «Ingeniería → Documentación → Paquete de revisión ZIP» toma una copia tras `flush` confirmado (o marca un ejemplo efímero), deriva esquema SVG/PDF, dossier HTML/PDF, informe Ingeniería JSON/HTML/CSV, listas de aparatos/conexiones/conductores/borneros/referencias/IO e índice HTML sin scripts ni recursos remotos. Un manifiesto verifica tamaños/hash de cada archivo. Si cambia la identidad, contenido o revisión antes de descargar, se rechaza el resultado. Las decisiones V9 persistidas se rotulan como históricas, no como una evaluación vigente.
- Verificación tras integrar: TypeScript app/core verde; **1604/1604** pruebas Node, 0 fallos/omitidas, **19,558 s**; build QA **417 módulos** en **6,24 s**; `qa/ingenieria-documentacion.mjs` **13/13** en **24,8 s**, 0 errores JavaScript, con descarga real del ZIP desde UI. ZIP abierto además por Python `zipfile` con CRC válido. No se ejecutó el gate histórico completo ni CI sobre este SHA.
- **M3 y DOC-02 permanecen EN_CURSO:** faltan pruebas de navegador de edición/cambio de proyecto durante la preparación del ZIP, verificación offline extraída del paquete, recuento de manguera frente a hilos, revisión humana de impresión y DOC-03…09. Una BOM con igual SKU y distintas variantes nominales sale en filas separadas, pero las tablas heredadas no explican aún la causa. ESQ-03 conserva el límite de solapes colineales. No declarar `FUNCTIONAL_COMPLETE`.

## Checkpoint de continuidad — 2026-09-24, DOC-02 offline y variantes

- Desde `96e7057`, `45de890` hizo visible la identidad/variante nominal que ya separaba partidas del BOM canónico. Dossier HTML/PDF e Ingeniería HTML/CSV explican, por ejemplo, el mismo SKU con 16 A frente a 20 A sin cambiar su agrupación. El PDF sintético fue renderizado e inspeccionado en las páginas de BOM e índice: filas y detalle legibles, sin recorte; queda pendiente aprobación humana de impresión real. `76190f1` añadió dos regresiones de navegador y un contrato de manguera frente a hilos individuales.
- `qa/paquete-documental-offline.mjs`: descarga UI, lectura ZIP STORE independiente, CRC/SHA-256 de las 16 piezas, extracción segura, apertura de índice/SVG/dossier/Ingeniería desde `file://` en sesión nueva offline, **17/17**, 0 errores JS y 0 solicitudes HTTP(S), **25,9 s** en el último corte. `qa/paquete-revision-obsoleto-m3.mjs`: crea A/B por UI, interrumpe una digestión SHA-256 real para entrelazar edición A y cambio A→B, rechaza ambos ZIP obsoletos y exporta solo B, **6/6**, 0 JS, **60,4 s**. `qa/ingenieria-documentacion.mjs` **13/13** en **34,4 s** y `qa/esquema-dossier.mjs` exit 0 con sus comprobaciones visibles, sin errores JS. Browser y servidor cerrados por cada arnés.
- TypeScript app/core verdes; batería integrada **1606/1606**, 0 fallos/omitidas, **25,845 s**; build QA **417 módulos**, **8,29 s**. `test/paquete-manguera-m3.test.ts` contrasta tiradas 3×/4× del levantamiento con siete conductores individuales y exige que el ZIP no convierta hilos en metros de manguera. El levantamiento de cubierta es global y no pertenece al `Proyecto`: incorporar esas tiradas en la revisión A/B mezclaría procedencia. La compra de manguera de un proyecto queda **NO MODELADA** hasta que exista entidad/vínculo persistente; no se extrapola por color/ruta.
- Este checkpoint no acredita CI, campaña histórica M9 ni aceptación humana. DOC-03 no puede basarse en snapshots de recuperación: pueden podarse o borrarse al eliminar el proyecto. Se necesita archivo inmutable de revisión emitida, no una comparación oportunista con autosave. `FUNCTIONAL_COMPLETE` sigue abierto.

## Checkpoint de continuidad — 2026-09-24, ESQ-04

- Rama `roadmap/1.0-functional`, sin promoción a `main`. `7a7b11c` añade QA navegador, `965ff92` plan de borrado puro y `659c671` integra acción eléctrica separada de «Borrar vista» en el esquema y aplica el mismo contrato al inspector 3D/multiselección.
- El preview enumera conexiones, vistas, hojas y datos técnicos afectados antes de borrar; cancelar no muta ni crea Undo. Una respuesta tardía de diálogo no se aplica a otro proyecto o a una revisión alterada. Deshacer/Rehacer restaura o retira el conjunto en una sola operación. Se conservan revisiones técnicas compartidas, hojas y archivo de decisiones V9; los vínculos directos, roles esclavos y metadatos de conductor afectados no quedan colgando.
- TypeScript app/core verde; batería integrada **1647/1647**, 0 fallos/omitidas, **94,6 s** mientras corrían QA navegador; build QA **422 módulos** en **8,03 s**. `qa/esquema-eliminacion-m2.mjs` **12/12** en **28,5 s**, 0 JS; QA existentes de desconexión y activación de representaciones exit 0, 6 y 5 hitos respectivamente, 0 JS, cierre limpio de browser/servidor. Evidencia focal, no campaña M9 ni aceptación M2 completa.
- Siguiente frontera M2: renumeración con preview estable y protección de designaciones congeladas; referencias cruzadas amplias, solapes colineales y aceptación multihoja siguen abiertos.

## Checkpoint de continuidad — 2026-09-24, renumeración ESQ-08

- `822fb70` cubre navegador, `96890f6` motor puro, `ab689f6` interfaz, `ad7e6df` documentos y `5e7a544` preservación de etiquetas al exportar. La renumeración ocurre solo tras vista previa y confirmación; reserva el texto real congelado aun si `numero` quedó obsoleto, detecta duplicados y conserva IDs, hojas, vistas, conexiones y archivo. Los conflictos bloquean toda aplicación. No hay renumeración automática al guardar.
- Antes de corregir la salida, PDF y paquete DOC-02 volvieron a llamar al numerador legacy en una copia; podían mostrar `-K100` donde el esquema persistía `-K1`. Ahora ambas emisiones mantienen las designaciones de la revisión. Si un aparato no tiene marca, los listados que la necesitan usan su ID estable; se debe renumerar explícitamente antes de una entrega profesional. La prueba documental usa PDF real y todas las piezas relevantes del paquete, sin mutar el Proyecto.
- TypeScript app/core verde; Node integrado **1654/1654** en **26,13 s**, 0 fallos/omitidas; build QA **423 módulos** en **7,23 s**. `qa/esquema-renumeracion-m2.mjs` **11/11** en **27,2 s**, 0 JS; `qa/ingenieria-documentacion.mjs` **13/13** en **36,5 s**, 0 JS; ZIP offline **17/17** en **17,6 s**, 0 JS. La primera invocación de Ingeniería no encontró el Chromium predeterminado de Playwright, sin llegar a ninguna comprobación; se repitió con `PW_CHROME` apuntando al Chromium local instalado. Esto es evidencia focal, no campaña M9/CI ni aceptación M2 completa.

## Checkpoint de continuidad — 2026-09-24, aceptación multihoja y referencias ESQ-08

- En `roadmap/1.0-functional`, sin promoción a `main`: `1df6211` diagnostica solapes colineales independientes de longitud positiva y los anuncia en SVG/PDF/DXF sin inventar un nudo; `7d0fc29`/`724f05f` prueban por navegador dos hojas, mando/potencia de un KM único, desconexión/reconexión real con ruta física pendiente, borrado/restauración de vista, simulación y tiempos de fase. `690ce87` advierte aparatos/conexiones sin vista también en exportaciones; `ab4d3f8` repone la vista del mismo aparato, con validación de casilla y Undo. El QA multihoja pasó **18/18**, 0 errores JS, **316 s** aislado; otra repetición con tests Node concurrentes tardó 401,2 s y no se usa como cifra de producto. El caso `w24` totalmente solapado necesitó Enter sobre el objeto accesible: no se afirma selección exclusiva con ratón.
- `8af55e1` deriva canal PLC↔terminal y circuito↔hojas solo desde borne/anclaje gráfico único e identidad persistente; diagnostica ausencias/ambigüedades, no elige por rótulo ni orden del array. `eaa5aef` añade índice desplegable con enlaces por IDs de hoja/vista, alcance de circuitos no exhaustivo y diagnósticos; navegador **11/11** en **11,0 s**, 0 JS. `ca282f6` exporta estas mismas referencias/diagnósticos al ZIP de una revisión; tests documentales focales **9/9**, incluidas dos regresiones nuevas, 0 fallos/omitidas.
- Antes de la UI/CSV finales, la batería Node integrada fue **1671/1671**, 0 fallos/omitidas, 29,18 s; app/core TypeScript y build QA **423 módulos** verdes. Tras CSV, core TypeScript y tests documentales focales verdes. La campaña histórica M9/CI, QA documental offline del nuevo ZIP y prueba integrada posterior a todos los cambios siguen pendientes. M2 y `FUNCTIONAL_COMPLETE` permanecen EN_CURSO.

## Checkpoint de continuidad — 2026-09-25, traslado visible ESQ-05

- En `roadmap/1.0-functional`, `060311f` introdujo el plan puro de movimiento por ID de vista y `2e8d496` lo integró en el esquema M2. El preview comprueba folio, rejilla, ocupación y documento vigente; el puntero anuncia la casilla sin escribir en `Proyecto`, sin crear Undo ni reconstruir el SVG en cada `pointermove`. La única aplicación ocurre al soltar sobre un destino válido. El traslado conserva identidad de vista, aparato y circuito y admite cambio de folio por el borde derecho.
- `qa/esquema-mover-vistas-m2.mjs` pasó **18/18**, 0 errores JavaScript, **90,4 s**: libre, ocupado, fuera de rejilla, clic sin arrastre, Escape, `pointercancel`, Undo/Redo, cambio de folio y guardar/reabrir. El arnés midió cierre de página **376 ms**, Chromium **143 ms** y servidor **0 ms**; no constituye una inspección global de procesos. TypeScript app y build QA de **427 módulos** quedaron verdes tras la integración.
- Sobre el checkpoint posterior a `2e8d496`, la batería Node integrada terminó **1705/1705**, 0 fallos/omitidos, **18,53 s**, exit 0. `qa/esquema-representaciones-m2.mjs` terminó con **6 hitos**, 0 JS, **19,3 s** tras acotar al grupo `.simbolo` un selector QA que también contaba sus bornes hijos; `qa/esquema-folios-m2.mjs` pasó **22/22**, 0 JS, **139,4 s**; `qa/esquema-estado-m2.mjs` pasó **21/21**, 0 JS, **92,0 s**. El ajuste del selector quedó en `0eea749`: no modifica el producto ni reduce las aserciones. La campaña integrada M9 sobre un candidato final continúa pendiente.
- En ese corte, ESQ-05 estaba **PARCIAL**: arrastre individual seguro, pero no alineación, grupo/copia/giro ni contrato completo de edición cotidiana. ESQ-07 todavía carecía de tamaño físico por folio (abordado en el checkpoint siguiente); ESQ-06 no acreditaba biblioteca IEC certificada. M2, M9 y `FUNCTIONAL_COMPLETE` no estaban aceptados. `main` permanecía en V9; no se promovió ni etiquetó 1.0 por ese checkpoint.

## Checkpoint de continuidad — 2026-09-25, formato físico por folio ESQ-07

- `cb0bfa2` persiste `formatoPapel?: 'A3' | 'A2'` en cada hoja M2 y monta A3 apaisado 420×297 mm o A2 apaisado 594×420 mm por ID. Ausencia del campo conserva el A3 histórico; A3 explícito y A3 heredado son estados distintos. El cargador rechaza un formato M2 desconocido porque descartarlo cambiaría la geometría; el legacy lo diagnostica. Rejilla, símbolos y enlaces usan las dimensiones de su propia hoja, sin dependencia del orden de arrays ni alteración de conductores.
- `aa701c5` añade selector y preview en Folios, cancelación sin Undo, aplicación única y etiqueta visible; el arrastre ESQ-05 usa las coordenadas del papel actual. `b88c6db` conserva tamaños mixtos en SVG y en el `/MediaBox` de cada página PDF, y muestra dimensiones por folio en el índice offline de la misma revisión. La inspección visual A2 descubrió que, al cerrar Folios, el lienzo seguía con zoom pequeño; `096354b` recalcula `aplicarZoomEsquema()` y la regresión comprueba que A2 se encuadra sin recorte. El formato físico no certifica escala de impresión, normativas ni legibilidad en papel real.
- Sobre el checkpoint con `096354b`: app TypeScript verde; Node **1711/1711** en **18,72 s**, 0 fallos/omitidos, exit 0; build QA **427 módulos** en **5,85 s**; `qa/esquema-formato-folios-m2.mjs` **12/12** en **86,3 s**, 0 JS. Las otras regresiones se ejecutaron antes de ese ajuste: mover vistas **18/18** en **89,7 s**, representaciones **6 hitos** en **21,9 s**, folios **22/22** en **136,0 s**; ZIP offline **17/17** en **5,2 s**, 0 peticiones de red y 0 JS. No mezclar esos pases previos con evidencia exacta del candidato final; esto no es la campaña M9 ni aceptación humana.
- La frontera A3/A2 de ESQ-07 quedó implementada y cubierta focalmente; otros tamaños no están modelados. ESQ-05 avanzado, ESQ-06, solape colineal y campaña/aceptación M9 permanecen abiertos. **M2 sigue EN_CURSO** y `FUNCTIONAL_COMPLETE` no se declara.

## Checkpoint de continuidad — 2026-09-25, selección y alineación ESQ-05

- En `roadmap/1.0-functional`, `564f890` añadió el plan puro de alineación y `ce22858` integró selección/acción visible y QA. Las vistas se seleccionan por ID con Shift+clic o Shift+Enter; el plan trabaja con 2–64 vistas del mismo folio, y aplica la alineación como **un único Undo**. No modifica aparatos, conductores ni el grafo eléctrico. Al cambiar de folio se limpia la selección. Cancelar, no-op y rechazo por destino inválido no escriben en el documento ni crean historial; guardado/reapertura conserva el resultado.
- El área de comprobación de ocupación considera las **cajas de símbolos**, no las etiquetas ni el recorrido de los hilos. No equivale a resolver automáticamente toda interferencia gráfica. ESQ-05 aún necesita localizar/pan, copia y giro para la edición diaria completa; no se declara terminado.
- Frontera verificada: tests focales del plan **9/9**; TypeScript core/app verdes; build QA **428 módulos** en **6,11 s**; batería Node **1720/1720**, 0 fallos/omitidos, **21,1 s**. `qa/esquema-alinear-vistas-m2.mjs` **14/14**, **102,9 s**, 0 errores JS; regresiones de mover **18/18**, **88,4 s**, y folios **22/22**, **136,0 s**, ambas 0 JS. `qa/esquema-representaciones-m2.mjs` completó sus **6 hitos**, exit 0 y 0 JS sobre el mismo árbol.
- Evidencia focal, no campaña M9 ni aceptación M2. No se hizo verificación GPU física ni aprobación visual/humana de impresión; `main` permanece como V9 estable y no se declara 1.0 ni `FUNCTIONAL_COMPLETE`.

## Checkpoint de continuidad — 2026-09-25, localizar y encuadrar ESQ-05

- Después de `da49d63`, `9309541` corrigió la navegación a una **vista exacta por ID**: el enlace en el índice de referencias elige el folio y desplaza el lienzo para mostrar la representación, incluso con zoom alto. Enter conserva el foco en esa vista; los enlaces que no tienen ID gráfico reinician el scroll de forma explícita. El recorte del lienzo permite alcanzar ambos bordes de la hoja. La acción de localización no modifica `Proyecto` ni crea Undo.
- El primer QA ampliado no pasó: entre 14 comprobaciones hubo dos fallos funcionales reales, vista localizada fuera del viewport y borde izquierdo inaccesible. Tras el ajuste UI/CSS, `qa/referencias-esquema-m2.mjs` pasó **16/16** en **14,4 s**, 0 errores JS. App TypeScript verde; build QA **428 módulos** en **6,01 s**; Node **1720/1720** en **18,214 s**, 0 fallos/omitidos.
- Regresiones focales posteriores: mover vistas **18/18** en **81,0 s**; formato A2/A3 **14/14** en **82,8 s**; navegación de issues **8/8** en **22,1 s**; alineación **14/14** en **97,7 s**, exit 0. Todas reportaron 0 JS. El cierre de la suite de alineación tardó decenas de segundos, sin procesos QA huérfanos detectados; no convertir esa duración en métrica de producto.
- ESQ-05 sigue **PARCIAL**: se cubren traslado, alineación y localización/encuadre, pero pan, copia y giro continúan abiertos. Son pruebas focales, no la campaña M9 ni aceptación M2, GPU física o revisión humana. `main` permanece en V9; no declarar `FUNCTIONAL_COMPLETE` ni versión 1.0.

## Checkpoint de continuidad — 2026-09-25, pan del esquema ESQ-05

- `27e77e0`, posterior al checkpoint documental `a98ac32`, permite desplazar el lienzo con foco mediante **Space+botón primario** o **botón medio**. La captura del gesto ocurre antes de handlers de bornes/símbolos para no iniciar conexión, selección o movimiento; durante el pan cambia solo el scroll. Los punteros táctiles quedan fuera de esta captura (su gesto nativo aún no se ha probado); las flechas nativas se conservan cuando el lienzo tiene foco. Al terminar se suprime el clic residual y se limpian eventos/estado en `pointerup`, `pointercancel`, `lostpointercapture`, `blur`, Escape y cierre del esquema.
- La primera ejecución de `qa/esquema-pan-m2.mjs` quedó roja porque el lienzo no recibía foco. La regresión final pasó **15/15** en **38,4 s**, 0 errores JS: incluye pérdida de Space por `blur`, cancelación de puntero, Escape, cerrar/reabrir, clic posterior sobre borne, control con Space y flecha, y confirma `Proyecto`/Undo inmutables. No se ocultó el fallo inicial.
- TypeScript app/core verde; build QA **428 módulos** en **6,05 s**; Node **1720/1720** en **18,515 s**, 0 fallos/omitidos. Con pan integrado, navegador: conexión pendiente **19/19** en **53,0 s**, mover **18/18** en **86,6 s**, referencias **16/16** en **16,0 s**, alineación **14/14** en **97,8 s** y formato A2/A3 **14/14** en **80,5 s**; todos 0 JS. Alineación acabó con exit 0 tras cierre lento de arnés, sin procesos QA huérfanos observados.
- ESQ-05 permanece **PARCIAL**: copia/pega y giro no están resueltos. ESQ-06, solape colineal independiente y campaña M9 siguen abiertos. Esto no acredita GPU física ni inspección humana del impreso; **M2 NO ACEPTADO**, sin `FUNCTIONAL_COMPLETE` ni versión 1.0. `main` continúa en V9.

## Checkpoint de continuidad — 2026-09-25, inventario local ESQ-06

- El generador vincula cada símbolo montado —incluidas vistas parciales de bobina/contactos— con un ID estable de plantilla y familia, derivados de la **misma resolución que dibuja los trazos**. El inventario cubre los 22 tipos; `valvula`, `resistencia`, `condensador`, `cable` y `otro` usan caja genérica cuando no declaran otra geometría, sin presentarse como símbolo especializado. El inspector y SVG exportado muestran origen y límites; el PDF lleva nota visible y IDs de plantillas en metadatos. La ficha no se persiste en `Proyecto`, así que no crea otra verdad eléctrica.
- Los trazos actuales se generan localmente en `src/motores/esquema.ts`. `package.json` declara `GPL-2.0-or-later` para el proyecto, **no** autoría/licencia individual acreditada por símbolo. Cada ficha marca `licenciaVerificada: false` y `conformidadNormativa: NO_VERIFICADA`; el comentario de lectura ya no afirma que el dibujo sea IEC. No se importó QElectroTech ni otra biblioteca externa. Una biblioteca personalizada, pruebas normativas y revisión de derechos siguen pendientes; ESQ-06 continúa **PARCIAL**.
- TypeScript app/core, build QA y Node **1726/1726** verdes; QA navegador `qa/esquema-procedencia-m2.mjs` **10/10** en **2,8 s**, 0 errores JS: fuente dedicada frente a válvula genérica, inspector, descarga SVG y constancia de Proyecto/Undo. Regresión de representaciones M2: seis hitos, exit 0, 0 JS en **19,2 s** sobre el ajuste final del inspector. Es evidencia focal, no revisión humana de impresión ni aceptación M2/M9. `main` permanece V9; no declarar `FUNCTIONAL_COMPLETE` ni 1.0.

## Checkpoint de continuidad — 2026-09-25, copia y giro ESQ-05

- `e037259` y `acb2bcf` quedaron respaldados mediante push normal en `origin/roadmap/1.0-functional`. Copiar una vista completa y única crea **otro aparato eléctrico y otra vista**, con IDs distintos y sin conductores; pegar solo en una casilla de folio válida y con hueco físico en la placa. El plan rechaza duplicar anclajes del mismo aparato, destinos ocupados, documentos cambiados, ejemplos de solo lectura y colocación sobre la puerta no resuelta. La operación usa un solo Undo; cancelación y rechazo no dejan aparatos fantasma. La copia desde vistas funcionales desdobladas continúa fuera de este contrato, no se finge como lista.
- `7ec0885` y `cae2afe` permiten giro editorial **solo 180°**: gira trazos y pines alrededor del centro, deja texto legible, no gira el aparato 3D ni modifica el circuito. El campo persistente `giro?: 180` conserva exactamente la orientación legacy al omitirse; el cargador rechaza una orientación desconocida en vez de descartarla. Copiar una vista girada conserva su orientación. Un giro 90° no está permitido hasta definir ocupación, rutas y caja girada de forma segura.
- App/core TypeScript verdes; Node integrado **1734/1734** (0 fallos/omitidos), build QA **430 módulos** en **5,96 s**. `qa/esquema-copiar-vista-m2.mjs` **14/14** en **23,5 s incluyendo cierre de Chromium/servidor**, 0 JS; `qa/esquema-representaciones-m2.mjs` completó sus **6 hitos**, exit 0. La suite nueva cubre folios, rechazo, Undo/Redo, reapertura, anclajes y circuito. Esto es evidencia focal, no revisión visual humana del impreso ni campaña histórica M9. **M2 sigue EN_CURSO; `FUNCTIONAL_COMPLETE` y 1.0 no se declaran.**

## Checkpoint de continuidad — 2026-09-25, respuesta R1 y entrega offline

- Commits funcionales separados: `82446e6` (Worker de rutas, invalidación y cancelación), `4d2d019` (QA R1 y contrato de un único bundle offline), `649f856` (vinculación técnica sin entidad vieja) y `e557bc9` (HTML de producción reproducible). `main` sigue siendo V9; esta es la rama de desarrollo.
- TypeScript app/core y Node **1736/1736** verdes, 0 fallos/omitidos. Gate de navegador **13/13**, 273 comprobaciones en **10:04**. Fusión **59/59** puntos frontales y **186/186** clics semánticos, sin ejes coincidentes; fixture de puerta y V8 importación **55/55** verdes. Entrega offline V3–V9 verde con Build ID **`8254F5160F`**, 0 errores JS/peticiones externas; verificador de frescura y reproducibilidad LF/CRLF verde. El focal R1 empaquetado temporalmente con sonda también probó Worker, unión manual y cancelación por la UI.
- R1 en GPU NVIDIA RTX 5070 Ti: listener de selección p95 **46,3 ms**, listener `pointerup` de aparato p95 **37,4 ms**, render directo p95 **16,3 ms**, 0 tareas largas en cinco drags. El gesto host bajó de **3817,3** a **378,5 ms** p95, pero las rutas completas aún tardan **4523,6 ms** p95. Un gesto de unión manual respondió en **472,6 ms** host (n=1). SwiftShader y otros equipos no están acreditados como fluidos; la inspección humana y el coste total siguen pendientes. **M0/R1 NO ACEPTADO; M1–M9 y `FUNCTIONAL_COMPLETE` siguen abiertos.**

## Checkpoint de continuidad — 2026-09-25, mensaje mínimo de ruteo

- El Worker ya no recibe fotos `data:` ni dossier, ingeniería o datos técnicos al arrastrar: `proyectoParaRuteo` le envía solo gabinete, anclajes, terminales y conexiones relevantes. La presencia de imagen y profundidad personalizada siguen en la firma y la ruta. El documento original no se reescribe. En una prueba con imagen de 4 MB, el mensaje serializado quedó por debajo del 1 % y conservó el anclaje; en los cinco tableros principales y el fixture de puerta las firmas y rutas fueron idénticas bit a bit.
- La detección local de Chromium ahora reconoce el directorio `chrome-win64` de Playwright en Windows, además del layout anterior. La instalación aislada de `pnpm` intentó generar `pnpm-lock.yaml`/`pnpm-workspace.yaml`; ambos eran artefactos nuevos de esa invocación y se retiraron. Se usaron ejecutables locales directos para no modificar la configuración del repositorio.
- App/core TypeScript verdes; batería Node **1738/1738**, 0 fallos/omitidos, **19,66 s**; build QA **432 módulos** en **6,64 s**. `qa/cables.mjs` terminó con **63/63** geometrías, 0 JS. Sonda R1 focal en NVIDIA/Chromium 153: gesto de aparato **396,8 ms**, listener `pointerup` **36 ms**, rutas listas **4569,2 ms**, 0 tareas largas, unión manual **434,1 ms**, `completo:true`, 100/100 geometrías y 0 JS. Es una muestra, no mejora medida para el fixture sin imágenes ni aceptación R1. El HTML de producción se regeneró con Build ID **`1A8AAE69FE`**; `qa/empaquetado.mjs` pasó V3–V9, 0 errores JS y 0 peticiones externas. El gate histórico de diez minutos no se repitió en este corte focal; su última evidencia corresponde al checkpoint anterior.
- **R1 sigue NO ACEPTADO:** el cálculo global de ~4,6 s, otras plataformas y aceptación humana permanecen abiertos. M1–M9 y `FUNCTIONAL_COMPLETE` tampoco se declaran cerrados.

## Checkpoint de continuidad — 2026-09-25, copia desdoblada ESQ-05

- Desde cualquier vista funcional de un aparato (bobina, polos o auxiliar), Copiar/Pegar crea **un aparato eléctrico nuevo y todas sus vistas**, sin copiar conductores. El folio de cada vista se conserva y todo el grupo recibe el mismo desplazamiento de casillas; pegar el grupo sobre otro folio o sobre casillas/cajas ocupadas se rechaza antes de `capturar()`. Un solo Undo/Redo gobierna aparato, colocación y todas las vistas. El portapapeles se invalida al editar una vista de origen, para no pegar un grupo distinto del preparado.
- La inspección del plano Mando/Potencia reveló un fallo real: un proyecto legacy con `designacion: -KM1` y sin `numero` daba también `-KM1` a la copia. La asignación compartida por copia desde esquema y portapapeles cuenta ahora la secuencia visible, además del número persistido. Original `-KM1`, copia `-KM2` en ambos folios y en SVG; los IDs eléctricos y la conexión original siguen intactos. Los screenshots QA quedan locales, no constituyen aprobación del plano impreso.
- TypeScript app/core y Node integrados **1745/1745** verdes, 0 fallos/omitidos; build QA **433 módulos**. QA visible de grupo **13/13** tras la última validación de cajas con el rótulo final, copia/giro previo **14/14**, representaciones M2 seis hitos, todos 0 errores JS y recursos cerrados. HTML offline Build **`4631B6B6BE`** reproducible y recorrido V3–V9 desde `file://` verde, sin JS ni peticiones externas. La campaña histórica completa y la aceptación humana del impreso aún no se han hecho para este SHA.
- Las capturas muestran bobina/auxiliar en Mando y polos en Potencia con designaciones distintas y sin cajas superpuestas; el texto es muy pequeño al encuadrar la hoja completa en pantalla. **M2 sigue EN_CURSO:** legibilidad física, símbolos personalizados ESQ-06, solape colineal y edición restante no están resueltos; tampoco se declara `FUNCTIONAL_COMPLETE` ni 1.0.
- `qa/mejoras.mjs`, sonda fuera del gate oficial, se actualizó a los cajones Añadir/Seleccionar y a la espera de IndexedDB; su comando ahora construye QA y cierra Chromium/servidor incluso ante excepción. Pasó **24/24** en **69,6 s**, 0 JS: búsqueda, duplicación Ctrl+D en cadena, designaciones únicas, Undo y ausencia de cables fantasma. Antes falló por precondiciones obsoletas; dos intentos posteriores usaron una build de producción sin `window.qa` y un ID de herramienta antiguo, ambos identificados y corregidos en el arnés. No equivale a aceptación M2 completa.

## Checkpoint de continuidad — 2026-09-25, dibujo personal ESQ-06

- «Mis Componentes» ofrece un símbolo vectorial **opt-in** (bloque/círculo/rombo, rótulo y hasta 32 segmentos acotados), separado de foto 3D, bornes y perfil eléctrico. El cargador acepta solo primitivas conocidas, no ejecuta SVG/HTML arbitrario, y registra un diagnóstico si un proyecto trae dibujo inválido. Autor y licencia son declaraciones de usuario **NO VERIFICADAS**; no se afirma IEC/NCh ni permiso de terceros.
- Definiciones y aparatos colocados conservan revisiones independientes. Paquete de proyecto V5 y `.tscomp` V4 transportan el dibujo; una versión antigua que lo perdería se rechaza. Plano, inspector y SVG muestran la procedencia; el PDF añade un anexo legible con autor/licencia declarados por plantilla personal. Las vistas funcionales desdobladas siguen usando sus símbolos de bobina/contactos por semántica.
- TypeScript app/core y batería Node **1749/1749** verdes, 0 fallos/omitidos; build QA **434 módulos**. `qa/simbolo-personal-m2.mjs` **12/12** en **66,4 s**, 0 JS: asistente, importación limpia, colocación, portabilidad, vista M2, inspector, SVG y PDF. `qa/esquema-procedencia-m2.mjs` histórico **10/10** en **2,8 s**, 0 JS; navegadores/servidores cerrados. El anexo PDF de dos páginas se renderizó con Poppler y se inspeccionó: texto y márgenes legibles, sin solape. La captura del esquema confirma dibujo y bornes, pero el rótulo es pequeño al encuadrar toda la hoja.
- El envoltorio `pnpm test` intentó instalar dependencias y falló por `ERR_PNPM_IGNORED_BUILDS` del host, antes de ejecutar tests. Se ejecutaron sus tres pasos equivalentes con dependencias existentes. Se retiraron solo los manifiestos temporales no versionados que esa invocación produjo. No hubo campaña M9 completa, impresión humana, revisión legal o certificación normativa. **M2 y `FUNCTIONAL_COMPLETE` continúan NO ACEPTADOS; `main` permanece V9.**

### Entrega offline del mismo incremento

- Producción **434 módulos**; `app/empaquetar.mjs` regeneró `dist-final/TableroStudio.html` y `desktop/app.html` byte a byte iguales con Build ID **`8CE0372AE7`**. `qa/empaquetado.mjs` abrió ese archivo por `file://` y completó sus recorridos V3–V9, exit 0, 0 errores JavaScript y 0 peticiones HTTP externas obligatorias. `herramientas/verificar-entrega.mjs` recompiló en temporal y confirmó frescura de ambos archivos y reproducibilidad LF/CRLF.
- Esta comprobación offline no crea aceptación M2/M9 ni sustituye la revisión humana del plano. El símbolo personal se probó en el navegador QA con servidor; el smoke del HTML de producción cubre el arranque y regresiones previas, no vuelve a crear el símbolo desde `file://`.

## Checkpoint de continuidad — 2026-09-25, solapes M2 seleccionables

- El solape colineal de hilos eléctricamente distintos **no está resuelto geométricamente**: la advertencia sigue visible y el SVG/PDF/DXF no inventan una unión. El panel M2 enumera por ID los conductores implicados y permite elegir cualquiera con ratón o teclado incluso cuando su trazo no tiene un segmento clicable exclusivo (caso `w24`). La selección no cambia identidad, recorrido ni topología.
- El barrido de solapes de la hoja se hace una vez por actualización del visor y se reutiliza para el SVG interactivo; las exportaciones independientes mantienen su propio diagnóstico. TypeScript app/core y Node **1749/1749** verdes, 0 fallos/omitidos; build QA **434 módulos**. `qa/esquema-aceptacion-m2.mjs` amplió el recorrido a **20/20** en **160,8 s**, 0 errores JS. El HTML offline regenerado tiene Build ID **`FC9FC704D4`**; smoke V3–V9 desde `file://` y verificación de reproducibilidad verdes.
- La tarea pendiente sigue siendo el trazado seguro que separe tinta colineal sin cortar una conexión verdadera, fabricar un nudo o atravesar símbolos. El selector por ID es una mitigación de interacción, **no aceptación de ESQ-03/M2 ni de 1.0**.

### Diagnóstico de la siguiente frontera M2

- El constructor actual `rutaHilo` envía los enlaces horizontales de cada hoja a una misma banda superior o inferior y, para extremos de distinta altura, puede hacer que la pata vertical atraviese otro símbolo. Sobre el arranque directo con representaciones M2 recién activadas, el diagnóstico puro encontró **32 solapes en la hoja de fuerza, 18 en mando y 7 cruces del interior de cajas de símbolos** (4+3). Es un censo de esa geometría, no una prueba de conformidad ni un nuevo fallo eléctrico: cada `conductorId` sigue distinto. Una futura corrección debe reservar corredores, respetar salidas de bornes y obstáculos, conservar orden estable e inspeccionarse en SVG/PDF/DXF; no basta desplazarlos en el render.
- FIN-02: «Acerca de» no debe anunciar `1.0.0` a partir del número técnico de `package.json` mientras M0–M9 están abiertos. La etiqueta pública ahora dice «preversión funcional en desarrollo» y conserva el Build ID; la aserción se añadió al smoke offline. Esta corrección de identificación no cambia formatos ni motor.
- El HTML offline Build **`E4A1F0B114`** pasó `qa/empaquetado.mjs` V3–V9, con 0 errores JS y 0 peticiones HTTP externas; el QA comprueba el archivo único servido por `file://`, no una build de desarrollo. Esto no sustituye la campaña integrada M9.

## Checkpoint de continuidad — 2026-09-25, corredores M2

- `8f5c9eb` añadió routing editorial por ID estable y bornes reales; `f31c86f` endureció la QA para exigir clic de ratón sobre la tinta del conductor `w24`. Antes de este corte el arranque directo M2 tenía 32+18 solapes y 7 penetraciones de símbolo; ahora tiene **0/0**. Los cinco ejemplos principales tienen 0 penetraciones y 0 casos sin corredor. Arranque, bomba, estrella-triángulo y control-24v no tienen solapes; la UMA mantiene **dos** solapes advertidos. Las rutas son proyección esquemática, no `Conductor.trazado`, longitud ni una unión nueva.
- TypeScript app/core y Node **1756/1756** verdes, 0 fallos/omitidos; build QA **435 módulos**; QA aceptación M2 **20/20** en **147,9 s**, 0 errores JS. Mover vistas 18/18, copiar grupo 13/13, representaciones seis hitos y formato A2/A3 14/14 pasaron antes del último ajuste de salidas laterales; no usarlos como evidencia final de ese ajuste. Producción **435 módulos**, HTML único Build **`70F1E528AE`**: smoke offline V3–V9 terminó `TODO OK`, 0 JS/solicitudes HTTP externas, y verificador de entrega confirmó frescura y bytes reproducibles LF/CRLF. No se observaron procesos Chromium/servidor del encargo tras cerrar las pruebas.
- Revisión visual técnica: el PDF A3 vectorial del arranque directo (dos hojas) no mostró cajas penetradas, pero el circuito ocupa pocas columnas y los rótulos parecen pequeños al ver el folio completo. Se requiere revisión humana del impreso; dos solapes de la UMA también necesitan una corrección editorial comprobable o control manual. La muestra Node del montaje M2 dio p50 **25,6 ms** en arranque y **163,7 ms** en UMA; no es benchmark GPU de respuesta. **M2 no aceptado; `FUNCTIONAL_COMPLETE`/1.0 no declarados.**
- Al retomar: verificar rama `roadmap/1.0-functional`, HEAD/remoto y árbol. Continuar desde los dos solapes UMA y la legibilidad/edición de rutas del plano sin modificar el grafo eléctrico. La campaña histórica M9/CI no se repitió para este checkpoint. `main` sigue como baseline V9 y no debe recibir esta preversión por accidente.

## Checkpoint M6 — ruta XYZ manual de referencia, 2026-09-26

- `rutaFisica` V1 y formato de proyecto 3 preservan identidad/conexión/propiedades; V1/V2 se leen sin convertirlos. El marco `PLACA` acepta solo extremos colocados en placa, no puerta ni campo. Datos desconocidos o dos escritores de ruta se rechazan sin autorouting reparador. Un XYZ legacy completo puede adoptarse únicamente con preview y confirmación; XY ambiguo queda legacy. El gesto y el inspector permiten nodos XYZ literales, Undo/Redo y reapertura. La malla M6 sigue segmentos rectos de la ruta; el render legacy no cambia.
- La ruta XYZ produce solo una referencia geométrica espacial. Ingeniería/DRC no adopta implícitamente ese largo: usa el largo eléctrico declarado o deja la magnitud indeterminada. El paquete distingue referencia M6, recorrido legacy 2D y propuesta de corte; una ruta M6 no fabrica metros de taller. CAB-27 (política visible de adopción) sigue pendiente.
- Validaciones del corte: app/core TypeScript, Node **1777/1777** sin fallos/omitidos, build QA 439 módulos, `qa/ruta-fisica-m6.mjs` **12/12** y 0 JS, `qa/paquete-documental-offline.mjs` **17/17**, `qa/cables-fusion.mjs` verde (59/59 frontal, 186/186 picking, 0 coincidencia de eje 3D en tres ejemplos). Producción 439 módulos y HTML Build **`63412C6EC1`**. Registrar el resultado final del smoke offline y del verificador antes de tratar el empaquetado como acreditado sobre este Build.
- **M6 NO ACEPTADO.** Siguiente frontera técnica: CAB-04 marcos de anclajes para puerta/campo; CAB-06/07 inserción y edición fiel de tramo; CAB-10/11 diagnóstico sin expulsión y canaleta habitable; CAB-25 radios/curvas; CAB-27 adopción de longitud; después escena densa, migración amplia y sustitución del escritor legacy. No promover la rama a `main` ni etiquetar 1.0 por este corte.

### Continuación M6 — física y diagnóstico informativo

- La simulación ya no inventa para un cable M6 la distancia recta entre posiciones de aparatos: sin longitud eléctrica declarada o inyección explícita, su impedancia queda `NO_MODELADO`. El estado de ruta XYZ por sí solo no adopta CAB-27. Una prueba con fuente física activa verifica ese límite, no un solver inactivo.
- El inspector ofrece «Revisar interferencias» bajo demanda. El reporte muestra la peor cercanía con otro cable y la peor invasión de sólido/canaleta según la geometría del modelo, sin expulsión ni cambio del proyecto. La QA visible atraviesa intencionalmente otro aparato y comprueba aviso más ruta intacta; **13/13**, 0 JS, 35,2 s sobre la última build QA. TypeScript app/core y Node **1777/1777**, 0 fallos/omitidos; build QA 439 módulos. CAB-10/11 siguen parciales: todavía no hay diagnóstico incremental durante drag, radio verificable ni cobertura visual densa de entradas a ductos.

### Continuación M6 — nodos sobre el tramo y cancelación del gesto

- La inserción M6 usa la posición del nodo en la polilínea ordenada, no su reproyección XYZ. Una ruta que vuelve al mismo punto ya no puede insertar por error en la primera pasada. La QA de doble clic real comprueba orden, profundidad Z, Undo/Redo y reapertura. Escape cancela antes/después del umbral; `pointercancel` y pérdida de foco tienen el mismo camino de restauración. El preview no deja una foto Undo falsa ni borra un Redo preexistente.
- TypeScript app/core y batería Node **1778/1778** (28,65 s, 0 fallos/omitidos) verdes tras el ajuste de captura; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **20/20**, 0 errores JS, **69,7 s** sobre la última build QA; `qa/montaje-numerico-m5.mjs` **27/27**, 0 JS, y `qa/cables-fusion.mjs` verde (0 ejes 3D coincidentes en tres ejemplos, 59/59 frontal, 186/186 picking) sobre la build inmediatamente anterior al añadido de cancelación por pérdida de foco. El HTML único Build **`321C8D0B28`** pasó el smoke offline V3–V9 desde `file://`, sin errores JS ni solicitudes HTTP obligatorias; verificador de entrega confirmó ambas copias frescas y bytes reproducibles LF/CRLF. Ninguna de estas pruebas equivale a la campaña histórica M9 completa o a aceptación visual humana de canaletas/puerta.
- **M6 sigue EN_CURSO.** CAB-06/07/13 avanzan, pero faltan mover tramos completos, radios/curvas explícitos, política de longitud adoptada, preview de colisiones, marcos móviles de puerta/campo, canaletas densas y transición del escritor legacy. El documento de versión sigue sin declarar `FUNCTIONAL_COMPLETE` ni 1.0 aceptada.

### Continuación M6 — tramo interior editable y recuperación por ID

- El inspector M6 permite desplazar ambos nodos de un tramo interior con un delta XYZ único y quitar cualquier nodo por su ID, sin depender del píxel del tirador. Los bornes quedan anclados; una propuesta pura valida todas las coordenadas antes de crear historial. Delta vacío/nulo e índices o coordenadas inválidas no dejan mutación parcial. Se conserva el límite de 128 nodos del archivo también en la creación desde UI. Los enlaces vecinos pueden cambiar y requieren revisión; todavía no se ofrecen radio ni movimiento de tramos ligados directamente a bornes.
- TypeScript app/core y Node **1780/1780**, 0 fallos/omitidos, **27,4 s**. Build QA **439 módulos**. La QA visible `qa/ruta-fisica-m6.mjs` **27/27**, 0 errores JS, **64,6 s** sobre el código del control antes de retirar el icono de mano de su etiqueta; comprueba Undo/Redo de un solo paso, reapertura, rechazo de campos vacíos y ausencia de desbordamiento a 1024 px. La captura del inspector estrecho se revisó: IDs, XYZ y botones permanecen legibles en el panel con scroll. HTML offline Build **`22C108D172`** generado; `verificar-entrega.mjs` confirmó las dos copias y reproducibilidad LF/CRLF. El smoke offline completo V3–V9 no se repitió sobre este Build; el último verde corresponde al Build `321C8D0B28` anterior.
- **M6 continúa parcial.** Faltan CAB-04 puerta/campo, CAB-10/11 feedback durante preview y canaleta densa, CAB-25 curvas/radios, CAB-27 adopción de metraje, planes/autorouting explícitos, escena densa y retirada del escritor legacy. No promover a `main` ni declarar versión 1.0 con este checkpoint.

### Continuación M6 — diagnóstico al confirmar el arrastre

- Al soltar un nodo manual, el Worker reparte rutas una vez y el editor inspecciona la geometría vigente de ese conductor. Si un tramo invade un aparato o canaleta, o presenta cercanía con otro cable, muestra un aviso sin expulsar el nodo ni modificar Undo. Durante `pointermove` no se ejecuta el barrido global. El diagnóstico también sigue disponible a demanda en el inspector. Es el peor aviso por categoría, no una declaración de fabricabilidad.
- TypeScript app/core y Node **1780/1780**, 0 fallos/omitidos; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **29/29** en **74,7 s**, 0 errores JS: un gesto real deja el punto fuera del aparato pero el segmento lo atraviesa, dispara aviso, conserva la ruta y permite Undo. La ejecución anterior de 29/29 también pasó en 87,3 s. Entre ambas, una invocación de QA abrió por error la build de producción generada para el smoke y expiró esperando `window.qa`; se restauró la build QA y se repitió sin tocar producto ni relajar la aserción.
- HTML offline Build **`248AC1EB63`**: smoke V3–V9 desde `file://` completó `TODO OK`, sin errores JS ni peticiones HTTP externas obligatorias. `verificar-entrega.mjs` confirmó ambas copias actuales e idénticas y bytes reproducibles LF/CRLF. El directorio efímero `app/dist` quedó nuevamente en modo QA tras la repetición; los dos HTML versionados conservan la build de producción verificada.
- **M6 sigue EN_CURSO.** En este corte aún faltaba aviso durante preview sin coste global, además de radios verificables, zonas de canaleta y marcos de puerta/campo; CAB-10/11 no estaban aceptados. Tampoco se ejecutó la campaña histórica M9 completa para ese incremento ni una inspección humana de tableros densos. No declarar `FUNCTIONAL_COMPLETE` o 1.0.

### Continuación M6 — aviso local durante el arrastre

- Los dos segmentos vecinos del nodo M6 se comprueban en la vista previa contra sólidos 3D preparados al comenzar el gesto. La barra muestra un aviso textual y el cable se colorea sin bloquear su posición. La comprobación puntual también distingue ahora profundidad real de proyección X/Y: un nodo situado por encima de un aparato no es una invasión. Las canaletas mantienen su interior útil y el diagnóstico completo al soltar; esta vista previa todavía no cubre pared/ranura de todo el tramo, radio mínimo ni proximidad a otros cables.
- App/core TypeScript, Node **1781/1781** sin fallos/omitidos; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **31/31**, **101,7 s** sobre el ajuste final del dominio, 0 JS: aviso visible antes de soltar, conservación y Undo después, y ausencia de falso aviso cuando el recorrido proyectado pasa sobre el aparato pero separado en Z. La función geométrica pura tiene 12 pruebas M6. En el fixture pequeño, 30 movimientos internos al navegador dieron p95 de listener **3,2 ms** (n=30, medición anterior al ajuste del límite de área, no benchmark de escena densa).
- Producción **439 módulos**, HTML Build **`98DE003B29`**; `verificar-entrega.mjs` confirmó ambas copias frescas e idénticas y reproducibilidad LF/CRLF. El smoke offline del Build exacto completó V3–V9 desde `file://`, `TODO OK`, 0 JS y 0 HTTP externo obligatorio. No se ejecutó el gate M9 integral ni inspección humana de canaleta densa. **M6, M9 y versión 1.0 siguen no aceptados.**

### Continuación M6 — entrada por ranura y aviso de pared

- El diagnóstico local del drag añade las partes sólidas de canaleta en los dos tramos vecinos del nodo, utilizando `RedCanaletas` preparada y el mismo verificador geométrico del diagnóstico definitivo. Interior útil y ranura permiten el recorrido; el diente genera aviso sin mover ni expulsar el cable. El radio procede del conductor. No se afirma que el muestreo a 3 mm pruebe toda fabricación o capacidad del ducto.
- TypeScript app/core, Node **1782/1782**, 0 fallos/omitidos; build QA **439 módulos**. `qa/ruta-fisica-m6.mjs` **34/34**, **180,8 s** en la última ejecución, 0 JS en ambos contextos: el primero conserva ruta/Undo/Z, el segundo comprueba gestos reales de ranura e invasión de diente. Un intento previo quedó rojo porque el fixture puso el nodo en X=75 mm, tangente al diente con el radio real; X=74 mm sí es invasión. No se relajó la aserción ni se cambió el detector para obtener verde.
- HTML Build **`7E3B4FD8F9`**, 439 módulos, con ambas copias frescas, idénticas y reproducibles según `verificar-entrega.mjs`. El smoke offline del Build exacto completó V3–V9 desde `file://`, `TODO OK`, 0 JS y 0 HTTP externo obligatorio. No se repitió el gate M9 completo, fusión ni escena densa después de este incremento. **M6/CAB-11 y 1.0 permanecen parciales.**

### Continuación M6 — contacto local entre conductores y límite del borne

- El arrastre manual prepara una rejilla de los demás conductores una vez; el preview consulta solo los dos tramos que cambian. Muestra la holgura 3D con el conductor vecino sin desplazar ninguna ruta y permite cancelar. Una proyección frontal coincidente a distinta Z no es contacto. Se corrigió además la exención del borne compartido: dos segmentos largos que comparten tornillo ya no quedan perdonados íntegramente porque su mínimo cae en el origen; solo se excluye la zona de salida del borne.
- Tests focales M6 **14/14**; TypeScript app/core y Node completo **1783/1783**, 0 fallos/omitidos, **33,9 s**; build QA **439 módulos**. QA M6 **37/37**, **473,6 s** en la ejecución final, 0 errores JS, con ratón real y Escape; la pasada anterior a la corrección del borne fue **37/37** en **237,1 s**. La diferencia de tiempo no tiene causa aislada demostrada. `qa/cables-fusion.mjs` pasó sobre la build corregida: 0 ejes 3D coincidentes en los tres ejemplos, 59/59 frontal, 186/186 picking semántico, 0 JS y ningún cable fantasma.
- El ruteo puro Node sobre los ejemplos principales tardó 252 ms (arranque), 108 ms (bomba), 1862 ms (estrella-triángulo), 218 ms (control PLC) y 1657 ms (UMA) en esta máquina. No es benchmark de latencia de `pointermove` ni prueba de escena densa. El HTML único Build **`C92FD75BF4`** quedó fresco e idéntico web/escritorio según el verificador; su smoke `file://` recorrió V3–V9 con `TODO OK`, 0 JS y 0 peticiones HTTP externas obligatorias. El reparto global al soltar y CAB-24 siguen pendientes; una caché solo en memoria no satisfaría persistencia tras reapertura. No se ejecutó el gate M9 integral. **M6 y 1.0 continúan NO ACEPTADOS.**

### Continuación M6 — la misma ruta manual en preview y al confirmar

- El constructor de `RutaCable` manual es ahora único. Los nodos persistentes consecutivos en el mismo XYZ mantienen ambos IDs pero comparten un vértice de la polilínea derivada; `indicesNodos` tiene el mismo significado durante drag y después del reparto. Mover uno separa los índices sin depender de una ruta cacheada anterior. No se cambió la forma de los recorridos automáticos ni el formato V1.
- TypeScript app/core, test M6 **15/15** y Node completo **1784/1784**, 0 fallos/omitidos, **26,1 s**. Build QA **439 módulos**; QA visible M6 **37/37**, **234,3 s**, 0 JS; `qa/cables-fusion.mjs` verde sobre esa build (cero coincidencia de ejes 3D en tres ejemplos, 59/59 frontal, 186/186 picking, sin cables fantasma ni JS). HTML offline Build **`5ED0A3B393`** fresco, idéntico y reproducible; smoke `file://` V3–V9 `TODO OK`, 0 JS/HTTP externo obligatorio. No se ejecutó el gate M9 integral, ni la escena densa de rutas M6 manuales. **M6 y 1.0 siguen parciales.**

### CAB-24 — localidad aún no resuelta, con reproducción durable

- `qa/diagnostico-localidad.mjs` compara las rutas de 28 conductores antes y después de convertir solamente `w4` a ruta manual XYZ en el ejemplo de arranque directo. Cambian **8 de 27 cables ajenos**. Tras serializar y reabrir, los 8 siguen con la geometría redistribuida: el problema no desaparece por guardar. `--assert-locality` da exit 1 deliberadamente y permanece fuera del gate verde. El contrato y los límites de la solución se detallan en `docs/avance/CAB24_LOCALIDAD.md`.
- No se cambió el producto, el formato ni el algoritmo de ruteo en este incremento. La decisión de representación persistente de planes automáticos sigue pendiente; no se afirmará CAB-24, M6 o 1.0 como aceptado por una mejora de caché solo de sesión. La última evidencia de tests/build/HTML sigue perteneciendo al checkpoint MON-04, no a una nueva campaña final.

### CAB-24 — núcleo V4 de planes asignados, todavía sin flujo de edición

- El modelo V4 y el lector admiten un plan automático por conductor, separado de la ruta M6 manual. Puntos XYZ y nodos XY se guardan como series planas sin redondeo; fuente, radio, anclajes y entorno local permiten rechazar importaciones obsoletas o hostiles en vez de replanificarlas sin avisar. El renderer consume el plan vigente y el Worker recibe el mismo campo. La captura es una operación explícita de API, nunca un efecto de render o carga.
- Pruebas focales del plan **8/8**: roundtrip exacto, `w4` editado con los otros 27 preservados, inversión de arrays, los cinco ejemplos sin cambio visual, ducto cercano/aparato lejano, archivo obsoleto y entradas corruptas. TypeScript app/core verde; suite Node integrada **1794/1794**, 0 fallos/omitidos, 26,8 s. Build QA **441 módulos**, 7,5 s. El `pnpm test` del host intentó consultar el registro npm pese a estar instalado y se interrumpió tras errores EACCES; se usaron los binarios locales de TypeScript y Node para la validación real. La sonda del flujo cotidiano continúa roja; CAB-24/M6/1.0 no están aceptados.

### CAB-24 — edición focal con planes automáticos durables

- La interfaz congela planes válidos antes de tocar cables y vuelve a aceptar solo la ruta automática editada. La ruta de los otros 27 conductores del arranque directo permanece exacta al cambiar `w4`, guardar y reabrir. Un nuevo conductor de mayor sección conectado al mismo borne se calcula frente a planes existentes sin invalidarlos ni fusionarse con ellos. El inspector deja pendientes los planes dependientes de una canaleta movida, conserva su geometría anterior sin dibujarla y ofrece una propuesta con avisos y confirmación. Undo/Redo y reapertura preservan la decisión. Escape de drag manual restaura el proyecto completo, incluidos planes capturados durante el gesto.
- Validación del incremento: TypeScript app/core verde; Node **1798/1798**, 0 fallos/omitidos, **38,0 s** mediante binarios locales; build QA **441 módulos**, **8,25 s**. `qa/localidad-planes-m6.mjs` **11/11**, **81,1 s**, 0 JS; `qa/ruta-fisica-m6.mjs` **37/37**, **233,0 s**, 0 JS. `qa/cables-fusion.mjs` verde sobre la misma build: cero ejes coincidentes en tres ejemplos, 59/59 frontal, 186/186 picking semántico, 61/61 sin fantasmas y 0 JS. `pnpm test` sigue pendiente de validación como wrapper en este host; los binarios locales ejecutaron exactamente typecheck/compilación/Node.
- Sigue **sin aceptación integral CAB-24/M6/1.0**: falta comprobar desde UI la creación de un cable en borne ya ocupado, movimiento de aparato con anclajes afectados, reservas y alternativas en canaleta densa, coste de autosave/snapshots y criterio físico visual. No se corrió el gate M9 completo, ni build/paquete de producción en este incremento. La sonda legacy que muta el objeto directamente conserva su contraejemplo sin pasar por la operación explícita de UI.

### CAB-24 — conexión y anclaje desde la interfaz

- El formulario real de un aparato añade un conductor de 6 mm² que comparte borne con `w4`; el nuevo recibe inmediatamente un plan automático y los otros 28 no cambian. Tras reapertura, el nuevo y los 28 anteriores conservan la ruta exacta. Después se mueve una canaleta: **16/29** planes locales quedan pendientes y los demás siguen dibujados. Mover `q1` desde su posición numérica deja pendiente `w4` sin representar un anclaje viejo; Undo restaura el aparato y el plan.
- App/core TypeScript y Node integrado **1798/1798**, 0 fallos/omitidos, **37,4 s**; build QA **441 módulos**, **8,82 s**. `qa/localidad-planes-m6.mjs` **16/16**, **156,8 s**, sin errores JS. La QA M6 37/37 y fusión/picking verdes documentadas arriba son del checkpoint inmediatamente anterior al añadido del plan al conectar; no se presentan como nueva ejecución sobre esta modificación. CAB-24/M6/1.0 todavía no tienen aceptación integral por canaleta densa, alternativas, coste de guardado y campaña M9 final.

### CAB-24 — localidad en ejemplo denso

- `qa/localidad-densa-m6.mjs` cubre por navegador el ejemplo estrella-triángulo de 61 conexiones y 59 rutas físicas: editar `w18` mantiene exactas las otras 60 rutas y, tras persistir/reabrir, conserva los 59 planes. La versión ampliada pasó **5/5** en **48,4 s**, 0 errores JS; segunda edición visible de color y recarga conservan ID/documento y el número de snapshots queda **1/1**. La edición hasta los 59 planes tardó **3,3 s** en este host; el documento serializado ocupó **299 972 bytes**. Una captura frontal-perspectiva muestra una escena cargada, pero no prueba radios, holgura ni ausencia de cruces desde todos los ángulos. Latencia/cuota de IndexedDB multiproyecto denso siguen sin medir.
- Este checkpoint de QA no modifica el producto. La batería Node **1798/1798**, build QA y QA focal **16/16** pertenecen al mismo HEAD de producto; la campaña histórica M9 completa y aceptación integral M6/1.0 siguen pendientes.

### CAB-24 — retención de snapshots densos

- La prueba focal del repositorio en memoria conserva veinte copias completas de un tablero con 59 planes: **6 031 696 bytes JSON** de snapshots frente a **301 581 bytes JSON** del documento, sin overhead de IndexedDB. La poda retira el más antiguo; restaurar no degrada rutas aceptadas y el otro proyecto mantiene su snapshot independiente. Prueba focal **1/1** en **4,75 s** (incluye ruteo, no mide latencia aislada de autosave); TypeScript app/core y batería Node integrada **1799/1799**, 0 fallos/omitidas, **29,8 s**. Quedan sin comprobar la cuota y latencia reales de IndexedDB en multiproyecto denso. CAB-24/M6/1.0 no aceptados por esta medición.

### CAB-24 — crecimiento secuencial del mismo borne

- Cuatro altas sucesivas, cinco conductores entre los mismos bornes, conservan los planes anteriores, sobreviven la reapertura y no comparten un eje fuera de las zonas de salida. El primer oráculo contaba **4,97 mm** de solape dentro del borne destino y era físicamente incorrecto; la nueva métrica recorta solo el radio de salida común. Quedan **tres contactos de aislaciones fuera de esa zona** en este caso extremo: el alta focal los advierte después de aceptar, pero todavía no exige decisión previa. Esto no prueba capacidad de cinco conductores en un terminal ni cierra CAB-23/CAB-24. TypeScript app/core y suites focales **148/148**; Node integrado **1801/1801**, build QA **441 módulos**. `qa/cables-fusion.mjs` terminó sin ejes coincidentes en los tres ejemplos, 59/59 frontal, 186/186 picking, 61/61 sin fantasmas y 0 JS sobre el cambio geométrico. La QA visible de localidad, ya con el aviso final, pasó **17/17** en **152,4 s**, 0 JS.
- La repetición integrada posterior al aviso UI pasó **1801/1801** en **31,2 s**, 0 fallos/omitidas. Con la build QA final, `qa/localidad-densa-m6.mjs` pasó **5/5** en **51,5 s** (59 planes, snapshot 1/1, 0 JS) y `qa/ruta-fisica-m6.mjs` pasó **37/37** en **251,4 s**, 0 JS. No se ejecutó el gate histórico M9 completo ni se aceptó visualmente el haz extremo. CAB-23/CAB-24/M6/1.0 permanecen parciales.

### Checkpoint CAB-23/CAB-24 — propuesta previa y anclajes de campo

- El alta automática desde formulario o gesto 3D sin codos se prepara en una copia. Cuando hay avisos, el diálogo precede al cambio persistente y cancelar deja intactos cableado e historial. No hay todavía alternativas de ruta ni revisión visible de las propuestas sin avisos; CAB-23 no está cerrado.
- La entrada de campo se calcula en un módulo puro compartido con el renderer. Si un aparato de campo añadido mueve anclajes repartidos, sus planes quedan pendientes; una importación con planes activos obsoletos se rechaza. La regresión focal cubre el caso.
- Antes del cierre se observaron test focal **16/16**, TypeScript app/core y Node **1804/1804** (0 fallos/omitidos, 23,6 s), build QA **443 módulos** (12,95 s) y `qa/prender-desde-cero.mjs` verde con 13 cables persistidos y 0 JS. TypeScript/Node/Vite se ejecutaron con binarios locales porque `pnpm` intentó consultar un registro bloqueado. La QA de localidad **20/20** es anterior al ajuste de anclajes y no sirve de aceptación final del conjunto.
- `qa/simulacion-industrial.mjs` está **roja** en el guardado del circuito VFD creado por UI: el repositorio indica `ProyectoPersistenciaInvalido`. Las maniobras previas pasaron; la causa interna aún no se identificó. Se añadió impresión de causas encadenadas para la siguiente ejecución. No declarar gate histórico, M6 ni 1.0 verdes.

### Checkpoint CAB-24 — firma de entorno persistente compatible con V4

- El bloqueo industrial anterior quedó explicado y corregido: `JSON.stringify` dependía del orden de claves de los bloques de terminales; el lector los reconstruía en otro orden. Doce planes válidos se rechazaban al guardar/cargar el tablero VFD. La firma nueva usa un orden fijo; las firmas V4 previas se comparan por contenido JSON cuando la cadena difiere. Cambiar físicamente el bloque sigue invalidando el plan. Render y cargador comparten el criterio.
- TypeScript app/core y Node **1805/1805**, 0 fallos/omitidos; build QA **443 módulos**. QA industrial completa **123/123** en **307,3 s**, 14 cables VFD persistidos y cambio de ejemplo correcto, 0 JS. La aserción de armadura 3D ahora espera un repintado acotado y continúa exigiendo desplazamiento real; una pasada prematura anterior quedó roja. QA localidad **20/20** en **128,3 s**, 0 JS. QA fusión/picking verde: 0 ejes coincidentes en tres ejemplos, 59/59 frontal, 186/186 picks, 61/61 sin fantasmas, 0 JS.
- No se ha ejecutado en este checkpoint el gate histórico M9 completo ni se ha aceptado la canaleta densa, las alternativas de ruta, radios/metraje, migración integral o la revisión visual final. CAB-23, CAB-24, M6, M9 y la versión funcional 1.0 continúan sin aceptación integral.
