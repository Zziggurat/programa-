# Campaña funcional hacia 1.0 — estado vigente

## Referencia y alcance

- Contrato: `docs/avance/TABLEROSTUDIO_MASTER_SPEC.md` (versión 1.0, 2026-09-22). El original aportado tenía SHA-256 `350ce427bcd0e4989a424def5334f664edeee90808a20a068def0bfe6e13bea8`.
- Baseline verificado: `main`, `origin/main` y `tablerostudio-v9` en `4c2924c4d12f4dc0982a7f0bf28d94e52b00366f`. Repositorio `Zziggurat/programa-`.
- Rama de desarrollo: `roadmap/1.0-functional`. `main` sigue como referencia estable. No publicar una versión 1.0 al terminar esta campaña.
- Estado terminal buscado: `FUNCTIONAL_COMPLETE`, antes de los encargos separados de arquitectura/datos y Project Breaker.

## Hitos

| Hito | Estado | Frontera actual |
|---|---|---|
| M0 · preparación | EN_CURSO | R1 30/100 conserva 5/5 hashes exactos. Mover aparatos y editar uniones de cable recalcula rutas en Worker inline con estado visible, invalidación y cancelación por Undo, también en HTML offline. En NVIDIA RTX 5070 Ti/D3D11: cinco drags dieron gesto host p95 378,5 ms (antes 3817,3), listener `pointerup` p95 37,4 ms y render p95 16,3 ms, sin tareas largas. Selección host p95 154 ms pero listener p95 46,3 ms; las rutas completas aún tardan p95 4523,6 ms. **R1 NO ACEPTADO** hasta verificar experiencia y coste residual. |
| M1 · flujo y componentes | EN_CURSO | Carcasa opcional seleccionable desde el asistente, recorte/escala visibles, ficha exacta, bornes/bloques, adopción A/B y portabilidad. Contrato integrado contactor A/B+PNG r2+paquete limpio probado; QA asistente 19/19, adopción 24/24, carcasa 15/15 y recorrido largo contactor+simulación+importación verde. Falta aceptación visual/humana y campaña M9; no se declara M1 aceptado. |
| M2 · esquema | EN_CURSO | Vista opt-in y una red; conexión entre hojas con ruta física pendiente (QA 19/19); cruces puntuales sin unión muestran hueco. ESQ-04 distingue vista de aparato. ESQ-08 renumera con preview y conserva etiqueta en documentos; canal PLC/terminal y circuito/hoja se proyectan por anclajes, con índice navegable y CSV de revisión. ESQ-09 navega por ID entre esquema, tablero, datos técnicos e incidencias (QA 8/8). ESQ-10 proyecta el resultado vigente de simulación sin mutar exportaciones (QA 21/21). ESQ-07 permite crear, editar, reordenar y borrar folios vacíos por ID con preview/Undo/reapertura; título/clase declarados aparecen en SVG/PDF/DXF y en el índice offline. Formato físico A3/A2 apaisado por folio, con A3 heredado, ya se monta y exporta sin alterar la red; el QA focal tras corregir encuadre A2 pasó 12/12. ESQ-05 permite arrastrar una vista M2 a casilla libre, incluso entre folios, alinear 2–64 vistas del mismo folio con un Undo, localizar/encuadrar la vista exacta y desplazar el lienzo con Space+primario o botón medio sin alterar el esquema; copia/pega de vistas completas como aparatos nuevos y giro 180° tienen prueba focal. El giro 90°, vistas funcionales desdobladas, símbolos personalizados y la edición diaria completa siguen pendientes. La aceptación multihoja focal pasó 18/18. El solape colineal independiente se advierte en SVG/PDF/DXF, pero no tiene desvío seguro ni selección exclusiva con ratón en el tramo totalmente superpuesto. ESQ-06 inventaría la plantilla local efectivamente dibujada en 22 tipos, inspector y exportación; licencia individual y conformidad normativa NO VERIFICADAS, sin biblioteca personalizada; M2 NO aceptado. |
| M3 · documentación | EN_CURSO | DOC-01/02 conservan procedencia y ZIP único verificable. DOC-03 archiva emisiones declaradas y compara revisiones; DOC-04 cubre aparatos, bornes y extremos; DOC-05 separa longitud eléctrica, ruta 2D y corte propuesto; DOC-06/07 mejoran impresión y CSV; DOC-08 pide permiso visible para imágenes del PDF y prueba portabilidad offline; DOC-09 evita aprobación implícita y emite borradores con fallos. Son incrementos focales, no aceptación M3: faltan revisión humana del impreso y campaña integrada sobre el último SHA. La manguera de Proyecto sigue NO MODELADA. |
| M4 · simulación práctica | EN_CURSO | SIM-03: fallo visible de boya, caída de KM y bomba parada (QA 9/9). SIM-10 sección declarada: cambio visible 2,5→6 mm² refresca física energizada y Undo/Redo, QA 8/8 sin JS. Ruta→física espera política de longitud M6; no aceptar SIM-10 completo ni los demás SIM por arrastre. |
| M5 · montaje/3D | PENDIENTE | Contratos de anclaje y calidad visual requieren evidencia nueva. |
| M6 · cables | PENDIENTE | Reconstrucción integral autorizada con migración; rutas manuales y topología deben preservarse. |
| M7 · CAD/mundo | PENDIENTE | DWG privado disponible; licencia/revisión del derivado publicado sin acreditar. |
| M8 · UX integrada | PENDIENTE | Consolidar recorridos, no abrir subsistemas duplicados. |
| M9 · cierre funcional | PENDIENTE | No iniciar hasta completar y verificar los flujos obligatorios. |

## Siguiente acción y recuperación

1. R1: Worker inline evita bloqueos largos en mover aparatos y editar uniones, con Undo/cancelación, pero el reparto global tarda ~4,5 s por gesto. Revisar la experiencia mientras espera, la creación de cables y el hardware de referencia adicional; no confundir latencia host Playwright con listener real ni aceptar R1 por una sola GPU.
2. M2: conservar el formato A3/A2 por folio y ampliar ESQ-05/06 desde la copia de aparatos completos y giro 180° hacia edición diaria de vistas desdobladas, giro 90° solo si se justifica geométricamente, símbolos personalizados y verificación documental de derechos por biblioteca. El solape colineal necesita una solución gráfica comprobable, no desplazar hilos automáticamente si fabrica T o atraviesa símbolos. Las referencias PLC/circuito indican solo alimentación identificada y anclajes únicos; no se presentan como retornos exhaustivos. Preservar vínculos, rutas pendientes, etiquetas persistidas y archivo DOC-03. La impresión física requiere revisión humana antes de aceptar M2.
3. M3: repetir campaña documental sobre el SHA integrado y someter el paquete impreso a revisión humana antes de aceptar el hito. El archivo de emisiones guarda proyecto+hash, no los bytes del ZIP; nunca presentarlo como copia recuperable ni aprobación. Tras M5/M6, reincorporar planos/longitudes físicas desde la misma revisión.
4. Cerrar evidencia M1 de producto y revisión humana de apariencia/montaje; no sustituirla por tests o capturas automatizadas.
5. M4: continuar SIM-01…10; la sección en vivo está cubierta, pero SIM-10 longitud de ruta espera la política de M6.

## Checkpoint de reanudación — 2026-09-23

- Rama: `roadmap/1.0-functional`; `main` y `origin/main` permanecen en V9 `4c2924c`.
- Cortes nuevos y separados: `a955637` (carcasa/portabilidad), `6f04a10` (vistas esquemáticas), `5a98001` (recorte UI), `2ce0141` (edición de vistas). `6f4b222` (reconciliación de mallas) ya estaba respaldado en la rama remota.
- TypeScript app y core: verde en el estado combinado. Pruebas focales combinadas: 25/25, 0 omitidas. QA visible de recorte 8/8 en 128,8 s y QA de vistas 6/6, ambos sin errores JavaScript, con navegador/servidor cerrados. Build QA focal de los cortes: verde.
- **No se ejecutó campaña completa de pruebas ni CI sobre este nuevo HEAD.** Estas comprobaciones focales no equivalen a aceptación M1, M2, M0 ni `FUNCTIONAL_COMPLETE`.

Esta campaña no incluye publicación automática del plano privado, arquitectura global, Project Breaker ni una release 1.0. Ninguna aceptación humana se presume realizada.

## Checkpoint de continuidad — 2026-09-23, M0/M3/M4 focal

- Rama `roadmap/1.0-functional` respaldada en remoto hasta `49c9db4`; `main` continúa en V9. Nuevos commits: `6c7052a` corrige solo el gesto visible del benchmark, `806d8cc` invalida el resultado eléctrico tras cambiar sección y tras Undo/Redo, `49c9db4` prueba identidad documental multivista. No se promocionó a `main`.
- Benchmark R1 focal en el mismo fixture 30/100 (SHA `f860d627…`), Chromium 1243/SwiftShader, n=1: selección host 4,70 s, drag host 24,43 s, seis movimientos 16,76 s, `pointerup` canvas 4,83 s, reparto 4,76 s, 10/98 rutas y 10 mallas modificadas; picking y undo/redo verdes, 0 errores JS. El primer intento rojo fue espera implícita de navegación del arnés tras un clic ya recibido, no aserción funcional; el segundo cerró navegador/servidor. **R1 NO ACEPTADO**: no hay GPU física ni cuantiles representativos.
- SIM-10 sección en vivo: `qa/seccion-en-vivo-m4.mjs` usa ejemplo y copia visibles, cambia 2,5→6 mm² en el inspector con el circuito energizado, comprueba R `0,137928→0,05747 Ω` y Undo/Redo; 8/8, 0 JS, 35,5 s, browser/servidor cerrados. El primer recorrido descubrió que Undo restauraba documento pero no snapshot eléctrico; quedó corregido, no silenciado. El cálculo de longitud a partir de ruta física sigue pendiente.
- M3: `test/documentacion-multivista-m3.test.ts` comprueba tres vistas de un contactor, una identidad/BOM, tres conexiones, dos referencias gráficas del enlace interhoja y exactitud de terminales en Ingeniería; 1/1 focal. No demuestra todavía procedencia uniforme ni paquete documental único.
- App/core TypeScript verdes; batería integrada posterior a esos tres commits: 1530/1530 en 31,15 s, 0 fallos y 0 skipped. Build QA 409 módulos en 7,81 s. El gate histórico completo M9 no se ha ejecutado sobre este HEAD.

## Checkpoint de continuidad — 2026-09-23, frontera M2/M3

- Rama `roadmap/1.0-functional` respaldada en `origin/roadmap/1.0-functional` hasta `b57fe47ddd65d8e4bff985478e2ef874dc165f60`; `origin/main` sigue en V9 `4c2924c`. Commits de este corte: `2c9c433` contrato físico de ruta pendiente, `02dfd2c` presentación y QA de navegador, `b57fe47` procedencia de HTML/PDF. El checkpoint documental previo `4d2731f` también quedó respaldado. No se promovió a `main`.
- ESQ-02: archivo V2 opt-in conserva conectividad eléctrica sin trazado físico, metros, ocupación, material ni malla 3D; ausencia del campo conserva proyectos V1. Un valor hostil o contradicción con trazado/longitud se diagnostica y congela guardado hasta revisión, no se degrada silenciosamente. Pruebas focales de modelo/Ingeniería 24/24; `qa/ruta-pendiente-m2.mjs` por UI de importación/inspector/reapertura 9/9 en 9,9 s, 0 JS. **Todavía no existe el gesto de conectar desde el esquema.**
- DOC-01 focal: HTML, dossier PDF y esquema PDF usan Project ID y revisión del repositorio solo tras `flush` confirmado; ejemplos quedan efímeros. La numeración PDF opera en copia. El dossier descarga exactamente el Blob mostrado; edición, cambio de identidad o revisión invalidan preview. `qa/procedencia-documental-m3.mjs` 10/10 en 83 s, 0 JS, browser/servidor cerrados. SVG/DXF/CSV y paquete documental completo **NO VERIFICADOS** para esta procedencia; DOC-01 sigue EN_CURSO.
- Typechecks app/core y batería Node integrada con exit 0 tras el corte; build QA 410 módulos verde. La campaña histórica M9, GPU física, aceptación visual humana y `FUNCTIONAL_COMPLETE` siguen pendientes.

## Checkpoint de continuidad — 2026-09-23 noche

- Rama `roadmap/1.0-functional`; `main` permanece en V9. Cortes locales nuevos desde `962bcc5`: `9289fb9` carcasa UI, `d59c87f` salida abierta de sensor, `12a79f3` y `d1f2041` rejilla, `dec2567`/`4262b37` aceptación M1, `0712c8e` test visual, `b552b95`/`4072c0d` activación M2 y `7e0bdb1` test de rótulos. Registrar SHA de respaldo remoto tras el push de la rama; no promover a main.
- Typecheck app/core verdes; 1529/1529 pruebas en 25,57 s, 0 fallos/omitidas. Build QA: 409 módulos en 6,46 s. Primera pasada completa roja por test estático obsoleto de serigrafía, corregido sin cambiar producto; la repetición íntegra es la evidencia válida.
- QA M4 boya 9/9 en 29,64 s, 0 errores JS; QA M2 5/5, QA asistente 19/19, adopción 24/24 y carcasa 15/15, 0 errores JS. El recorrido largo `qa/componentes-personalizados.mjs` pasó `TODO OK`: contactos polo/NA/NC, 11 cables, guardado/reapertura, paquete en navegador limpio y equivalencia nativa hasta el contrato que el nativo declara; 0 errores JS. No se ejecutó todavía todo el gate histórico M9 sobre este HEAD.
- `qa/rejilla-equivalencia-r1.mjs`: 5/5 escenarios idénticos (28/23/59/98/98 rutas); R1+drag 4,98 s en Node en este equipo. La mejora de CPU no acredita respuesta de arrastre ni certifica comportamiento en GPU física. No queda ningún proceso QA intencionalmente en ejecución.

## Checkpoint de continuidad — 2026-09-24, gesto M2 y coste de rutas M0

- Rama `roadmap/1.0-functional`, sin promoción a `main`. `4bb190c` acota los sólidos candidatos por envolvente XY de cada ruta antes de la comprobación geométrica exacta; `8ebbcc5` habilita conectar bornes desde el esquema y crea una sola conexión eléctrica con `estadoRutaFisica: 'pendiente'`, sin ruta, malla, longitud ni material supuestos. Las entradas anteriores describen el estado de sus propios SHA y se conservan como evidencia histórica.
- El gesto M2 parte de un borne real visible, conserva el origen entre hojas, exige confirmación, revalida extremos/duplicados/proyecto y permite cancelar; Undo/Redo, persistencia y reapertura mantienen una sola identidad eléctrica. `qa/esquema-conexion-pendiente-m2.mjs`: **19/19**, 0 fallos y 0 errores JS, 90,6 s. Es evidencia focal, no aceptación de ESQ-03 ni del hito M2 completo.
- Tras integrar esos cortes, typechecks app/core verdes; batería Node **1551/1551**, 0 fallos, 0 omitidos, 28,8 s; build QA **410 módulos** en aproximadamente **6,8 s**. No se atribuye a este SHA la campaña histórica completa de navegador ni CI.
- M0 conserva 5/5 salidas exactas del repartidor en los escenarios oficiales. Dos pares exploratorios en el mismo proceso Node: 6153→5010 ms y 6241→4805 ms; R1-drag 5280→4410 ms en otra pareja. No son cuantiles ni prueba en GPU física; **R1 NO ACEPTADO**. M3 DOC-01 sigue parcial para SVG/DXF/CSV y paquete; M4 longitud de ruta→física no se abordó en este corte y espera política persistente de origen/revisión, no medición de la malla renderizada.
- Regresiones de navegador afectadas por el cambio: fusión **verde** en 189,5 s (59/59 puntos frontales, 186/186 clics semánticos, 0 ejes 3D coincidentes); activación/desdoblamiento M2 **verde** en 41,3 s tras corregir únicamente un selector QA ambiguo; desconexión M2 **verde** en 111,1 s; fixture de puerta **verde** en 19,6 s, sin geometrías fantasma. Todas reportaron 0 errores JS. No se ejecutó la campaña M9 completa ni se afirma aceptación visual/GPU física.

## Checkpoint de continuidad — 2026-09-24, DOC-01 ampliado y cruces ESQ-03

- El código de este corte M2/M3 quedó separado en `e0cbb3a` (Ingeniería), `557150d` (harness), `a717a7a` (esquema) y `d65a1e3` (entregables), sin cambios de código después de la validación combinada. No acredita CI ni la campaña histórica de navegador. ESQ-03 distingue cruces puntuales interiores y en T sin conexión de nudos de borne real sin alterar `Proyecto`: el conductor que debe apartarse recibe un hueco en SVG, PDF y DXF y el hit-target SVG usa el mismo tramo visible. En el caso de una coincidencia sobre un nudo ajeno, el hilo independiente también queda separado gráficamente. Solapes colineales independientes, nudo DXF relleno, renumeración y aceptación multihoja siguen abiertos; M2 continúa EN_CURSO.
- DOC-01 reutiliza procedencia confirmada de repositorio o marca efímera explícita después de confirmar revisión/snapshot en la UI. La cobertura focal alcanza esquema SVG/DXF, placa DXF, rótulos PDF, Ingeniería V7 JSON/HTML/cuatro CSV y Diseño V9 exportado, además de HTML/dossier/esquema PDF previos. `qa/procedencia-documental-m3.mjs`: **16/16**, 0 errores JS, **89,207 s** en la repetición sobre el corte final; `qa/esquema-dossier.mjs`: **28/28**, 0 errores JS, **134,8 s** antes del ajuste gráfico en T; `qa/datos-proyecto.mjs` verde, incluyendo IP vacío y rechazo de IP mal formado; `qa/esquema-desconexion-m2.mjs` verde también después del ajuste en T. La revisión visual automatizada de SVG/PDF confirmó huecos de cruce y cajetín legibles, pero no sustituye aceptación humana de impresión.
- TypeScript app/core verdes; batería Node integrada posterior al ajuste de cruces en T **1584/1584** en **30,949 s**, 0 fallos/omitidos; build QA **411 módulos** en **6,45 s**. Es evidencia focal del árbol probado, no de un commit publicado ni del gate M9 completo. DOC-02 sigue pendiente: falta un paquete de revisión única y reconciliar las distintas claves de agrupación BOM de Documentación e Ingeniería. M0/R1, M1, M2, M3 y `FUNCTIONAL_COMPLETE` no se declaran aceptados.

## Checkpoint para reanudar — 2026-09-24 noche, DOC-02 incremental

- Tras el checkpoint anterior, `c5b5fb7` unificó la agrupación BOM de dossier/Ingeniería desde instancias persistentes y `8f72137` añadió ZIP32 portátil con manifiesto SHA-256; `2ed20a3` añadió lista de señales/IO vinculada a bornes y conexiones reales. `ef3683e` integró ensamblador e interfaz; `89c38bc` cubrió la descarga humana en navegador. Trabajar solo en `roadmap/1.0-functional`; `main` sigue siendo V9 estable. No publicar 1.0.
- «Ingeniería → Documentación → Paquete de revisión ZIP» toma una copia tras `flush` confirmado (o marca un ejemplo efímero), deriva esquema SVG/PDF, dossier HTML/PDF, informe Ingeniería JSON/HTML/CSV, listas de aparatos/conexiones/conductores/borneros/referencias/IO e índice HTML sin scripts ni recursos remotos. Un manifiesto verifica tamaños/hash de cada archivo. Si cambia la identidad, contenido o revisión antes de descargar, se rechaza el resultado. Las decisiones V9 persistidas se rotulan como históricas, no como una evaluación vigente.
- Verificación tras integrar: TypeScript app/core verde; **1604/1604** pruebas Node, 0 fallos/omitidas, **19,558 s**; build QA **417 módulos** en **6,24 s**; `qa/ingenieria-documentacion.mjs` **13/13** en **24,8 s**, 0 errores JavaScript, con descarga real del ZIP desde UI. ZIP abierto además por Python `zipfile` con CRC válido. No se ejecutó el gate histórico completo ni CI sobre este SHA.
- **M3 y DOC-02 permanecen EN_CURSO:** faltan pruebas de navegador de edición/cambio de proyecto durante la preparación del ZIP, verificación offline extraída del paquete, recuento de manguera frente a hilos, revisión humana de impresión y DOC-03…09. Una BOM con igual SKU y distintas variantes nominales sale en filas separadas, pero las tablas heredadas no explican aún la causa. ESQ-03 conserva el límite de solapes colineales. No declarar `FUNCTIONAL_COMPLETE`.

## Checkpoint de continuidad — 2026-09-24, DOC-02 offline y variantes

- Desde `96e7057`, `45de890` hizo visible la identidad/variante nominal que ya separaba partidas del BOM canónico. Dossier HTML/PDF e Ingeniería HTML/CSV explican, por ejemplo, el mismo SKU con 16 A frente a 20 A sin cambiar su agrupación. El PDF sintético fue renderizado e inspeccionado en las páginas de BOM e índice: filas y detalle legibles, sin recorte; queda pendiente aprobación humana de impresión real. `76190f1` añadió dos regresiones de navegador y un contrato de manguera frente a hilos individuales.
- `qa/paquete-documental-offline.mjs`: descarga UI, lectura ZIP STORE independiente, CRC/SHA-256 de las 16 piezas, extracción segura, apertura de índice/SVG/dossier/Ingeniería desde `file://` en sesión nueva offline, **17/17**, 0 errores JS y 0 solicitudes HTTP(S), **25,9 s** en el último corte. `qa/paquete-revision-obsoleto-m3.mjs`: crea A/B por UI, interrumpe una digestión SHA-256 real para entrelazar edición A y cambio A→B, rechaza ambos ZIP obsoletos y exporta solo B, **6/6**, 0 JS, **60,4 s**. `qa/ingenieria-documentacion.mjs` **13/13** en **34,4 s** y `qa/esquema-dossier.mjs` exit 0 con sus comprobaciones visibles, sin errores JS. Browser y servidor cerrados por cada arnés.
- TypeScript app/core verdes; batería integrada **1606/1606**, 0 fallos/omitidas, **25,845 s**; build QA **417 módulos**, **8,29 s**. `test/paquete-manguera-m3.test.ts` contrasta tiradas 3×/4× del levantamiento con siete conductores individuales y exige que el ZIP no convierta hilos en metros de manguera. El levantamiento de cubierta es global y no pertenece al `Proyecto`: incorporar esas tiradas en la revisión A/B mezclaría procedencia. La compra de manguera de un proyecto queda **NO MODELADA** hasta que exista entidad/vínculo persistente; no se extrapola por color/ruta.
- Este checkpoint no acredita CI, campaña histórica M9 ni aceptación humana. DOC-03 no puede basarse en snapshots de recuperación: pueden podarse o borrarse al eliminar el proyecto. Se necesita archivo inmutable de revisión emitida, no una comparación oportunista con autosave. `FUNCTIONAL_COMPLETE` sigue abierto.

## Checkpoint de continuidad — 2026-09-24, ESQ-04

- Rama `roadmap/1.0-functional`, sin promoción a `main`. `7a7b11c` añade QA navegador, `965ff92` plan de borrado puro y `659c671` integra acción eléctrica separada de «Borrar vista» en el esquema y aplica el mismo contrato al inspector 3D/multiselección.
- El preview enumera conexiones, vistas, hojas y datos técnicos afectados antes de borrar; cancelar no muta ni crea Undo. Una respuesta tardía de diálogo no se aplica a otro proyecto o a una revisión alterada. Deshacer/Rehacer restaura o retira el conjunto en una sola operación. Se conservan revisiones técnicas compartidas, hojas y archivo de decisiones V9; los vínculos directos, roles esclavos y metadatos de conductor afectados no quedan colgando.
- TypeScript app/core verde; batería integrada **1647/1647**, 0 fallos/omitidas, **94,6 s** mientras corrían QA navegador; build QA **422 módulos** en **8,03 s**. `qa/esquema-eliminacion-m2.mjs` **12/12** en **28,5 s**, 0 JS; QA existentes de desconexión y activación de representaciones exit 0, 6 y 5 hitos respectivamente, 0 JS, cierre limpio de browser/servidor. Evidencia focal, no campaña M9 ni aceptación M2 completa.
- Siguiente frontera M2: renumeración con preview estable y protección de designaciones congeladas; referencias cruzadas amplias, solapes colineales y aceptación multihoja siguen abiertos.

## Checkpoint de continuidad — 2026-09-24, renumeración ESQ-08

- `822fb70` cubre navegador, `96890f6` motor puro, `ab689f6` interfaz, `ad7e6df` documentos y `5e7a544` preservación de etiquetas al exportar. La renumeración ocurre solo tras vista previa y confirmación; reserva el texto real congelado aun si `numero` quedó obsoleto, detecta duplicados y conserva IDs, hojas, vistas, conexiones y archivo. Los conflictos bloquean toda aplicación. No hay renumeración automática al guardar.
- Antes de corregir la salida, PDF y paquete DOC-02 volvieron a llamar al numerador legacy en una copia; podían mostrar `-K100` donde el esquema persistía `-K1`. Ahora ambas emisiones mantienen las designaciones de la revisión. Si un aparato no tiene marca, los listados que la necesitan usan su ID estable; se debe renumerar explícitamente antes de una entrega profesional. La prueba documental usa PDF real y todas las piezas relevantes del paquete, sin mutar el Proyecto.
- TypeScript app/core verde; Node integrado **1654/1654** en **26,13 s**, 0 fallos/omitidas; build QA **423 módulos** en **7,23 s**. `qa/esquema-renumeracion-m2.mjs` **11/11** en **27,2 s**, 0 JS; `qa/ingenieria-documentacion.mjs` **13/13** en **36,5 s**, 0 JS; ZIP offline **17/17** en **17,6 s**, 0 JS. La primera invocación de Ingeniería no encontró el Chromium predeterminado de Playwright, sin llegar a ninguna comprobación; se repitió con `PW_CHROME` apuntando al Chromium local instalado. Esto es evidencia focal, no campaña M9/CI ni aceptación M2 completa.

## Checkpoint de continuidad — 2026-09-24, aceptación multihoja y referencias ESQ-08

- En `roadmap/1.0-functional`, sin promoción a `main`: `1df6211` diagnostica solapes colineales independientes de longitud positiva y los anuncia en SVG/PDF/DXF sin inventar un nudo; `7d0fc29`/`724f05f` prueban por navegador dos hojas, mando/potencia de un KM único, desconexión/reconexión real con ruta física pendiente, borrado/restauración de vista, simulación y tiempos de fase. `690ce87` advierte aparatos/conexiones sin vista también en exportaciones; `ab4d3f8` repone la vista del mismo aparato, con validación de casilla y Undo. El QA multihoja pasó **18/18**, 0 errores JS, **316 s** aislado; otra repetición con tests Node concurrentes tardó 401,2 s y no se usa como cifra de producto. El caso `w24` totalmente solapado necesitó Enter sobre el objeto accesible: no se afirma selección exclusiva con ratón.
- `8af55e1` deriva canal PLC↔terminal y circuito↔hojas solo desde borne/anclaje gráfico único e identidad persistente; diagnostica ausencias/ambigüedades, no elige por rótulo ni orden del array. `eaa5aef` añade índice desplegable con enlaces por IDs de hoja/vista, alcance de circuitos no exhaustivo y diagnósticos; navegador **11/11** en **11,0 s**, 0 JS. `ca282f6` exporta estas mismas referencias/diagnósticos al ZIP de una revisión; tests documentales focales **9/9**, incluidas dos regresiones nuevas, 0 fallos/omitidas.
- Antes de la UI/CSV finales, la batería Node integrada fue **1671/1671**, 0 fallos/omitidas, 29,18 s; app/core TypeScript y build QA **423 módulos** verdes. Tras CSV, core TypeScript y tests documentales focales verdes. La campaña histórica M9/CI, QA documental offline del nuevo ZIP y prueba integrada posterior a todos los cambios siguen pendientes. M2 y `FUNCTIONAL_COMPLETE` permanecen EN_CURSO.

## Checkpoint de continuidad — 2026-09-25, traslado visible ESQ-05

- En `roadmap/1.0-functional`, `060311f` introdujo el plan puro de movimiento por ID de vista y `2e8d496` lo integró en el esquema M2. El preview comprueba folio, rejilla, ocupación y documento vigente; el puntero anuncia la casilla sin escribir en `Proyecto`, sin crear Undo ni reconstruir el SVG en cada `pointermove`. La única aplicación ocurre al soltar sobre un destino válido. El traslado conserva identidad de vista, aparato y circuito y admite cambio de folio por el borde derecho.
- `qa/esquema-mover-vistas-m2.mjs` pasó **18/18**, 0 errores JavaScript, **90,4 s**: libre, ocupado, fuera de rejilla, clic sin arrastre, Escape, `pointercancel`, Undo/Redo, cambio de folio y guardar/reabrir. El arnés midió cierre de página **376 ms**, Chromium **143 ms** y servidor **0 ms**; no constituye una inspección global de procesos. TypeScript app y build QA de **427 módulos** quedaron verdes tras la integración.
- Sobre el checkpoint posterior a `2e8d496`, la batería Node integrada terminó **1705/1705**, 0 fallos/omitidos, **18,53 s**, exit 0. `qa/esquema-representaciones-m2.mjs` terminó con **6 hitos**, 0 JS, **19,3 s** tras acotar al grupo `.simbolo` un selector QA que también contaba sus bornes hijos; `qa/esquema-folios-m2.mjs` pasó **22/22**, 0 JS, **139,4 s**; `qa/esquema-estado-m2.mjs` pasó **21/21**, 0 JS, **92,0 s**. El ajuste del selector quedó en `0eea749`: no modifica el producto ni reduce las aserciones. La campaña integrada M9 sobre un candidato final continúa pendiente.
- En ese corte, ESQ-05 estaba **PARCIAL**: arrastre individual seguro, pero no alineación, grupo/copia/giro ni contrato completo de edición cotidiana. ESQ-07 todavía carecía de tamaño físico por folio (abordado en el checkpoint siguiente); ESQ-06 no acreditaba biblioteca IEC certificada. M2, M9 y `FUNCTIONAL_COMPLETE` no estaban aceptados. `main` permanecía en V9; no se promovió ni etiquetó 1.0 por ese checkpoint.

## Checkpoint de continuidad — 2026-09-25, formato físico por folio ESQ-07

- `cb0bfa2` persiste `formatoPapel?: 'A3' | 'A2'` en cada hoja M2 y monta A3 apaisado 420×297 mm o A2 apaisado 594×420 mm por ID. Ausencia del campo conserva el A3 histórico; A3 explícito y A3 heredado son estados distintos. El cargador rechaza un formato M2 desconocido porque descartarlo cambiaría la geometría; el legacy lo diagnostica. Rejilla, símbolos y enlaces usan las dimensiones de su propia hoja, sin dependencia del orden de arrays ni alteración de conductores.
- `aa701c5` añade selector y preview en Folios, cancelación sin Undo, aplicación única y etiqueta visible; el arrastre ESQ-05 usa las coordenadas del papel actual. `b88c6db` conserva tamaños mixtos en SVG y en el `/MediaBox` de cada página PDF, y muestra dimensiones por folio en el índice offline de la misma revisión. La inspección visual A2 descubrió que, al cerrar Folios, el lienzo seguía con zoom pequeño; `096354b` recalcula `aplicarZoomEsquema()` y la regresión comprueba que A2 se encuadra sin recorte. El formato físico no certifica escala de impresión, normativas ni legibilidad en papel real.
- Sobre el checkpoint con `096354b`: app TypeScript verde; Node **1711/1711** en **18,72 s**, 0 fallos/omitidos, exit 0; build QA **427 módulos** en **5,85 s**; `qa/esquema-formato-folios-m2.mjs` **12/12** en **86,3 s**, 0 JS. Las otras regresiones se ejecutaron antes de ese ajuste: mover vistas **18/18** en **89,7 s**, representaciones **6 hitos** en **21,9 s**, folios **22/22** en **136,0 s**; ZIP offline **17/17** en **5,2 s**, 0 peticiones de red y 0 JS. No mezclar esos pases previos con evidencia exacta del candidato final; esto no es la campaña M9 ni aceptación humana.
- La frontera A3/A2 de ESQ-07 quedó implementada y cubierta focalmente; otros tamaños no están modelados. ESQ-05 avanzado, ESQ-06, solape colineal y campaña/aceptación M9 permanecen abiertos. **M2 sigue EN_CURSO** y `FUNCTIONAL_COMPLETE` no se declara.

## Checkpoint de continuidad — 2026-09-25, selección y alineación ESQ-05

- En `roadmap/1.0-functional`, `564f890` añadió el plan puro de alineación y `ce22858` integró selección/acción visible y QA. Las vistas se seleccionan por ID con Shift+clic o Shift+Enter; el plan trabaja con 2–64 vistas del mismo folio, y aplica la alineación como **un único Undo**. No modifica aparatos, conductores ni el grafo eléctrico. Al cambiar de folio se limpia la selección. Cancelar, no-op y rechazo por destino inválido no escriben en el documento ni crean historial; guardado/reapertura conserva el resultado.
- El área de comprobación de ocupación considera las **cajas de símbolos**, no las etiquetas ni el recorrido de los hilos. No equivale a resolver automáticamente toda interferencia gráfica. ESQ-05 aún necesita localizar/pan, copia y giro para la edición diaria completa; no se declara terminado.
- Frontera verificada: tests focales del plan **9/9**; TypeScript core/app verdes; build QA **428 módulos** en **6,11 s**; batería Node **1720/1720**, 0 fallos/omitidos, **21,1 s**. `qa/esquema-alinear-vistas-m2.mjs` **14/14**, **102,9 s**, 0 errores JS; regresiones de mover **18/18**, **88,4 s**, y folios **22/22**, **136,0 s**, ambas 0 JS. `qa/esquema-representaciones-m2.mjs` completó sus **6 hitos**, exit 0 y 0 JS sobre el mismo árbol.
- Evidencia focal, no campaña M9 ni aceptación M2. No se hizo verificación GPU física ni aprobación visual/humana de impresión; `main` permanece como V9 estable y no se declara 1.0 ni `FUNCTIONAL_COMPLETE`.

## Checkpoint de continuidad — 2026-09-25, localizar y encuadrar ESQ-05

- Después de `da49d63`, `9309541` corrigió la navegación a una **vista exacta por ID**: el enlace en el índice de referencias elige el folio y desplaza el lienzo para mostrar la representación, incluso con zoom alto. Enter conserva el foco en esa vista; los enlaces que no tienen ID gráfico reinician el scroll de forma explícita. El recorte del lienzo permite alcanzar ambos bordes de la hoja. La acción de localización no modifica `Proyecto` ni crea Undo.
- El primer QA ampliado no pasó: entre 14 comprobaciones hubo dos fallos funcionales reales, vista localizada fuera del viewport y borde izquierdo inaccesible. Tras el ajuste UI/CSS, `qa/referencias-esquema-m2.mjs` pasó **16/16** en **14,4 s**, 0 errores JS. App TypeScript verde; build QA **428 módulos** en **6,01 s**; Node **1720/1720** en **18,214 s**, 0 fallos/omitidos.
- Regresiones focales posteriores: mover vistas **18/18** en **81,0 s**; formato A2/A3 **14/14** en **82,8 s**; navegación de issues **8/8** en **22,1 s**; alineación **14/14** en **97,7 s**, exit 0. Todas reportaron 0 JS. El cierre de la suite de alineación tardó decenas de segundos, sin procesos QA huérfanos detectados; no convertir esa duración en métrica de producto.
- ESQ-05 sigue **PARCIAL**: se cubren traslado, alineación y localización/encuadre, pero pan, copia y giro continúan abiertos. Son pruebas focales, no la campaña M9 ni aceptación M2, GPU física o revisión humana. `main` permanece en V9; no declarar `FUNCTIONAL_COMPLETE` ni versión 1.0.

## Checkpoint de continuidad — 2026-09-25, pan del esquema ESQ-05

- `27e77e0`, posterior al checkpoint documental `a98ac32`, permite desplazar el lienzo con foco mediante **Space+botón primario** o **botón medio**. La captura del gesto ocurre antes de handlers de bornes/símbolos para no iniciar conexión, selección o movimiento; durante el pan cambia solo el scroll. Los punteros táctiles quedan fuera de esta captura (su gesto nativo aún no se ha probado); las flechas nativas se conservan cuando el lienzo tiene foco. Al terminar se suprime el clic residual y se limpian eventos/estado en `pointerup`, `pointercancel`, `lostpointercapture`, `blur`, Escape y cierre del esquema.
- La primera ejecución de `qa/esquema-pan-m2.mjs` quedó roja porque el lienzo no recibía foco. La regresión final pasó **15/15** en **38,4 s**, 0 errores JS: incluye pérdida de Space por `blur`, cancelación de puntero, Escape, cerrar/reabrir, clic posterior sobre borne, control con Space y flecha, y confirma `Proyecto`/Undo inmutables. No se ocultó el fallo inicial.
- TypeScript app/core verde; build QA **428 módulos** en **6,05 s**; Node **1720/1720** en **18,515 s**, 0 fallos/omitidos. Con pan integrado, navegador: conexión pendiente **19/19** en **53,0 s**, mover **18/18** en **86,6 s**, referencias **16/16** en **16,0 s**, alineación **14/14** en **97,8 s** y formato A2/A3 **14/14** en **80,5 s**; todos 0 JS. Alineación acabó con exit 0 tras cierre lento de arnés, sin procesos QA huérfanos observados.
- ESQ-05 permanece **PARCIAL**: copia/pega y giro no están resueltos. ESQ-06, solape colineal independiente y campaña M9 siguen abiertos. Esto no acredita GPU física ni inspección humana del impreso; **M2 NO ACEPTADO**, sin `FUNCTIONAL_COMPLETE` ni versión 1.0. `main` continúa en V9.

## Checkpoint de continuidad — 2026-09-25, inventario local ESQ-06

- El generador vincula cada símbolo montado —incluidas vistas parciales de bobina/contactos— con un ID estable de plantilla y familia, derivados de la **misma resolución que dibuja los trazos**. El inventario cubre los 22 tipos; `valvula`, `resistencia`, `condensador`, `cable` y `otro` usan caja genérica cuando no declaran otra geometría, sin presentarse como símbolo especializado. El inspector y SVG exportado muestran origen y límites; el PDF lleva nota visible y IDs de plantillas en metadatos. La ficha no se persiste en `Proyecto`, así que no crea otra verdad eléctrica.
- Los trazos actuales se generan localmente en `src/motores/esquema.ts`. `package.json` declara `GPL-2.0-or-later` para el proyecto, **no** autoría/licencia individual acreditada por símbolo. Cada ficha marca `licenciaVerificada: false` y `conformidadNormativa: NO_VERIFICADA`; el comentario de lectura ya no afirma que el dibujo sea IEC. No se importó QElectroTech ni otra biblioteca externa. Una biblioteca personalizada, pruebas normativas y revisión de derechos siguen pendientes; ESQ-06 continúa **PARCIAL**.
- TypeScript app/core, build QA y Node **1726/1726** verdes; QA navegador `qa/esquema-procedencia-m2.mjs` **10/10** en **2,8 s**, 0 errores JS: fuente dedicada frente a válvula genérica, inspector, descarga SVG y constancia de Proyecto/Undo. Regresión de representaciones M2: seis hitos, exit 0, 0 JS en **19,2 s** sobre el ajuste final del inspector. Es evidencia focal, no revisión humana de impresión ni aceptación M2/M9. `main` permanece V9; no declarar `FUNCTIONAL_COMPLETE` ni 1.0.

## Checkpoint de continuidad — 2026-09-25, copia y giro ESQ-05

- `e037259` y `acb2bcf` quedaron respaldados mediante push normal en `origin/roadmap/1.0-functional`. Copiar una vista completa y única crea **otro aparato eléctrico y otra vista**, con IDs distintos y sin conductores; pegar solo en una casilla de folio válida y con hueco físico en la placa. El plan rechaza duplicar anclajes del mismo aparato, destinos ocupados, documentos cambiados, ejemplos de solo lectura y colocación sobre la puerta no resuelta. La operación usa un solo Undo; cancelación y rechazo no dejan aparatos fantasma. La copia desde vistas funcionales desdobladas continúa fuera de este contrato, no se finge como lista.
- `7ec0885` y `cae2afe` permiten giro editorial **solo 180°**: gira trazos y pines alrededor del centro, deja texto legible, no gira el aparato 3D ni modifica el circuito. El campo persistente `giro?: 180` conserva exactamente la orientación legacy al omitirse; el cargador rechaza una orientación desconocida en vez de descartarla. Copiar una vista girada conserva su orientación. Un giro 90° no está permitido hasta definir ocupación, rutas y caja girada de forma segura.
- App/core TypeScript verdes; Node integrado **1734/1734** (0 fallos/omitidos), build QA **430 módulos** en **5,96 s**. `qa/esquema-copiar-vista-m2.mjs` **14/14** en **23,5 s incluyendo cierre de Chromium/servidor**, 0 JS; `qa/esquema-representaciones-m2.mjs` completó sus **6 hitos**, exit 0. La suite nueva cubre folios, rechazo, Undo/Redo, reapertura, anclajes y circuito. Esto es evidencia focal, no revisión visual humana del impreso ni campaña histórica M9. **M2 sigue EN_CURSO; `FUNCTIONAL_COMPLETE` y 1.0 no se declaran.**

## Checkpoint de continuidad — 2026-09-25, respuesta R1 y entrega offline

- Commits funcionales separados: `82446e6` (Worker de rutas, invalidación y cancelación), `4d2d019` (QA R1 y contrato de un único bundle offline), `649f856` (vinculación técnica sin entidad vieja) y `e557bc9` (HTML de producción reproducible). `main` sigue siendo V9; esta es la rama de desarrollo.
- TypeScript app/core y Node **1736/1736** verdes, 0 fallos/omitidos. Gate de navegador **13/13**, 273 comprobaciones en **10:04**. Fusión **59/59** puntos frontales y **186/186** clics semánticos, sin ejes coincidentes; fixture de puerta y V8 importación **55/55** verdes. Entrega offline V3–V9 verde con Build ID **`8254F5160F`**, 0 errores JS/peticiones externas; verificador de frescura y reproducibilidad LF/CRLF verde. El focal R1 empaquetado temporalmente con sonda también probó Worker, unión manual y cancelación por la UI.
- R1 en GPU NVIDIA RTX 5070 Ti: listener de selección p95 **46,3 ms**, listener `pointerup` de aparato p95 **37,4 ms**, render directo p95 **16,3 ms**, 0 tareas largas en cinco drags. El gesto host bajó de **3817,3** a **378,5 ms** p95, pero las rutas completas aún tardan **4523,6 ms** p95. Un gesto de unión manual respondió en **472,6 ms** host (n=1). SwiftShader y otros equipos no están acreditados como fluidos; la inspección humana y el coste total siguen pendientes. **M0/R1 NO ACEPTADO; M1–M9 y `FUNCTIONAL_COMPLETE` siguen abiertos.**
